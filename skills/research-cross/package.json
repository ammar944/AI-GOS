{
  "name": "research-cross",
  "version": "1.0.0",
  "description": "Cross-analysis synthesis for AIGOS v3 - compares completed sourced research skill outputs without collecting new evidence.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "validate": "node --import tsx/esm scripts/validate.ts",
    "sanity-check": "node --import tsx/esm scripts/sanity-check.ts",
    "test": "npm run check && npm run validate && npm run sanity-check example/output.json"
  },
  "dependencies": {
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
