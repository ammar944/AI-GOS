# example/

Fixture that demonstrates a complete, realistic run. Used by CI and as onboarding for future skill authors.

Expected contents:

- `input.json` — realistic onboarding payload matching `references/input-schema.ts`
- `output.json` — fully populated output matching `references/output-schema.ts`

Keep the fixture small but realistic enough to exercise edge cases (null fields, multiple competitors, etc.).
