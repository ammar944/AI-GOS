# assets/

Templates and static files used in the skill's output. Not loaded into Claude's context — read by scripts at render time.

Expected contents:

- `report-shell.html` — editorial HTML shell (masthead, footer, CSS link)
- `styles.css` — shared report styles
- `report-components/*.html` — partial templates (pills, cards, stacked-bar placeholders)
