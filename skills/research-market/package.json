{
  "name": "research-market",
  "version": "0.1.0",
  "type": "module",
  "description": "Market and category research — fans out subagents to collect TAM signals, category maturity, timing, and competitive intensity. Agent collects. TypeScript validates and renders.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "validate": "node --import tsx/esm scripts/validate.ts",
    "sanity-check": "node --import tsx/esm scripts/sanity-check.ts",
    "merge-fragments": "node --import tsx/esm scripts/merge-fragments.ts",
    "report": "node --import tsx/esm scripts/generate-report.ts",
    "orchestrate": "node --import tsx/esm scripts/orchestrate.ts",
    "test": "npm run check && npm run validate && npm run sanity-check example/output.json"
  },
  "dependencies": {
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
