- Fixed the Journey market overview race: Realtime research results now update chat state without auto-waking the model, and `industryMarket` waits for explicit `[SECTION_APPROVED]` before the conversation continues.
- Added a route-level review gate so if the market overview result exists but has not been approved, the agent only prompts the user to review the artifact instead of advancing to ICP questions.
- Reduced `research-worker` market overview latency by switching the industry runner to a smaller default model (`claude-haiku-4-5-20251001` via `RESEARCH_INDUSTRY_MODEL` override), lowering token budget, and biasing the prompt toward fast first-pass coverage.
- Fixed Journey UX regressions from screenshot review: timeout banner now tracks actually running sections instead of missing future prerequisites, research/reasoning parts are filtered out of assistant bubbles so the dedicated artifact trigger card is the only Market Overview card, and the prefill kickoff prompt now says only "Building your market overview now."
- Investigated a separate prefill failure state: when a URL yields 0 extracted fields, the UI now tells the user to check the URL/TLD and offers "Try another URL" instead of only "Continue without prefill." This came up with `saaslaunch.com`, while the public site appears to be `saaslaunch.net`.
- Fixed the next Journey regression where Market Overview research could be dispatched but no loading card appeared. Added `research-dispatch-state` parsing so queued `tool-research*` outputs also mark sections active, which should surface the Module 01 loading card immediately instead of waiting until the final realtime result lands.
- Replaced the fake Market Overview loading copy/progress with real worker activity from `journey_sessions.job_status`. Artifact/loading cards now show queued/running/heartbeat timing instead of invented “scanning Reddit / pulling benchmarks” phrases. True partial artifact content streaming is still a separate backend change.
- Added first-pass real runner activity streaming: research-worker now persists `job_status.updates` with stage events from the Anthropic beta tool runner (`server_tool_use`, `tool_use`, first synthesis text, write-complete), and the Journey artifact panel renders those updates live while research runs.
- Upgraded runner activity streaming from generic stage labels to actual evidence cues: the worker now emits real web-search queries, top returned source titles/domains, and draft fact lines extracted from the streamed JSON snapshot (e.g. `draft market size: ...`) so the loading feed reflects real research data rather than made-up progress text.
