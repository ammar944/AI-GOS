---
name: synthesize-media-plan
description: >
  Media plan synthesis — transforms research and positioning inputs into a multi-phase paid media plan with campaigns, ad groups, and creative angles.
version: 0.1.0
---

# synthesize-media-plan (bridge)

This is the Claude Code bridge for the `synthesize-media-plan` skill. The full implementation lives at `skills/synthesize-media-plan/`.

When Claude invokes this skill:

1. Read the full SKILL.md at `skills/synthesize-media-plan/SKILL.md`
2. Use the prompts in `skills/synthesize-media-plan/references/` to drive collection
3. Run the deterministic tail via `cd skills/synthesize-media-plan && npm run orchestrate` (once scripts exist)

**Status**: scaffolded 2026-04-24. Bridge is functional but the underlying skill is a stub.
