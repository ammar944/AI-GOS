---
phase: 27-design-foundation
plan: 01
type: execute
---

<objective>
Set up design system foundation with Framer Motion, Geist fonts, and CSS tokens.

Purpose: Establish the core dependencies and design tokens that all v2.0 Design Refresh components will build upon.
Output: Framer Motion installed, Geist Sans font configured, comprehensive CSS design tokens in globals.css.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/docs/AI-GOS-DESIGN-SYSTEM.md

**Design spec reference:**
The AI-GOS-DESIGN-SYSTEM.md contains complete color system, typography scale, spacing system, and animation specifications.

**Current state:**
- Layout uses Inter font (should be Geist Sans)
- Geist Mono already configured
- globals.css has SaaSLaunch color system (oklch-based)
- No Framer Motion installed

**Relevant source files:**
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install framer-motion dependency</name>
  <files>package.json, package-lock.json</files>
  <action>
    Install framer-motion package:
    ```
    npm install framer-motion
    ```
    This provides the animation primitives for the design system (motion.div, useMotionValue, AnimatePresence, springs).
  </action>
  <verify>npm ls framer-motion shows version installed, no peer dependency warnings</verify>
  <done>framer-motion appears in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Configure Geist Sans font replacing Inter</name>
  <files>src/app/layout.tsx</files>
  <action>
    Replace Inter with Geist Sans in layout.tsx:

    1. Import Geist from next/font/google (it's available as "Geist")
    2. Configure Geist with:
       - variable: "--font-geist-sans"
       - subsets: ["latin"]
       - display: "swap"
    3. Remove Inter import
    4. Update body className to use geist.variable instead of inter.variable

    Keep Geist_Mono as-is for monospace.

    Note: next/font/google provides Geist as a single import (not Geist_Sans).
  </action>
  <verify>npm run dev starts without font errors, inspect element shows Geist Sans in computed font-family</verify>
  <done>Layout uses Geist Sans, --font-geist-sans CSS variable works, page renders with new font</done>
</task>

<task type="auto">
  <name>Task 3: Add design system CSS tokens</name>
  <files>src/app/globals.css</files>
  <action>
    Add new CSS custom properties to :root and .dark sections in globals.css. Add AFTER the existing variables, in a new clearly commented section.

    **Add to :root (for reference, dark is primary):**
    ```css
    /* ============================================
       AI-GOS Design System v2.0 Tokens
       ============================================ */

    /* Background Hierarchy */
    --bg-base: #000000;
    --bg-elevated: #0a0a0a;
    --bg-surface: #0c0c0c;
    --bg-hover: #111111;
    --bg-active: #181818;

    /* Border System (Opacity-based) */
    --border-subtle: rgba(255, 255, 255, 0.04);
    --border-default: rgba(255, 255, 255, 0.06);
    --border-hover: rgba(255, 255, 255, 0.10);
    --border-focus: rgba(255, 255, 255, 0.15);
    --border-strong: rgba(255, 255, 255, 0.20);

    /* Text Hierarchy */
    --text-primary: #ffffff;
    --text-secondary: #a0a0a0;
    --text-tertiary: #666666;
    --text-quaternary: #444444;
    --text-muted: #333333;

    /* Accent Colors */
    --accent-blue: #3b82f6;
    --accent-blue-hover: #60a5fa;
    --accent-blue-subtle: rgba(59, 130, 246, 0.10);
    --accent-blue-glow: rgba(59, 130, 246, 0.15);
    --accent-purple: #8b5cf6;
    --accent-purple-subtle: rgba(139, 92, 246, 0.20);

    /* Semantic */
    --success: #22c55e;
    --success-subtle: rgba(34, 197, 94, 0.10);
    --warning: #f59e0b;
    --error: #ef4444;

    /* Gradients */
    --gradient-primary: linear-gradient(135deg, #3b82f6 0%, #2563eb 50%, #1d4ed8 100%);
    --gradient-accent: linear-gradient(135deg, #3b82f6, #8b5cf6);
    --gradient-text: linear-gradient(180deg, #ffffff 0%, #888888 100%);
    --gradient-surface: linear-gradient(180deg, rgba(255,255,255,0.03) 0%, transparent 100%);

    /* Spacing (4px grid) */
    --space-0: 0;
    --space-1: 4px;
    --space-2: 8px;
    --space-3: 12px;
    --space-4: 16px;
    --space-5: 20px;
    --space-6: 24px;
    --space-7: 28px;
    --space-8: 32px;
    --space-10: 40px;
    --space-12: 48px;
    --space-14: 56px;
    --space-16: 64px;

    /* Border Radius */
    --radius-sm: 6px;
    --radius-md: 8px;
    --radius-lg: 12px;
    --radius-xl: 16px;
    --radius-2xl: 20px;
    --radius-full: 9999px;
    ```

    **Add keyframe animation for gradient shift:**
    ```css
    @keyframes gradientShift {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    ```

    Note: Keep existing SaaSLaunch/oklch variables - the new tokens supplement them for the v2.0 design refresh components.
  </action>
  <verify>CSS parses without errors (npm run dev), tokens accessible via var(--bg-base) etc in browser DevTools</verify>
  <done>All design system tokens defined in globals.css, gradientShift keyframe exists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] framer-motion in package.json dependencies
- [ ] Page renders with Geist Sans font (check computed styles)
- [ ] CSS variables accessible in browser DevTools (--bg-base, --accent-blue, etc.)
</verification>

<success_criteria>
- framer-motion installed
- Geist Sans font configured and rendering
- All CSS design tokens defined
- No build or runtime errors
- Ready for foundation component development (27-02)
</success_criteria>

<output>
After completion, create `.planning/phases/27-design-foundation/27-01-SUMMARY.md`
</output>
