---
phase: 33-output-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/strategic-blueprint/polished-blueprint-view.tsx
  - src/components/strategic-research/output-section-card.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Complete page displays blueprint as section cards, not markdown"
    - "Each section card shows section content using existing renderers"
    - "Section cards are read-only (no edit buttons, no review buttons)"
    - "Cards follow SaaSLaunch design language"
  artifacts:
    - path: "src/components/strategic-blueprint/polished-blueprint-view.tsx"
      provides: "Main complete page view component with section cards"
      exports: ["PolishedBlueprintView"]
    - path: "src/components/strategic-research/output-section-card.tsx"
      provides: "Read-only section card for output display"
      exports: ["OutputSectionCard"]
  key_links:
    - from: "src/components/strategic-blueprint/polished-blueprint-view.tsx"
      to: "src/components/strategic-research/output-section-card.tsx"
      via: "import OutputSectionCard"
      pattern: "import.*OutputSectionCard"
    - from: "src/components/strategic-research/output-section-card.tsx"
      to: "src/components/strategic-research/section-content.tsx"
      via: "SectionContentRenderer"
      pattern: "SectionContentRenderer"
---

<objective>
Create the PolishedBlueprintView component for the complete page, displaying the blueprint as polished section cards instead of the current markdown document editor.

Purpose: Replace the markdown-based DocumentEditor with a card-based layout that matches the review page aesthetic but is read-only. This addresses OUT-01 and OUT-02.

Output: Two new components - PolishedBlueprintView (container) and OutputSectionCard (simplified read-only card).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SAASLAUNCH-DESIGN-LANGUAGE.md

# Reference existing components for patterns
@src/components/strategic-research/section-card.tsx
@src/components/strategic-research/document-section.tsx
@src/components/strategic-research/section-content.tsx
@src/components/strategic-blueprint/blueprint-viewer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OutputSectionCard Component</name>
  <files>src/components/strategic-research/output-section-card.tsx</files>
  <action>
Create a read-only version of the section card for the complete page display.

**Key differences from DocumentSection:**
- No edit button, no "Mark as Reviewed" button
- No isEditing, isReviewed, hasEdits props
- Always expanded (no collapse/expand toggle)
- Simpler header with just section number, icon, and label
- Uses SectionContentRenderer for content display (existing)
- Includes SourcesList for citations (existing)

**Component structure:**
```tsx
interface OutputSectionCardProps {
  sectionKey: StrategicBlueprintSection;
  sectionData: unknown;
  citations?: Citation[];
}
```

**Styling (SaaSLaunch design language):**
- Card background: `var(--bg-card)` which is `rgb(12, 14, 19)`
- Border: `var(--border-default)` which is `rgb(31, 31, 31)`
- Section number: Use `var(--accent-blue)` (`rgb(54, 94, 255)`) background with white text
- Heading font: `var(--font-heading)` (Instrument Sans)
- Section icon: Same icons as review page (TrendingUp, Users, Package, Swords, Lightbulb)

**Implementation:**
1. Import existing icons from document-section.tsx pattern
2. Import SectionContentRenderer and SourcesList from existing files
3. Create clean card layout with no interactive elements
4. Pass isEditing=false and empty onFieldChange to SectionContentRenderer
  </action>
  <verify>
- File exists at src/components/strategic-research/output-section-card.tsx
- Component exports OutputSectionCard
- TypeScript compiles: `npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "output-section-card" || echo "No TS errors in file"`
  </verify>
  <done>OutputSectionCard component created with read-only display of section content using existing renderers</done>
</task>

<task type="auto">
  <name>Task 2: Create PolishedBlueprintView Component</name>
  <files>src/components/strategic-blueprint/polished-blueprint-view.tsx</files>
  <action>
Create the main container component that displays all 5 sections as cards.

**Component structure:**
```tsx
interface PolishedBlueprintViewProps {
  strategicBlueprint: StrategicBlueprintOutput;
}
```

**Layout:**
- Container with max-width matching review page (`max-w-4xl` or similar)
- Vertical stack of 5 OutputSectionCard components
- Consistent spacing between cards (`space-y-6`)
- Framer Motion stagger animation on mount (fadeUp with delay)

**Implementation:**
1. Import OutputSectionCard from strategic-research
2. Import STRATEGIC_BLUEPRINT_SECTION_ORDER from output-types
3. Map over section order to render cards
4. Pass section data and citations from strategicBlueprint
5. Add entrance animation with stagger using framer-motion

**Animations (matching v2.0 patterns):**
- Use fadeUp variant from @/lib/motion
- Stagger children with 0.05s delay between cards
- Duration ~0.4s with easings.out

**Do NOT include:**
- Header with metadata (that stays in the parent page.tsx)
- Action buttons (those stay in parent)
- PDF export (that's in parent header)
  </action>
  <verify>
- File exists at src/components/strategic-blueprint/polished-blueprint-view.tsx
- Component exports PolishedBlueprintView
- Component renders 5 OutputSectionCard components
- TypeScript compiles: `npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "polished-blueprint" || echo "No TS errors in file"`
  </verify>
  <done>PolishedBlueprintView component created, rendering 5 section cards with stagger animation</done>
</task>

<task type="auto">
  <name>Task 3: Export Components from Index</name>
  <files>src/components/strategic-research/index.ts, src/components/strategic-blueprint/index.ts</files>
  <action>
Export the new components from their respective index files.

**For strategic-research/index.ts:**
- Add: `export { OutputSectionCard } from "./output-section-card";`

**For strategic-blueprint/index.ts:**
- Check if file exists, create if needed
- Add: `export { PolishedBlueprintView } from "./polished-blueprint-view";`
- Keep existing exports if any

This ensures clean imports in the next plan when integrating into page.tsx.
  </action>
  <verify>
- `grep -l "OutputSectionCard" src/components/strategic-research/index.ts`
- `grep -l "PolishedBlueprintView" src/components/strategic-blueprint/index.ts`
  </verify>
  <done>Both components exported from their index files for clean imports</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npm run build` or `npx tsc --noEmit`
2. Both new files exist and export correctly
3. OutputSectionCard uses SectionContentRenderer (code inspection)
4. PolishedBlueprintView renders 5 cards (code inspection)
</verification>

<success_criteria>
- OutputSectionCard displays section content without edit/review buttons
- PolishedBlueprintView renders all 5 strategic blueprint sections
- Styling follows SaaSLaunch design language (dark cards, blue accents)
- Components are properly exported for integration in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/33-output-page-polish/33-01-SUMMARY.md`
</output>
