---
phase: 29-onboarding-refresh
plan: 05
type: execute
---

<objective>
Refresh final steps (Assets/Proof, Budget/Targets) and add entrance animations to /generate page.

Purpose: Complete the onboarding refresh with remaining steps and page-level polish.
Output: Updated step-assets-proof, step-budget-targets, and animated /generate page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/docs/AI-GOS-DESIGN-SYSTEM.md
@.planning/phases/29-onboarding-refresh/29-02-SUMMARY.md
@.planning/phases/29-onboarding-refresh/29-04-SUMMARY.md

**Established patterns from previous plans:**
- FloatingLabelInput/FloatingLabelTextarea for form fields
- MagneticButton Continue/Back pattern
- staggerContainer/staggerItem animations
- fadeUp for section animations
- Design token colors throughout

**Page-level animations from Design System:**
- Page entrance: fadeUp with 0.6s duration
- Stagger delay: 0.1s between elements
- Hero section: can use fadeDown for title, fadeUp for content

**Files to update:**
@src/components/onboarding/step-assets-proof.tsx
@src/components/onboarding/step-budget-targets.tsx
@src/app/generate/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh step-assets-proof</name>
  <files>src/components/onboarding/step-assets-proof.tsx</files>
  <action>
Update step-assets-proof.tsx:

1. Add imports:
- FloatingLabelInput from @/components/ui/floating-label-input
- FloatingLabelTextarea from @/components/ui/floating-label-textarea
- MagneticButton from @/components/ui/magnetic-button
- motion from framer-motion
- fadeUp, staggerContainer, staggerItem from @/lib/motion

2. Update heading:
```tsx
<motion.div className="space-y-2" variants={fadeUp} initial="initial" animate="animate">
  <h2 className="text-[24px] font-bold tracking-tight" style={{ color: 'var(--text-primary)' }}>
    Assets & Proof
  </h2>
  <p style={{ color: 'var(--text-secondary)', fontSize: '15px' }}>
    Share your existing assets and social proof
  </p>
</motion.div>
```

3. Replace Input → FloatingLabelInput
4. Replace Textarea → FloatingLabelTextarea

5. If there are URL input fields (for asset links):
- Use FloatingLabelInput with type="url"
- Style validation feedback with var(--error) or var(--success)

6. If there are file upload areas:
- Container: rounded-xl, border-2 border-dashed var(--border-default)
- Hover: border-color var(--accent-blue), background var(--accent-blue-subtle)
- Icon: var(--text-tertiary), hover var(--accent-blue)
- Text: text-[14px] var(--text-secondary)

7. Wrap in staggerContainer, apply MagneticButton pattern
  </action>
  <verify>npm run build passes, assets form styled with design tokens</verify>
  <done>step-assets-proof refreshed with v2.0 styling</done>
</task>

<task type="auto">
  <name>Task 2: Refresh step-budget-targets</name>
  <files>src/components/onboarding/step-budget-targets.tsx</files>
  <action>
Update step-budget-targets.tsx:

1. Add same imports as Task 1

2. Update heading:
```tsx
<motion.div className="space-y-2" variants={fadeUp} initial="initial" animate="animate">
  <h2 className="text-[24px] font-bold tracking-tight" style={{ color: 'var(--text-primary)' }}>
    Budget & Targets
  </h2>
  <p style={{ color: 'var(--text-secondary)', fontSize: '15px' }}>
    Set your marketing budget and growth goals
  </p>
</motion.div>
```

3. Replace Input → FloatingLabelInput
4. Replace Textarea → FloatingLabelTextarea

5. For currency/number inputs:
- Use FloatingLabelInput with type="number" or type="text" with formatting
- Prefix currency symbol in label if needed

6. If there are slider components:
- Track: h-2, rounded-full, background var(--bg-hover)
- Fill: background var(--gradient-accent)
- Thumb: w-5 h-5, rounded-full, background var(--accent-blue), shadow

7. If there are goal cards or target displays:
- Container: rounded-xl, p-6, border var(--border-default)
- Background: var(--bg-elevated)
- Value text: text-[24px] font-bold var(--text-primary), font-mono (Geist Mono)
- Label: text-[13px] var(--text-tertiary)

8. Apply staggerContainer and MagneticButton pattern
  </action>
  <verify>npm run build passes, budget form styled consistently</verify>
  <done>step-budget-targets refreshed with FloatingLabel and design tokens</done>
</task>

<task type="auto">
  <name>Task 3: Add entrance animations to /generate page</name>
  <files>src/app/generate/page.tsx</files>
  <action>
Add page-level animations to the generate page:

1. Add imports:
```tsx
import { motion } from "framer-motion";
import { fadeUp, fadeDown, staggerContainer, staggerItem, easings } from "@/lib/motion";
```

2. Animate the onboarding state header:
```tsx
<motion.div
  className="mx-auto max-w-4xl mb-8 text-center"
  initial={{ opacity: 0, y: -20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, ease: easings.out }}
>
  <motion.h1
    className="text-3xl font-bold tracking-tight md:text-4xl"
    style={{ color: 'var(--text-primary)' }}
  >
    Generate Your Strategic Blueprint
  </motion.h1>
  <motion.p
    className="mt-2"
    style={{ color: 'var(--text-secondary)', fontSize: '15px' }}
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ delay: 0.2 }}
  >
    Complete the onboarding form to generate your comprehensive Strategic Blueprint
  </motion.p>
</motion.div>
```

3. Animate the stage indicator:
```tsx
<motion.div
  className="flex items-center justify-center gap-4 mt-6"
  initial={{ opacity: 0, y: 10 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ delay: 0.3, duration: 0.5 }}
>
  {/* Stage circles */}
</motion.div>
```

4. Style stage indicator circles with design tokens:
- Active: background var(--gradient-primary), text white
- Inactive: background var(--bg-hover), text var(--text-tertiary), border var(--border-default)
- Arrow: var(--text-tertiary)

5. Animate the auto-fill button:
```tsx
<motion.div
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  transition={{ delay: 0.4 }}
>
  <MagneticButton
    variant="outline"
    size="sm"
    className="mt-4"
    onClick={handleAutoFill}
    style={{
      border: '1px solid var(--border-default)',
      color: 'var(--text-secondary)',
    }}
  >
    <Wand2 className="mr-2 h-4 w-4" />
    Auto-fill with Sample Data
  </MagneticButton>
</motion.div>
```

6. Update page background to use design system:
- Background: var(--bg-base) (#000000)
- Remove gradient-to-b from-background to-muted/20 (too light for v2.0)
  </action>
  <verify>npm run build passes, page loads with smooth entrance animations</verify>
  <done>/generate page has entrance animations and v2.0 color scheme</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete onboarding refresh with v2.0 design system across all 9 step components, wizard, and generate page</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/generate
    3. Check page entrance animation (fade in from top)
    4. Verify header text uses proper typography (Geist Sans)
    5. Step through each form step (1-9):
       - Floating labels animate on focus
       - Gradient focus line appears below inputs
       - Buttons have magnetic hover effect
       - Progress bar has gradient fill
       - Step indicators show correct states
    6. Check dark theme colors:
       - Background is pure black (#000000)
       - Text is white/gray hierarchy
       - Accent blue (#3b82f6) on focus states
    7. Test responsive behavior on mobile (< 768px)
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] All 9 step components use FloatingLabel inputs
- [ ] All steps have MagneticButton navigation
- [ ] /generate page has entrance animations
- [ ] All text uses design system colors
- [ ] Human verification checkpoint passed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Human approved the visual refresh
- Phase 29 complete
</success_criteria>

<output>
After completion, create `.planning/phases/29-onboarding-refresh/29-05-SUMMARY.md`
</output>
