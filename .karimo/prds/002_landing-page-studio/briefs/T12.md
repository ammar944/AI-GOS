---
task_id: T12
title: SKILL.md for Claude Code skill loading
priority: must
complexity: 3
wave: 6
depends_on: [T10]
files:
  - skills/landing-page-studio/SKILL.md
---

# Task Brief: T12 — SKILL.md

## Goal

Verifiable goal: "Create `skills/landing-page-studio/SKILL.md` that follows the canonical research-competitor SKILL.md pattern — YAML frontmatter with name + description (~150 chars, trigger-rich), then full architecture documentation — verified by diffing frontmatter field names against `skills/research-competitor/SKILL.md` and confirming all required sections are present."

---

## Context

### What SKILL.md Is (vs Slash Command)

| Mechanism | File | Who Uses It | How Loaded |
|-----------|------|-------------|------------|
| Slash command | `.claude/commands/<name>.md` | Claude Code (interactive) | User types `/<name>` in Claude Code session |
| SKILL.md | `skills/<name>/SKILL.md` | Any Claude agent | Agent reads file to understand skill capabilities |

SKILL.md is a **generic capability descriptor**. Any Claude agent — not just Claude Code — can read SKILL.md to understand what the skill does, what inputs it needs, what outputs it produces, and how to invoke it programmatically. It is the "API contract" for the skill from an agent's perspective.

The slash command (T11) is for interactive Claude Code users. SKILL.md is for automated agents and orchestrators. Both describe the same underlying skill — from different angles.

### Canonical Reference: research-competitor SKILL.md

From OMC observations, `skills/research-competitor/SKILL.md` has:
- YAML frontmatter with `name` and `description` fields
- 5-phase deterministic pipeline documentation
- Full architecture: fan-out subagents + deterministic Node tail
- Sanity gate rules
- Input/output contracts
- Invocation instructions

> Verified 2026-05-01 (post-write). `skills/research-competitor/SKILL.md` frontmatter uses exactly two fields: `name` and `description`. Reference structure:
>
> ```yaml
> ---
> name: research-competitor
> description: Use when running AIGOS competitor research, competitor intelligence, competitor landscape analysis, ad library research, pricing/review mining, or share-of-voice research for a company, product, URL, or market. The agent collects sourced facts and then runs deterministic TypeScript validation/report scripts from this skill folder.
> ---
> ```
>
> Description is ~310 characters here (longer than the 150 target — the rule of thumb is trigger-rich, not strictly capped). For T12, write:
>
> ```yaml
> ---
> name: landing-page-studio
> description: Use when generating landing page HTML for SaaS products from a BrandSpec, picking design directions (editorial / bento / hero-art / brutalist / etc.), or tweaking a generated landing page section. Produces single-file HTML + Tailwind cdnjs + lucide inline. Output goes to skill output/ directory.
> ---
> ```

---

## Implementation Notes

### File Location

`skills/landing-page-studio/SKILL.md`

This is the ONLY file this task creates. T10 must be complete first (SKILL.md documents the CLI interface T10 provides).

### Required Frontmatter Structure

Mirror `skills/research-competitor/SKILL.md` exactly. Based on observations:

```yaml
---
name: landing-page-studio
description: >
  Generate production-ready SaaS landing pages from a BrandSpec JSON. Produces
  3 HTML variants per direction (layout, mood, copy angle). Supports full pipeline,
  plan-only, render-only, and in-place tweak modes. Trigger: landing page, HTML,
  brand spec, SaaS page, direction, tweak.
version: 1.0.0
---
```

The description must be ~150 characters and include trigger keywords agents would search for.

### Required Sections

Based on the research-competitor SKILL.md pattern:

```markdown
# Landing Page Studio

{one-paragraph summary of what the skill does}

## Inputs

{BrandSpec JSON schema reference — point to T4's schema file}
{Required fields: companyName, tagline, colors, fonts, targetAudience, tone}

## Outputs

{3 HTML files per direction, plan JSON, direction manifest}
{File naming convention: out/{direction-slug}-{section}.html}

## Pipeline Architecture

{Describe the 5-step pipeline: validate → pick direction → plan sections → render HTML → optional tweak}
{Note which steps call LLMs and which are deterministic}

## Subcommands (CLI Interface)

{Mirror the CLI help from T10 — list all 5 subcommands with their flags}

## Invocation

{How an agent invokes this skill:}
{  via CLI: npx tsx skills/landing-page-studio/scripts/generate.ts full --brand X --out Y}
{  via slash command: /landing-page-studio path/to/brand-spec.json (Claude Code only)}

## Environment Variables

| Variable | Required | Purpose |
|----------|----------|---------|
| ANTHROPIC_API_KEY | Yes (for LLM steps) | Direction picker + section planner |
| OLLAMA_API_KEY | Yes (for LLM steps) | Alternative model path |

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | Success |
| 1 | User error (bad args, missing file) |
| 2 | Pipeline step failure |
| 3 | Missing environment variable |

## Limitations

{What this skill does NOT do — per scope-decisions.md:}
{- Does not deploy to any hosting provider}
{- Does not generate images or assets}
{- Does not support non-SaaS page types in v1}

## Quality Bar

{Sanity gate rules — what makes output acceptable:}
{- HTML must be valid (no unclosed tags)}
{- All 3 direction variants must be produced for full subcommand}
{- Brand colors must appear in rendered output}
```

### Description Length Check

After writing, ensure the description is trigger-rich (includes keywords agents would search for: "landing page", "HTML", "SaaS", "BrandSpec", "design direction") and concise enough to scan in the Claude Code command picker. Length is not strictly capped — `skills/research-competitor/SKILL.md` description is ~310 characters and that's fine. Aim for clarity over arbitrary length.

---

## Acceptance Criteria

- [ ] File exists at `skills/landing-page-studio/SKILL.md`
- [ ] YAML frontmatter present with `name` field = `landing-page-studio`
- [ ] YAML frontmatter `description` field is trigger-rich and includes keywords: "landing page", "HTML", "SaaS", "BrandSpec", "design direction" — length is not capped (research-competitor's reference description is ~310 chars and is the canonical pattern)
- [ ] Frontmatter field names match `skills/research-competitor/SKILL.md` exactly
- [ ] Documents all 5 CLI subcommands (plan, generate, full, tweak-text, tweak-regen)
- [ ] Documents required environment variables
- [ ] Documents all 4 exit codes (0/1/2/3)
- [ ] Includes a Limitations section noting what v1 does NOT do
- [ ] No other files modified

---

## Gotchas

1. **Frontmatter field names must match research-competitor exactly** — Claude Code and agents use the same parser for all SKILL.md files. A field name mismatch (e.g. `skill_name` vs `name`) will cause the skill to be unrecognized.
2. **Description is trigger-rich, not marketing copy** — write for searchability, not aesthetics. Include the actual words agents/users type when looking for this capability.
3. **Document the CLI interface from T10** — SKILL.md must accurately reflect T10's subcommands and flags. Write T10 first, then document it here. If T10 changes, update SKILL.md.
4. **Version field** — set to `1.0.0`. The research-competitor pattern uses semantic versioning.

---

## Verification Steps

```bash
# 1. File exists
ls -la /Users/ammar/Dev-Projects/AI-GOS-main/skills/landing-page-studio/SKILL.md

# 2. Has frontmatter
head -10 /Users/ammar/Dev-Projects/AI-GOS-main/skills/landing-page-studio/SKILL.md

# 3. Description length
python3 -c "
import re, sys
content = open('skills/landing-page-studio/SKILL.md').read()
desc = re.search(r'description: >?\n\s+(.+?)(\n\w|\Z)', content, re.DOTALL)
if desc:
    text = ' '.join(desc.group(1).split())
    print(f'Description length: {len(text)} chars')
    print(f'Text: {text}')
"

# 4. Compare frontmatter field names against reference
grep -A 10 '^---' /Users/ammar/Dev-Projects/AI-GOS-main/skills/research-competitor/SKILL.md | head -15
grep -A 10 '^---' /Users/ammar/Dev-Projects/AI-GOS-main/skills/landing-page-studio/SKILL.md | head -15

# 5. All subcommands documented
for cmd in plan generate full tweak-text tweak-regen; do
  grep -q "$cmd" /Users/ammar/Dev-Projects/AI-GOS-main/skills/landing-page-studio/SKILL.md \
    && echo "$cmd: present" || echo "$cmd: MISSING"
done

# 6. Exit codes documented
for code in 0 1 2 3; do
  grep -q "| $code " /Users/ammar/Dev-Projects/AI-GOS-main/skills/landing-page-studio/SKILL.md \
    && echo "Exit $code: present" || echo "Exit $code: MISSING"
done
```

---

## References

- Canonical SKILL.md reference: `skills/research-competitor/SKILL.md`
- CLI interface being documented: `skills/landing-page-studio/scripts/generate.ts` (T10)
- Slash command (parallel, different mechanism): `.claude/commands/landing-page-studio.md` (T11)

---

*Generated by KARIMO Brief Writer | PRD: landing-page-studio | Task: T12 | Wave: 6*
