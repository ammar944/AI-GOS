---
task_id: T3
title: Build 15-direction vocabulary taxonomy
priority: must
complexity: 3
wave: 2
depends_on: [T1]
files:
  - skills/landing-page-studio/references/directions.json
---

# Task Brief: T3

**Title:** Build 15-direction vocabulary taxonomy
**Priority:** must
**Complexity:** 3/10
**Wave:** 2 (depends on T1 — scaffold must exist first)

---

## Goal

Create `skills/landing-page-studio/references/directions.json` — a JSON data file (NOT a TypeScript module) containing exactly 15 design directions across 5 disjoint families (Editorial, Bento, Hero-art, Brutalist, Eastern-typographic), each with paired vocabulary names per `scope-decisions.md` item 2. Done when: the file exists at the path above, validates as JSON via `jq`, contains exactly 15 entries with the required field shape, and the 5 anchor names from scope-decisions.md are present verbatim.

---

## Context

The `direction` field on `BrandSpec` (T2) is a string key that selects one of 15 design directions. These directions are the creative vocabulary the landing-page-studio uses to style generated pages. Each direction maps to a distinct visual aesthetic with associated prompt language, color tendencies, typography mood, and layout behavior.

The 15 directions must cover the full range of landing page aesthetics seen across SaaS, DTC, creative agencies, and enterprise products. They are used by:

1. **Section generators** (T4/T5/T6) — select prompt vocabulary based on direction
2. **Orchestrator** (T4) — validates that `BrandSpec.direction` is a known key
3. **BrandSpec validation** — optional runtime check against this list

The taxonomy lives in `references/` per the PRD canonical layout (static reference data, not executable logic). Output is **JSON, not a TypeScript module** — type definitions for `Direction` belong in T2's `contracts/` folder if needed downstream.

---

## The 15 Directions — Family Structure (LOCKED)

Per `scope-decisions.md` item 2, the taxonomy uses **vocabulary-teaching paired names** (`<aesthetic-anchor> / <exemplar-brand>` format) and is structured as **5 disjoint families × 3 variants per family = 15 directions**. Disjointness is enforced so T4's planner picks 3 from different families per BrandSpec.

The 5 anchor families (locked — must use these names):

1. **Editorial / Linear-Stripe** — magazine-grade typography, dense paragraphs, refined whitespace
2. **Bento / Notion-Vercel** — modular card grid, asymmetric tiles, soft borders
3. **Hero-art / Lovable-Anthropic** — large hero illustration, generous vertical rhythm, soft gradients
4. **Brutalist / Are.na** — raw geometry, sans-serif weights, exposed structure, no ornament
5. **Eastern-typographic / Kenya-Hara** — vertical/horizontal type rhythm, generous negative space, restrained palette

Per family, define **3 variants** that differ on layout density, color temperature, or typographic register but stay within the family aesthetic. Variant naming follows the same `<sub-anchor> / <exemplar>` pattern. Examples (illustrative — implementer refines):

| Family | Variant key | Variant name |
|--------|-------------|--------------|
| Editorial | `editorial-linear-stripe` | Editorial / Linear-Stripe (anchor) |
| Editorial | `editorial-stripe-press` | Editorial / Stripe-Press |
| Editorial | `editorial-apple-newsroom` | Editorial / Apple-Newsroom |
| Bento | `bento-notion-vercel` | Bento / Notion-Vercel (anchor) |
| Bento | `bento-vercel-edge` | Bento / Vercel-Edge |
| Bento | `bento-linear-cards` | Bento / Linear-Cards |
| Hero-art | `hero-art-lovable-anthropic` | Hero-art / Lovable-Anthropic (anchor) |
| Hero-art | `hero-art-anthropic-claude` | Hero-art / Anthropic-Claude |
| Hero-art | `hero-art-vercel-ai` | Hero-art / Vercel-AI |
| Brutalist | `brutalist-arena` | Brutalist / Are.na (anchor) |
| Brutalist | `brutalist-bold-sans` | Brutalist / Bold-Sans |
| Brutalist | `brutalist-mono-grid` | Brutalist / Mono-Grid |
| Eastern-typographic | `eastern-kenya-hara` | Eastern-typographic / Kenya-Hara (anchor) |
| Eastern-typographic | `eastern-muji-editorial` | Eastern-typographic / Muji-Editorial |
| Eastern-typographic | `eastern-tokyo-type` | Eastern-typographic / Tokyo-Type |

The 5 anchor variants (rows marked `(anchor)`) MUST use exactly the names above. The 10 sibling variants are illustrative — the implementer can substitute equivalent paired names that stay within each family aesthetic. Generic names like `glass-morphism`, `dark-luxury`, `clean-minimal`, `gradient-mesh` are NOT acceptable — every entry must be a paired vocabulary name per the levels-framework meta-insight.

---

## Acceptance Criteria

(verbatim from `tasks.yaml` T3)

- [ ] File `skills/landing-page-studio/references/directions.json` exists
- [ ] Contains exactly 15 directions across 5 disjoint families: Editorial, Bento, Hero-art, Brutalist, Eastern-typographic
- [ ] Each direction has fields: `id`, `name`, `family`, `summary`, `do[]`, `dont[]`, `exemplar_brands[]`
- [ ] Disjointness enforced — T4's planner picker MUST select 3 from disjoint families
- [ ] JSON validates against an inline schema documented at the top of the file (use a `$schema` comment line or a separate `directions.schema.json`)
- [ ] At least 3 directions per family (gives T4's planner room to pick freshly per BrandSpec)
- [ ] All 15 entries use paired vocabulary names per the format above (`<aesthetic-anchor> / <exemplar-brand>`)
- [ ] The 5 anchor names from scope-decisions.md item 2 are present verbatim
- [ ] No imports from outside `skills/landing-page-studio/` (it's a JSON data file — no imports at all)
- [ ] `cat references/directions.json | jq 'length'` returns 15

(Optional — for type-safety in downstream T4/T5 callers — the implementer MAY add a companion `contracts/directions.ts` file exporting a Zod schema and `DirectionKey` union type. This is NOT required for T3 to be considered done.)

---

## Implementation Notes

### JSON Shape

```json
{
  "$comment": "15-direction vocabulary taxonomy for landing-page-studio. 5 disjoint families × 3 variants. Names follow the <aesthetic-anchor> / <exemplar-brand> pattern per scope-decisions.md item 2.",
  "directions": [
    {
      "id": "brutalist-arena",
      "name": "Brutalist / Are.na",
      "family": "Brutalist",
      "summary": "Raw geometry, sans-serif weights, exposed structure. No ornament — function as form.",
      "do": [
        "Use thick borders (3-6px solid)",
        "Expose grid lines and gutters",
        "Stick to 2-3 color total",
        "Use monospace or geometric sans for accent type"
      ],
      "dont": [
        "Use rounded corners (radius > 0)",
        "Soften with gradients or shadows",
        "Add ornament (icons-as-decoration, frames, flourishes)"
      ],
      "exemplar_brands": ["Are.na", "Pentagram", "Werkplaats Typografie"]
    }
    // ... 14 more entries — 2 more Brutalist variants + 3 each of Editorial, Bento, Hero-art, Eastern-typographic
  ]
}
```

### Format Rules

- File extension: `.json` (NOT `.ts`). T3 produces a static data file.
- Top-level: `{ "directions": [...] }` array of 15 objects. The wrapping object (vs a bare array) leaves room for future metadata fields without breaking consumers.
- Per-entry fields are exactly: `id`, `name`, `family`, `summary`, `do[]`, `dont[]`, `exemplar_brands[]`. No extra fields. No optional fields — all required.
- `id` is kebab-case, derived from name (e.g. "Brutalist / Are.na" → `brutalist-arena`).
- `family` is one of: `Editorial`, `Bento`, `Hero-art`, `Brutalist`, `Eastern-typographic` (exact spelling, hyphenated where shown).
- `do[]` and `dont[]` should each have 3-5 entries — enough to materially constrain a generator prompt (T5 reads these).
- `exemplar_brands[]` should have 2-4 real brand/site names — used by T4's planner to write rationales.

/** Union of all valid direction keys */
export type DirectionKey = typeof DIRECTION_TAXONOMY[number]["key"];

// Convenience set for O(1) lookup
const DIRECTION_KEY_SET = new Set(DIRECTION_TAXONOMY.map((d) => d.key));

export function isValidDirection(key: string): key is DirectionKey {
  return DIRECTION_KEY_SET.has(key);
}
```

### All 15 Entries

Implement each direction from the table in the Goal section. For each entry, write:

- `description`: 1-2 sentences capturing the visual feeling and use case
- `promptVocabulary`: 5-8 words/phrases an LLM can inject into section-generation prompts
- `cssHints`: 3-5 strings describing CSS tendency (not actual CSS declarations — descriptive)

**Example for `glass-morphism`:**
```typescript
{
  key: "glass-morphism",
  label: "Glass Morphism",
  description:
    "Frosted-glass surfaces with layered transparency and soft blur. " +
    "Popular in modern SaaS products and Apple-influenced design.",
  promptVocabulary: ["frosted", "translucent", "layered", "light", "blur", "ethereal", "depth"],
  cssHints: [
    "backdrop-filter: blur(12px)",
    "background: rgba(255,255,255,0.1-0.3)",
    "border: 1px solid rgba(255,255,255,0.2)",
    "box-shadow: soft and diffuse",
  ],
},
```

### No Imports Required

This file is pure TypeScript data — no `import` statements needed. The `as const` assertion on the array enables the `DirectionKey` union type inference.

---

## Gotchas

**Exactly 15 directions — no more, no fewer.**
The orchestrator in T4 will validate `BrandSpec.direction` against this list. If the count is wrong, downstream tasks will break. Count explicitly before committing.

**`as const` is required on the array.**
Without `as const`, TypeScript cannot infer the union type `typeof DIRECTION_TAXONOMY[number]["key"]`. The `DirectionKey` type derivation depends on it.

**Keys must be kebab-case, lowercase.**
The keys are user-facing in `BrandSpec.direction`. Use `kebab-case` only (e.g. `"clean-minimal"`, not `"cleanMinimal"` or `"clean_minimal"`).

**`cssHints` are descriptive, not executable.**
They describe CSS tendencies for a human implementer or LLM to interpret — not actual CSS to inject verbatim. Keep them as strings like `"border-radius: 0"` or `"heavily weighted typography"`, not CSS classes or selectors.

**Cross-check PRD before finalizing.**
If `.karimo/prds/002_landing-page-studio/scope-decisions.md` lists specific direction names, those take precedence over this brief's table. Read scope-decisions.md item 2 first.

---

## Verification Steps

```bash
# 1. File exists at the correct path
test -f skills/landing-page-studio/references/directions.json && echo "PASS file exists" || echo "FAIL"

# 2. Valid JSON (parses cleanly)
jq empty skills/landing-page-studio/references/directions.json && echo "PASS valid JSON" || echo "FAIL invalid JSON"

# 3. Exactly 15 entries
test "$(jq '.directions | length' skills/landing-page-studio/references/directions.json)" = "15" \
  && echo "PASS 15 entries" || echo "FAIL wrong count"

# 4. All 5 anchor families present (Editorial, Bento, Hero-art, Brutalist, Eastern-typographic)
jq -r '.directions[].family' skills/landing-page-studio/references/directions.json | sort -u | wc -l
# Should print 5
jq -r '.directions[].family' skills/landing-page-studio/references/directions.json | sort -u
# Should list exactly: Bento, Brutalist, Eastern-typographic, Editorial, Hero-art

# 5. At least 3 entries per family
jq -r '.directions | group_by(.family) | map({family: .[0].family, count: length}) | .[]' \
  skills/landing-page-studio/references/directions.json
# Each count should be >= 3

# 6. All 5 anchor names present verbatim per scope-decisions.md item 2
for anchor in "Editorial / Linear-Stripe" "Bento / Notion-Vercel" "Hero-art / Lovable-Anthropic" "Brutalist / Are.na" "Eastern-typographic / Kenya-Hara"; do
  match=$(jq --arg a "$anchor" '.directions[] | select(.name == $a)' skills/landing-page-studio/references/directions.json)
  if [ -n "$match" ]; then echo "PASS anchor: $anchor"; else echo "FAIL missing anchor: $anchor"; fi
done

# 7. All ids are unique
total=$(jq '.directions | length' skills/landing-page-studio/references/directions.json)
unique=$(jq -r '.directions[].id' skills/landing-page-studio/references/directions.json | sort -u | wc -l)
test "$total" = "$unique" && echo "PASS unique ids" || echo "FAIL duplicate ids"

# 8. Required fields present on every entry
jq -e '[.directions[] | select(.id and .name and .family and .summary and .do and .dont and .exemplar_brands)] | length == 15' \
  skills/landing-page-studio/references/directions.json \
  && echo "PASS all entries have required fields" || echo "FAIL missing fields on some entry"

# 9. No generic aesthetic names — every name must follow "<aesthetic-anchor> / <exemplar>" pattern (contains " / ")
jq -r '.directions[] | select(.name | contains(" / ") | not) | .name' \
  skills/landing-page-studio/references/directions.json
# Should print nothing (empty output = pass)
```

---

## References

- `skills/landing-page-studio/` — created by T1 (must exist before starting)
- `.karimo/prds/002_landing-page-studio/scope-decisions.md` — item 2: direction families (read first — overrides this brief's table if different)
- `.karimo/prds/002_landing-page-studio/research/summary.md` — design vocabulary research
- `.karimo/prds/002_landing-page-studio/research/external/levels-framework.md` — external design direction framework (if present)
- `.karimo/prds/002_landing-page-studio/PRD_landing-page-studio.md` — direction field definition in BrandSpec section

---

*Generated by KARIMO Brief Writer | PRD: 002_landing-page-studio | Task: T3 | Wave: 2*
