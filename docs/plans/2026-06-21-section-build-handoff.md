# Section-Build Handoff — the 7-Section Honest-Ceiling Climb to 9/10

> Generated 2026-06-21 by the `section-build-megaspec` workflow (`wf_15d0cb81-edd`, 91 agents: per-item recon → 3-angle spec → 4-lens adversarial vet → synthesis, + cross-section integration). Each spec is read-only-derived and adversarially vetted; apply with judgment, not blindly.

## Execution contract (read first)
- **Terminator + bar:** `scripts/zz-value-read.mjs` — after each section, run a paid harness section run (`--live`, budget-gated) then `node scripts/zz-value-read.mjs <run> --gate`. The deterministic ceiling + calibrated value-read is the bar. A section is "done" when its finalScore ≥ its honest target with NO fabrication-cap.
- **Brake:** the harness defaults to corpus-only; paid runs are `--live` and budget-gated (`_budget.json`, `_ABORT` file). One paid run at a time, no loops.
- **Honesty:** honest gaps are NEUTRAL; never invent a number/quote/person not traceable to the frozen corpus or a fetched source. The deterministic floor is BLIND to prose fabrication — a human reviews any value-jump without new structured evidence.
- **Additive only:** schema changes are optional fields; never break a prior committed artifact, an existing test (esp. the `section-registry` allowedTools contract), or a renderer.
- **`src/` untouched by Phase 0** — this is net-new section work on top of commit `6d8ce161`.

## Readiness overview

| Item | Target | Edits | Tests | Fab-risks | Readiness |
|---|---|---|---|---|---|
| Phase-1 Keystone — confidence-split + adaptive envelope | 8 | 3 | 1 | 1 | 🟢 READY |
| §4.1 Competitor Landscape | 8 | 3 | 4 | 4 | 🟢 READY |
| §4.2 Voice of Customer | 8 | 4 | 6 | 4 | 🟢 READY |
| §4.3 Market & Category | 8 | 3 | 1 | 1 | 🟢 READY |
| §4.4 Buyer & ICP | 7.5 | 5 | 3 | 5 | 🟢 READY |
| §4.5 Offer Diagnostic | 8 | 4 | 4 | 4 | 🟢 READY |
| §4.6 Demand & Intent | 8 | 4 | 4 | 4 | 🟡 NEEDS-REVISION |
| §4.7 Paid Media Plan | 8 | 4 | 6 | 5 | 🟡 NEEDS-REVISION |

## Build order & cross-section integration

**Verified execution order** (keystone first so every section rebases onto the shared `run-section.ts`/`verdict-hero.tsx` edits; Competitor unblocks PaidMedia; PaidMedia last):

1. keystone (Phase-1 cross-cutting): run-section.ts deriveValueReadiness helper + valueReadiness write -> trust-tier.ts deriveValueReadinessBadge accessor -> verdict-hero.tsx optional prop -> wire all 7 renderers -> tests. Lands the most-shared run-section.ts + verdict-hero edits FIRST so every section rebases onto them.
2. §4.1 Competitor Landscape: claim-extractor.ts displayUrlFieldPaths Set + fieldPath-scoped guard (THE load-bearing unblocker) + claim-extractor.test.ts 3-assertion pin + competitor-landscape.ts comment-only + SKILL.md URL-discipline (rebase onto already-committed §4.7 reconciler). Unblocks Competitor's own commit AND PaidMedia's competitor rows. No upstream dependency.
3. §4.2 Voice of Customer: voice-of-customer-synthesis.ts domain-counter fix (getRegistrableDomain at 590/600 + painSourceCount 509-510) + run-section.ts directional-stamp demotion (1727-1752) + drop invented priorSolution (2059) + voice-of-customer.ts schema (additive confidenceTier/permalinkResolved) + voice-of-customer-floors.ts + SKILL.md + 2 tests. Unblocks PaidMedia's VoC re-attribution.
4. §4.3 Market & Category: market-category.ts floor reconcile (marketSignalCount at 652, ledger/verifier-gated $13B promotion) + SKILL.md + schema floor tests. Single-owner schema, no run-section.ts collision.
5. §4.4 Buyer & ICP: buyer-icp.ts schema (additive icpScorecard/painWeight) + run-section.ts injectPersonaGap relaxation (7440) + buyer-icp-acquisition-ledger.ts BUYER_ICP_PROMOTED_PERSONA_FLOOR (268) + grounded-buyer-unit.ts + SKILL.md + tests. Re-verify keystone valueReadiness after this lands (it changes coverage.readiness inputs).
6. §4.5 Offer Diagnostic: offer-diagnostic.ts schema (additive demandSignal/benchmark) + run-section.ts demandIntentEvidenceGapArtifact wire (4551/8889/11750) + SKILL.md honest-read reframe + tests. Depends on Demand intent-mix data being final.
7. §4.6 Demand & Intent: demand-intent.ts schema (additive economicsProvenance/operatorEconomics) + demand-intent.tsx renderer (rebase onto keystone VerdictHero wire) + demand-intent.test.tsx fixtures (same change, per decode-fallbacks enum caveat) + gap-note.tsx + SKILL.md. No run-section.ts touch.
8. §4.7 Paid Media Plan (LAST, consumes all upstream): paid-media-evidence-pack.ts demandIntentCandidates + looksLikeNamedRecords widen (410) + snapMoneyProvenanceForLabel snap-to-derived + run-section.ts VoC re-attribution post-processor + paid-media-plan.ts schema (additive evidenceBinding) + paid-media-plan.tsx renderer (rebase onto keystone VerdictHero wire) + SKILL.md + 2 evidence-pack tests. Can only score once Demand binder, VoC re-attribution, and Competitor (now committing) have all landed.

**Shared-file conflict surface** (apply-order / isolation):

| File | Touched by | Risk | Resolution |
|---|---|---|---|
| `src/lib/lab-engine/agents/run-section.ts` | keystone (deriveValueReadiness helper after hasHonestEmptyCore ~4684 + valueReadiness write at the committed body ~5433/computedTrust region ~5516), §4.2 Voice of Customer (directional-stamp boilerplate at 1727-1752; invented priorSolution scaffolding at 2059), §4.4 Buyer & ICP (injectPersonaGap at 7440 — relax the validatorGradePersonaCount<3 self-label gap injection), §4.5 Offer Diagnostic (demandIntentEvidenceGapArtifact cross-section wire at ~4551/8889/11750 — supply real Demand intent mix to channelTruth), §4.7 Paid Media Plan (deterministic VoC re-attribution post-processor: set sourceSection=positioningVoiceOfCustomer on token-matched competitorReviewInsights rows) | HIGHEST. run-section.ts is 14,572 lines and is the single most-shared file (5 of 8 items). The five edits target DIFFERENT, non-overlapping line regions (~1727-2059 VoC, ~4551-5516 | SEQUENCE, do not parallel-merge. Apply order: (1) keystone run-section.ts helper+write FIRST (Phase-1 zero-risk keystone; defines deriveValueReadiness that later sections inherit); (2) §4.1 claim-extr |
| `src/components/research-v2/primitives/verdict-hero.tsx + the 7 section-renderers (buyer-icp.tsx, competitor-landscape.tsx, demand-intent.tsx, market-category.tsx, offer-diagnostic.tsx, paid-media-plan.tsx, voice-of-customer.tsx)` | keystone (adds optional valueReadiness prop to VerdictHero + wires deriveValueReadinessBadge(artifact.verifierSummary) at ALL 7 VerdictHero call sites), §4.6 Demand & Intent (edits demand-intent.tsx renderer — keyword cluster chart + per-block readiness badges + gap-note degradation), §4.7 Paid Media Plan (edits paid-media-plan.tsx renderer — evidenceBinding provenance strip + amber competitor gap rows) | MEDIUM. The keystone touches the shared verdict-hero.tsx primitive (adds one optional prop — additive, no existing call site breaks because the prop is optional and absent renders  | Land the keystone verdict-hero.tsx prop + all 7 renderer wires FIRST (plan build order: Phase-1 keystone before any section rescope). The 5 renderers no later item touches (buyer-icp, competitor-lands |
| `src/lib/lab-engine/agents/verification/claim-extractor.ts (+ its __tests__/claim-extractor.test.ts)` | §4.1 Competitor Landscape (THE load-bearing keystone-unblocker: add module-level displayUrlFieldPaths Set after selfAuthoredLabelPaths line 23; add a fieldPath-scoped continue guard beside the constructedAdLibraryLinkPattern skip at 271; pin with a 3-assertion test) | LOW AS SCOPED, but a TRAP if a future item edits claim extraction. In the CURRENT 8-item plan, claim-extractor.ts is edited by §4.1 ONLY — NO live collision. The risk is latent: th | ISOLATE + SEQUENCE-FIRST among section items. §4.1's claim-extractor edit is the keystone unblocker (it un-fails Competitor AND unblocks PaidMedia's competitor rows) — land it FIRST, right after the c |
| `src/lib/lab-engine/artifacts/schemas/competitor-landscape.ts + src/lib/lab-engine/skills/positioning-competitor-landscape/SKILL.md` | §4.1 Competitor Landscape (this plan: competitor-landscape.ts gets a COMMENT-ONLY additive note that url=navigation / sourceUrl=citation, no behavior change; SKILL.md gets a new ### URL discipline subsection + version bump 3.1.0-lab->3.1.1-lab + updated date), §4.7-enrichment tier/coverage reconciler (ALREADY LANDED on this branch per commits d36ab8d8 'Competitor Landscape tier/coverage' and a525645f — the substantive owner of competitor-landscape.ts schema additions; may also bump the SKILL.md) | LOW-MEDIUM. competitor-landscape.ts: §4.1's change here is explicitly COMMENT-ONLY (no field/behavior change) — the substantive tier/coverage schema work is the already-committed § | §4.1 keeps its competitor-landscape.ts touch strictly comment-only (per the plan) so it does not compete with the §4.7 reconciler's field additions — place the comment on the url/sourceUrl field decla |
| `src/lib/lab-engine/agents/run-section.ts` | keystone valueReadiness write @~5351 (sectionEnriched) + @~5433 (committed body), VoC §4.2 permalink-rule demotion @~1726 + invented-priorSolution drop @~2052, BuyerICP §4.4 injectPersonaGap @~7442, Offer §4.5 channelTruth demand-signal wire (~4551/8889/11750 cross-section precedent region), PaidMedia §4.7 VoC re-attribution post-processor + snapMoneyProvenanceForLabel hard-code | HIGH coordination, LOW per-hunk textual collision. 5 items mutate one 14,572-line file at non-overlapping ranges. Two worktree branches both touching run-section.ts force a same-fi | Land keystone's run-section.ts hunk FIRST (it is far from every section hunk). Then sequence the section run-section.ts edits one-merge-at-a-time onto the updated mainline; each item re-greps its anch |
| `src/lib/lab-engine/agents/verification/claim-extractor.ts` | Competitor displayUrlFieldPaths Set @19-23 region + url-loop guard @268-283 | LOW. Only Competitor edits this file in this plan. The Set is additive, namespaced to one literal path. Risk is solely that a FUTURE claim-extractor change rebroadens the exemption | Land FIRST among section items (it is the keystone unblocker). Pin with the 3-assertion test in claim-extractor.test.ts (NOT evidence-support.test.ts — that path in §4.1 is stale): (a) https://brex.co |
| `src/lib/lab-engine/artifacts/artifact-envelope.ts` | keystone valueReadiness additive optional in verifierSummary/computedTrust | LOW. Additive optional under the existing .nullable().transform().optional() pattern; frozen artifacts re-parse unchanged. NOTE the spec's path `schemas/artifact-envelope.ts:439` i | Use the real path artifacts/artifact-envelope.ts. Verify frozen positioning*.json re-parse before/after. Owned by keystone only; no section item touches it. |
| `src/components/research-v2/section-renderers/{all 7}.tsx + verdict-hero.tsx + trust-tier.ts` | keystone: optional valueReadiness prop on VerdictHero; each of 7 renderers passes deriveValueReadinessBadge at its VerdictHero call; deriveValueReadinessBadge appended to trust-tier.ts | MEDIUM. Competitor §4.1, VoC §4.2, Market §4.3, BuyerICP §4.4, Offer §4.5, Demand §4.6, PaidMedia §4.7 EACH also edit their own renderer (visualization work). Keystone touches all  | Land keystone's 7-renderer VerdictHero wiring FIRST so the optional prop + badge exist; each section's later renderer/visualization edit then builds on top in its own renderer only. trust-tier.ts: app |
| `src/lib/lab-engine/skills/positioning-competitor-landscape/SKILL.md (+ each section's SKILL.md)` | Competitor URL-discipline subsection + metadata.version bump; §4.7 enrichment may also bump it | LOW. Each section owns its own SKILL.md; only cross-touch is a metadata.version bump if two items edit the same skill. | Coordinate the metadata.version bump per skill (one bump per landing). Competitor's SKILL.md edit is a defense-in-depth backstop, prose-anchored (insert before ## Iron Laws @45) to survive line drift  |
| `build-plan vs Competitor spec test-file divergence` | claim-extractor.test.ts vs evidence-support.test.ts | medium | pin in claim-extractor.test.ts; plan naming evidence-support.test.ts is stale, follow the spec |
| `trust-tier.ts buildVerificationFlag location mis-stated` | keystone badge | low | buildVerificationFlag is in verification-tier.ts not trust-tier.ts; badge appends after trustTierDotClass |
| `competitor-landscape.tsx VerdictHero no-op until competitor fix` | keystone renderer | low | wire 6 other renderers first; competitor one after the fix |
| `VoC and Competitor prose floor is blind` | VoC prose, Competitor prose | high | run output through value-read and eyeball prose; SKILL law badging derived sentences as inference |
| `VoC successLanguage and Competitor publicWeaknesses unproven` | successLanguage, Brex G2 quotes | medium | re-run and confirm; Brex G2 quotes are in the grill artifact not the deck |
| `verdict-hero.tsx prop under-specified` | optional prop, rich-count line | low-medium | add copy, placement, and a test case for the rich-count line |

**Completeness critic — gaps before one-shot autonomous execution:**

> All three specs are empirically grounded; root-causes confirmed; zero laundering; conflicts list the gaps.

---

## Per-item build specs

### Phase-1 Keystone — confidence-split + adaptive envelope — target 8 · 🟢 READY

**Root cause.** Demand: confidence 0.4 yet computedTrust claimSupportShare 1, honestEmptyCore true, lead keywordDemand readiness rich. run-section.ts:4857 + hasHonestEmptyCore:4683 cap headline 0.4 on one sibling gap. Keep confidence as internal groundingConfidence (test:183 pins 0.4); add sibling valueReadiness from per-block coverage.readiness. Additive in verifierSummary z.record (artifact-envelope.ts:439).

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/agents/run-section.ts`** — `(a) new helper after hasHonestEmptyCore ~4699; (b) computedTrust literal ~5512, anchor containmentKnownRate line not the trust-null guard above.`
  - *Change:* (a) deriveValueReadiness(body): recurse, collect coverage.readiness in rich/adequate/thin/gap; tally blocksByReadiness; leadReadiness=strongest (rich>adequate>thin>gap,null); anyRich=rich>0; reuse getRecord. (b) add valueReadiness=deriveValueReadiness(sectionEnriched) (final body ~5351, committed ~5433); do NOT modify confidence; do NOT use deriveWave2TrustConfidence ~4798 (coverage.readiness absent pre-reconcile).
  - *Why:* Per-block rollup orthogonal to 0.4 groundingConfidence; reads post-reconcile sectionEnriched; 0.4 untouched keeps test:183 green.
- **`src/components/research-v2/trust-tier.ts`** — `append after trustTierDotClass.`
  - *Change:* deriveValueReadinessBadge(verifierSummary): extract computedTrust.valueReadiness, return leadReadiness/anyRich/blocksByReadiness or null. Do not touch buildVerificationFlag (2 live callers: section-profile-persistence.ts:216, commit-patch.ts:174).
  - *Why:* Null-tolerant accessor; only deriveTrustTier was uncalled in tsx.
- **`verdict-hero.tsx + 7 renderers`** — `verdict-hero.tsx optional valueReadiness prop; each renderer passes deriveValueReadinessBadge(artifact.verifierSummary) at its VerdictHero call.`
  - *Change:* Optional valueReadiness(leadReadiness/anyRich/blocksByReadiness); when anyRich render a muted rich-count line, no chrome; absent=as today.
  - *Why:* No render site=no-op; surfaces the split in all 7. Additive.

**Tests.**
- Extend run-section-tier-honesty.test.ts: rich-lead honestEmptyCore keeps confidence 0.4 AND valueReadiness rich (keep line 183). Add value-readiness.test.ts + verdict-hero.test.tsx + frozen guard over the 7 harness-ramp positioning JSONs.

**Verify.**
```bash
npx tsc --noEmit && npm run test:run
```

**Fabrication risks to guard.**
- Floor is confidence-blind AND prose-blind; keystone does NOT rewrite verdict prose at run-section.ts 2152/2723/3006/3066/4072; sequence those right after.

**Shared files:** `run-section.ts (localized 4683/5512, land first); verdict-hero.tsx (all 7); trust-tier.ts (buildVerificationFlag off-limits).`

**Sequencing.** Keystone first: run-section.ts helper+write -> trust-tier badge -> VerdictHero prop + wire all 7 -> tests. Section prose fixes follow.

**Open questions.**
- DESIGN.md eyebrow vs collapsed diagnostic; copy gitignored tmp artifacts to a fixtures dir.


### §4.1 Competitor Landscape — target 8 (from 1) · 🟢 READY

**Root cause.** GATE-vs-FIELD-CONTRACT COLLISION, NOT scarcity. In run harness-ramp-2e3adf77 the section HARD-FAILED at the evidence-support step (run-section.ts) with "evidence-gate: 8 unsupported load-bearing claims exceed max 0" and committed only positioningCompetitorLandscape.error.json (verified: KEYS=['error','stack']; no body artifact). The 8 failing claims are all kind:"url" on competitor BARE HOMEPAGES (https://brex.com, https://www.airbase.com, https://www.bill.com, https://www.spendesk.com, https://www.navan.com, https://www.coupa.com, https://tipalti.com, https://ramp.com) — confirmed verbatim in the error.json. Mechanism, all verified in live source: (1) competitor-landscape.ts:52 `competitorSchema.url: z.string().min(1)` is the competitor's DISPLAY/navigation homepage; the citation is the SEPARATE sourceUrl at :57. (2) claim-extractor.ts extractStringClaims (signature :237) runs a url loop at :268 `for (const match of value.matchAll(urlPattern))` that harvests EVERY https substring in EVERY string field as a load-bearing kind:"url" claim; the only existing skip (:271) is for constructed ad-library deep-links. (3) walkValue (:468) recurses into array items with fieldPath UNCHANGED (:491; comment :480 "array indices elided") → a competitor url's fieldPath is EXACTLY `body.competitorSet.competitors.url`, NEVER `...competitors.N.url`. (4) evidence-support.ts:53 defaultLoadBearingKinds=["numeric","sourceAttribution","url"] makes url load-bearing; getMaxUnsupportedAllowed (:1612) defaults to 0 (armed via LAB_VERIFIER_MAX_UNSUPPORTED). A bare homepage the model typed into the display field is never a fetched-source body → untraceable → unsupported → 8 > max 0 → whole section hard-fails. PROOF it is not scarcity: the frozen-corpus run harness-ramp-37be5efb committed 6 differentiated competitors + a publicWeaknesses block cleanly UNDER THE SAME ARMED GATE (max 0 — corrected from the spec's false "gate=Infinity"; that same run hard-failed BuyerICP/DemandIntent on the identical "exceed max 0" message), because there the model populated competitor.url with REAL fetched listicle/pricing pages (all traceable). The ONLY difference between the runs is the string the model put in competitor.url (real fetched page vs bare homepage). gate.json scores this section ceiling:2 band:"absent"; value-read/facts.json llmScore:1 ("No deliverable... a buyer gets nothing to act on") — lowest in the deck. The underlying value (8-competitor differentiation set + negative-space SOV insight + 2 real Brex G2 complaint quotes) exists in the body the model produced; it was laundered to /dev/null by this one false-positive claim class.

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/agents/verification/claim-extractor.ts`** — `(1) Add a new module-level const immediately AFTER the selfAuthoredLabelPaths Set (which closes at line 23 with `]);`). Mirror its shape exactly: `// Display/navigation URL paths: the competitor card's homepage is an identity
// the section AUTHORS for navigation, NOT a citation (the citation is the
// sibling `sourceUrl`). Harvesting it as a load-bearing url claim guarantees an
// unsupported flag when the model writes a bare never-fetched homepage there,
// which hard-failed positioningCompetitorLandscape under the armed gate
// (run harness-ramp-2e3adf77: 8 bare homepages > max 0). Index-less fieldPath
// because walkValue elides array indices (see below).
const displayUrlFieldPaths = new Set(["body.competitorSet.competitors.url"]);` — (2) Inside extractStringClaims, in the url loop body at lines 268-283, add a guard RIGHT BESIDE the existing constructedAdLibraryLinkPattern skip (currently `if (constructedAdLibraryLinkPattern.test(url)) { continue; }` at ~271-273). Insert, as a SEPARATE `if` BEFORE the pushClaim: `if (displayUrlFieldPaths.has(fieldPath)) {
  continue;
}`. `fieldPath` is already a destructured parameter of extractStringClaims (signature lines 237-249) — no plumbing change.`
  - *Change:* THE LOAD-BEARING FIX. Exempt the competitor display/navigation homepage field (index-less fieldPath body.competitorSet.competitors.url) from load-bearing kind:"url" claim extraction, mirroring the selfAuthoredLabelPaths precedent (Set at :19, consulted via .has(fieldPath) at :255). Scope is by fieldPath via exact-string Set membership, NOT by fieldName. The competitor's REAL citation (sourceUrl, fieldPath body.competitorSet.competitors.sourceUrl) and every other section's url citation fields (different fieldPaths) stay load-bearing.
  - *Why:* The bare competitor homepage in competitor.url is a navigation identity (and the structural verifier at competitor-landscape.ts:893-899 REQUIRES it to be a present, valid-URL-shaped string — so the field must stay populated; we only suppress the claim harvest). Harvesting it as a load-bearing url claim against the armed max-0 gate is the sole cause of the hard-fail. The body's real evidence already commits cleanly. fieldPath-scoping is provably safe: the path is `.url` NOT `.N.url` (walkValue index-less, confirmed :491 + comment :480), and exact-string .has() matches NOTHING else — buyer-icp.ts:475 (body.personaReality.personas.url), market-category.ts:264, and all sourceUrl fields sit at different fieldPaths and remain load-bearing.
- **`src/lib/lab-engine/agents/verification/__tests__/claim-extractor.test.ts`** — `Add ONE new `it(...)` block inside the existing top-level `describe("extractClaims", ...)` (opens line 5). Place it after the last `it` in that describe (the dedup test ending ~line 226, before the block closes ~227) OR add a sibling `describe("display-url field-path exemption", ...)` immediately after the `describe("self-authored label paths", ...)` block (closes ~line 264). Pass a synthetic body directly to extractClaims (it roots at fieldPath "body"): `{ competitorSet: { competitors: [{ name: "Brex", url: "https://brex.com", sourceUrl: "https://www.brex.com/spend-trends/ramp-competitors" }] }, keywordDemand: { topKeywords: [{ sourceUrl: "https://www.spyfu.com/overview/domain?query=ramp.com" }] } }`. Mirror the url-assertion style at lines 51-69 and the precedent-test style at 228-264.`
  - *Change:* PIN test with THREE assertions: (a) NO kind:"url" claim whose value === "https://brex.com" is emitted (display homepage exempt) — assert `claims.some(c => c.kind==="url" && c.value==="https://brex.com")` is `false`; (b) the competitor's sibling sourceUrl listicle page IS still emitted as a kind:"url" claim (citation at fieldPath body.competitorSet.competitors.sourceUrl stays load-bearing) — assert arrayContaining `{kind:"url", value:"https://www.brex.com/spend-trends/ramp-competitors"}`; (c) a url at a DIFFERENT non-exempt fieldPath (the spyfu sourceUrl under keywordDemand) IS still emitted — proving the exemption is exact-fieldPath-scoped, not fieldName-scoped.
  - *Why:* Locks the fix and its scope so a future refactor can't silently re-broaden it to fieldName==='url' (which would mute 7+ sections' real citations) or narrow it to never fire. Assertion (c) proves fieldPath-scope via exact-string Set membership — NOT via any cross-section call (there is none: extractClaims has a single prod call site, structural-verifier.ts:576, invoked per-section with one body). Do NOT phrase this as a 'voice-of-customer reviewUrl' cross-section assertion — that call does not exist.
- **`src/lib/lab-engine/skills/positioning-competitor-landscape/SKILL.md`** — `DEFENSE-IN-DEPTH BACKSTOP (not the load-bearing fix). Anchor by HEADING TEXT, not line number: insert a new `### URL discipline` subsection immediately BEFORE the `## Iron Laws` heading (line 45), i.e. as the final content under `## Inputs` (heading at line 34, bullets end ~line 44). Bump metadata.version (line 5: `3.1.0-lab` → `3.1.1-lab`) and metadata.updated (line 6: `2026-06-11` → today) in the SAME edit.`
  - *Change:* Add ~3 lines: "### URL discipline — Put the competitor's real fetched page (the listicle/pricing/review URL you actually read) in `sourceUrl`. The `url` field is the competitor's homepage for navigation only; never invent or paste a bare homepage as if it were a citation. Every numeric/quote claim must trace to a `sourceUrl` you fetched."
  - *Why:* Steers the model to populate sourceUrl with the real fetched page and treat url as navigation — reducing the chance of regenerating bare homepages. Backstop only; the claim-extractor exemption is the deterministic load-bearing fix and must land regardless. Prose-anchored to survive line drift.

**SKILL.md.** Additive, defense-in-depth only (NOT load-bearing): add a `### URL discipline` subsection under `## Inputs` in positioning-competitor-landscape/SKILL.md instructing the model to put the real fetched page in `sourceUrl` and treat `url` as navigation-only / never paste a bare homepage as a citation. Bump metadata.version 3.1.0-lab → 3.1.1-lab and metadata.updated. Anchor by heading text (before `## Iron Laws`), not line number. The deterministic claim-extractor exemption is the actual fix; the section commits at its honest ceiling even if the model ignores this prose.

**Tests.**
- NEW it() in claim-extractor.test.ts (describe 'extractClaims' or a new 'display-url field-path exemption' describe): asserts (a) body.competitorSet.competitors.url bare homepage 'https://brex.com' emits NO kind:'url' claim, (b) sibling competitor.sourceUrl listicle page IS still emitted as kind:'url', (c) a url at a different fieldPath (keywordDemand...sourceUrl) IS still emitted — proving exact-fieldPath scope.
- REGRESSION (must stay green, do NOT modify): claim-extractor.test.ts:6 'extracts numeric, quote, url, and entity-name claims...' — its url claim is competitor.sourceUrl 'https://www.gong.io/pricing' (NOT .url), unaffected.
- REGRESSION (must stay green): structural-verifier.test.ts:130 'verifies numeric, quote, url...' — uses competitor.sourceUrl, not .url; and the SpyFu/Facebook url assertions at :628 and :749 are at other fieldPaths — all unaffected.
- REGRESSION (must stay green): provenance-gate.test.ts:429/612 — empty competitorSet.competitors arrays, no url claims, unaffected.

**Verify.**
```bash
npm run test:run -- src/lib/lab-engine/agents/verification/__tests__/claim-extractor.test.ts
npm run test:run -- src/lib/lab-engine/agents/verification/__tests__/structural-verifier.test.ts src/lib/lab-engine/agents/verification/__tests__/provenance-gate.test.ts
npx tsc --noEmit
npm run test:run
```

**Fabrication risks to guard.**
- LOW/NONE for the core fix: it suppresses a false-positive claim, invents zero data. The competitor body content (names, sourceUrls, G2 quotes, weaknesses) is whatever the model fetched live; the exemption does not add or alter any field.
- HARD-RULE-4 BLIND SPOT (the real residual risk to the honest 8): the deterministic value-read floor is BLIND to (a) wrong-company / wrong-product quote attribution — the '2 real Brex G2 complaint quotes' could be misattributed or the negative-space SOV insight ('Ramp 6 verified creatives vs no competitor running a coherent verified campaign') could be prose-overstated, and (b) token-subset entity matches in differentiation prose. The url exemption lets the body COMMIT; it does NOT verify that prose. A buyer's trust in the section now rests on prose the floor cannot check. Mitigation: the competitor.sourceUrl, evidence (sourceAttribution kind), and quote (quoteAttribution) claims REMAIN load-bearing and gated — only the navigation url is exempt — so fabricated quotes/numbers still get caught; the exemption does not relax those.
- DO-NOT-OVER-BROADEN: a future edit that keys the exemption on fieldName==='url' instead of the fieldPath Set would mute real citation url fields in buyer-icp.ts:475, market-category.ts:264, voice-of-customer/offer-diagnostic/paid-media/demand-intent/strategic-insight — laundering fabrications elsewhere. Test assertion (c) pins against this.
- CORRECTED PROVENANCE (do NOT propagate the candidate spec's claim): the clean prior run harness-ramp-37be5efb ran at the ARMED gate (max 0), NOT 'gate=Infinity'. It passed because competitor.url held REAL fetched pages. Carrying 'gate=Infinity' into the handoff would mis-describe the harness and mislead a debugger.

**Shared files:** `src/lib/lab-engine/agents/verification/claim-extractor.ts — SHARED with any other §4.x item that adjusts claim extraction (e.g. Market/Demand numeric-claim handling, BuyerICP entity handling). The displayUrlFieldPaths Set is additive and namespaced to one literal path, so conflicts are unlikely, but two items both editing the extractStringClaims body or the module-level Set region (lines 19-23) will textually collide. SEQUENCE this item's claim-extractor edit FIRST (it's the keystone unblocker) or have later items rebase onto it.`, `src/lib/lab-engine/agents/verification/__tests__/claim-extractor.test.ts — SHARED test file; additive it()/describe block, low collision risk but coordinate insertion point.`, `src/lib/lab-engine/artifacts/schemas/competitor-landscape.ts — touched here only by an additive comment (no behavior change in THIS spec); §4.7 tier/coverage reconciler work also touches this file and is the substantive owner. Keep this spec's change comment-only to avoid conflict with §4.7.`, `src/lib/lab-engine/skills/positioning-competitor-landscape/SKILL.md — owned by this section; §4.7 enrichment may also bump it. Coordinate the metadata.version bump.`

**Sequencing.** DO FIRST among the seven sections (plan §4.1 priority 1). The claim-extractor exemption is the keystone: it unblocks Competitor Landscape's own commit AND PaidMedia's competitor rows (PaidMedia consumes competitor data). No upstream dependency — the fix is self-contained in claim-extractor.ts + its test + an additive SKILL/schema comment. The §4.7 tier/coverage reconciler for Competitor (already landed per recent commits d36ab8d8/a525645f) is independent and additive; this fix does not depend on it and must not regress it. Land this before any other §4.x item that edits claim-extractor.ts so they rebase onto the displayUrlFieldPaths Set rather than colliding.

**Open questions.**
- Does the live model reliably produce ≥4-6 competitors with correct sourceUrls once the gate stops hard-failing? The frozen-corpus run proved the body is producible (6 competitors), but live tool availability under 6-way fan-out contention is a separate risk (cf. memory note: sections that pass solo can starve under full fan-out). A solo production-path rerun (/api/research-v2/rerun-section) post-fix would confirm the honest 8 before trusting the full-deck run.
- Should the §4.1 SKILL 'URL discipline' line also be added to the shared claim-discipline guidance other sections inherit, or kept section-local? Kept section-local here (surgical); broadening is out of scope for §4.1.
- After the fix, are the 2 Brex G2 complaint quotes attributed to a fetched sourceUrl that actually contains them (so the quoteAttribution claim verifies)? If the quotes ride on competitor.url alone with no real sourceUrl, they may still be unsupported. Verify in the post-fix solo rerun that complaint quotes carry a traceable sourceUrl — this is the HARD-RULE-4 prose risk the floor can't catch and the main thing standing between 'commits' and an honest 8.


### §4.2 Voice of Customer — target 8 · 🟢 READY

**Root cause.** VoC=2 is LAUNDERING, not scarcity, and the binding chokepoint is NOT where the plan §4.2 or the candidate specs aimed. EMPIRICALLY VERIFIED against the committed positioningVoiceOfCustomer.json + live src/lib/lab-engine:

THE COMMITTED ARTIFACT: confidence 0.45, body.evidenceGap=true, verification=null. evidenceGapReport shows foundPainQuoteCount=12, foundDistinctPainSourceCount=4, observedPainSourceDomains=[trustpilot.com, g2.com, trustradius.com, capterra.com], yet sufficiency={promoted:2, rejected:57, candidatesFound:59} and summary literally says "Found 1 independent domains; need at least 3" — FALSE on its own 4-domain data. Every strategic field is an apology ("evidence gap: ...").

WHY IT COMMITTED THE APOLOGY (the real control flow, traced line-by-line):
1. The prepass `result` (the pack that gates synthesis) is built EXCLUSIVELY from `selectAdmissibleVoiceOfCustomerCandidates` → `getAdmissibleVoiceOfCustomerCandidates` → `isAdmissibleQuote` (run-section.ts:10254/10304/10330/10355/10416). `isAdmissibleQuote` (quote-admission.ts:281) calls `evaluateQuoteAdmission`, which at ~:265 pushes the FATAL reason `source-url-not-permalink` whenever `!isPermalinkishUrl(url)`. Trustpilot/G2/TrustRadius/Capterra review-PAGE quotes lack a per-review permalink → 57 of 59 candidates rejected → the strict pack collapses to ~2 quotes from 1 permalinked domain.
2. `getUniqueDomains(selectedCandidates)` over that strict pack = 1 → the candidate pack-gate (voice-of-customer-candidates.ts:378 `domains.length < VOC_CANDIDATE_MIN_DOMAINS`) returns `buildGap({reason:'insufficient_independent_domains'})` → `result.ok=false`.
3. In run-section.ts the synthesis-success branch is gated at :3852 `if (!voiceOfCustomerPrepass.result.ok)` → routes to `buildVoiceOfCustomerEvidenceGapArtifact` (verification=null, evidenceGap=true, apology prose). `synthesizeVoiceOfCustomerFromCandidates` (which emits REAL grounded strategicVerdict/fourForces at voice-of-customer-synthesis.ts:340-410, no evidenceGap, confidence 0.72) and `verifySectionBody` (run-section.ts:3936) NEVER run.
4. value-read deterministicCeiling (zz-value-read.mjs) hits branch 4 (verification===null) → ceiling 7, band 'unverified-directional'. The LLM read scores the APOLOGY at 2. FINAL = min(2, 7) = 2.

THE LEVER: a directional review pack already survives in `getDirectionalVoiceOfCustomerCandidates` (run-section.ts:10194 → `isDirectionalAdmissibleQuote`, quote-admission.ts:294) which tolerates the LONE `source-url-not-permalink` reason on trusted hosts (capterra/g2/trustradius/trustpilot ALL in trustedQuoteHosts, quote-admission.ts:65-79). That pool is currently used only to feed the GAP body, never the synthesis pack. Routing the directional pack into synthesis on strict-fail makes `synthesizeVoiceOfCustomerFromCandidates` run → real prose + verification object → escapes branch 4. The quotes still render with an honest evidenceTier='directional' badge (field already exists on painQuoteSchema:91, additive 4.7 enrichment) so nothing is presented as independently-verified verbatim VoC.

CEILING ARITHMETIC CORRECTION (vetting was right): a clean synthesis commit with ≥4 verified claims lands the DETERMINISTIC ceiling at 9 (CLEAN_CEILING=9, MIN_SUBSTANTIVE_CLAIMS=4), NOT 8. The honest-8 target is the LLM value-read cap gated by prose quality (decisionCriteria genuinely unacquirable from public reviews; directional tier caps below 'fully verified'), never the floor.

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/agents/run-section.ts`** — `buildVoiceOfCustomerCandidatePrepass — the finalization block AFTER the last strict `result = selectAdmissibleVoiceOfCustomerCandidates(...)` (currently ~:10416) and BEFORE `const usableCandidates = ...` (~:10423). Verify exact line by grepping `const usableCandidates = getAdmissibleVoiceOfCustomerCandidates` and inserting immediately above it.`
  - *Change:* ADDITIVE directional-pack fallback. After all live acquisition has run and the strict `result` is final, if `!result.ok` AND result.gap.reason is the permalink-driven `insufficient_independent_domains` (use the existing `isVoiceOfCustomerCandidateFloorGap` / direct reason check) AND the directional pool clears the floors, build a SECOND pack from the directional pool: `const directionalResult = selectVoiceOfCustomerCandidates(getDirectionalVoiceOfCustomerCandidates({candidates, subjectDomain}));` and if `directionalResult.ok`, set `result = directionalResult` and mark each promoted quote's evidenceTier='directional' (so downstream renders the honest badge). Do NOT change the strict path when result.ok is already true. Do NOT lower VOC_MIN_DOMAINS or VOC_CANDIDATE_MIN_DOMAINS. The directional pool already excludes subject-domain/chrome/not-human-voice quotes (those reasons stay fatal in isDirectionalAdmissibleQuote), so the truth floor is unchanged — only the lone per-review-permalink reason is tolerated on trusted hosts.
  - *Why:* This is THE binding lever. It makes the 4-real-domain Ramp pack reach `synthesizeVoiceOfCustomerFromCandidates` so the section commits a body WITH a verification object (escaping value-read branch 4 verification===null→7 into branch 5) and real grounded prose instead of the apology. result.ok=true also flips run-section.ts:3852 away from the gap artifact. Reuses the existing directional pool + existing optional evidenceTier field — zero new schema surface, fully additive.
- **`src/lib/lab-engine/agents/run-section.ts`** — `synthesis-success branch, the dedupedBody/output construction immediately before `verifySectionBody` (~:3924-3936). Verify by grepping `const { body: dedupedBody } = dedupeQuoteBearingFields`.`
  - *Change:* When the pack reaching synthesis came from the directional fallback (carry a boolean flag on the prepass result, e.g. result.directionalFallback===true, set in edit #1), stamp evidenceTier='directional' on the unpermalinked promoted quotes/objections/switching rows on the SYNTHESIZED body via the existing `downgradeUnpermalinkedVocQuotes`-style relabel, BEFORE `verifySectionBody`, leaving strategicInsight + fourForcesBalanceVerdict + all block prose INTACT. Do NOT set body.evidenceGap. Permalink status becomes a per-row tier badge, never a section kill-switch.
  - *Why:* Honesty preservation (Hard Rule 1): the synthesized prose is genuine and grounded, but the underlying quotes are review-page (non-permalink) extracts. The directional tier badge tells the reader/renderer these are review-sourced directional signal, not independently-verified verbatim VoC — exactly the plan §4.2 'demote permalink rule to a per-row/per-block tier BADGE' intent — without reintroducing the apology. additiveSafe: reuses the optional evidenceTier field (painQuoteSchema:91) the 4.7 enrichment already shipped.
- **`src/lib/lab-engine/artifacts/schemas/voice-of-customer.ts`** — `getSourceKey (:353-358), consumed by the secondary painSourceCount floor (:509-518) and the dedup at :368.`
  - *Change:* Return `getRegistrableDomain(sourceUrl) ?? fallback` instead of `new URL(sourceUrl).hostname.replace(/^www\./,'')`. getRegistrableDomain is NOT yet imported in this file — ADD the import from '../../domain-utils' (verify path: schemas/ is one dir below lab-engine; domain-utils.ts is at lab-engine root, so '../../domain-utils'). Collapses subdomain variants (ca.trustpilot.com → trustpilot.com) so the schema painSourceCount floor counts the same registrable domains as synthesis.
  - *Why:* DEFENSIVE HARDENING ONLY — explicitly NOT the binding fix for this run (the committed pack's quote sourceUrls are bare hosts so painSourceCount=3 already passes ≥VOC_MIN_DOMAINS=3). It guarantees the secondary schema floor and the synthesis counter agree, so a FUTURE subdomain-bearing directional pack that commits via synthesis (edit #1/#2) can't then trip the schema painSourceCount floor at :514 and fall back to the gap artifact. Do NOT gate the build on it; do NOT touch the synthesis-side counter (voice-of-customer-synthesis.ts uniqueDomains:200 already uses getRegistrableDomain and is correct).
- **`src/lib/lab-engine/skills/positioning-voice-of-customer/SKILL.md`** — `Iron Law / evidence-handling section (grep for 'permalink' or 'directional' or 'Iron Law' to locate). Read the file first.`
  - *Change:* Add ONE Iron Law clarifying the directional tier: review-page (non-permalink) quotes from trusted hosts (Trustpilot/G2/TrustRadius/Capterra) are ADMISSIBLE as directional buyer signal and MUST be rendered with an explicit 'directional · review-sourced' tier badge — they are NOT to be relabeled into a section-wide evidence-gap apology, and they are NOT to be presented as independently-verified verbatim VoC. fourForcesVerdict and strategicVerdict are DERIVED inference layered on the quotes (planner synthesis), never buyer-stated language — label as derived read. Do NOT add domain content/numbers/named mechanisms (provenance-gate exemplar-leak risk).
  - *Why:* Aligns the skill prose with the code behavior so the section's self-presentation is honest-directional, not apologetic, supporting the LLM value-read rise. Prose-only, no exemplar domain content.

**Tests.**
- NEW unit test in src/lib/lab-engine/agents/__tests__/run-section-voice-of-customer-candidates.test.ts (or a new voice-of-customer-directional-pack.test.ts): build a candidate set of 4 trusted-host review-page (non-permalink) quotes across trustpilot/g2/trustradius/capterra + ≥VOC_MIN_QUOTES total; assert that selectVoiceOfCustomerCandidates over getDirectionalVoiceOfCustomerCandidates yields result.ok===true with ≥3 distinct registrable domains, whereas the strict selectAdmissibleVoiceOfCustomerCandidates over the same set returns insufficient_independent_domains. Pins the lever.
- NEW unit test asserting the synthesized body from a directional-fallback pack: synthesizeVoiceOfCustomerFromCandidates over the directional pack returns ok with body.strategicInsight.strategicVerdict NOT starting with 'evidence gap' and body.evidenceGap !== true; and that each promoted painLanguage quote carries evidenceTier==='directional'. Pins the honesty badge + no-apology.
- UPDATE src/lib/lab-engine/agents/__tests__/voice-of-customer-directional-promotion.test.ts: this file currently exercises getDirectionalVoiceOfCustomerCandidates feeding the GAP body. Add a case that the SAME directional pool, when it clears the floor, is routed to synthesis (not the gap body) — assert the no-longer-apology path. Verify the existing assertions about the disjoint carve still pass (do not break them).
- RUN src/lib/lab-engine/agents/verification/__tests__/quote-admission.test.ts unchanged — confirm isDirectionalAdmissibleQuote / isAdmissibleQuote contract is untouched (no edits to quote-admission.ts; this is a regression guard).
- NEW/UPDATE schema test in any voice-of-customer schema test file: getSourceKey('https://ca.trustpilot.com/review/x') === 'trustpilot.com' and painSourceCount over [trustpilot.com, ca.trustpilot.com, g2.com] === 2 (defensive-hardening regression).
- RUN the scoped 44 from the plan: src/app/api/research-v2/run-lab-section/__tests__/route.test.ts (16) + src/lib/lab-engine/agents/__tests__/run-section-corpus-only.test.ts (28) — must stay green; the corpus-only VoC fixture must NOT regress to evidenceGap and must NOT laundered-commit.

**Verify.**
```bash
npm run test:run -- src/lib/lab-engine/agents/__tests__/run-section-voice-of-customer-candidates.test.ts src/lib/lab-engine/agents/__tests__/voice-of-customer-directional-promotion.test.ts
npm run test:run -- src/lib/lab-engine/agents/verification/__tests__/quote-admission.test.ts src/lib/lab-engine/agents/__tests__/voice-of-customer-synthesis.test.ts
npm run test:run -- src/app/api/research-v2/run-lab-section/__tests__/route.test.ts src/lib/lab-engine/agents/__tests__/run-section-corpus-only.test.ts
npx tsc --noEmit
npm run test:run
node scripts/zz-value-read.mjs (against a fresh harness re-run of VoC on the Ramp fixture) — confirm band moves from 'unverified-directional' (ceiling 7) to 'clean'/'thin-clean' with verification attached, and the LLM value-read rises from 2 toward 8. The numeric 8 is the LLM cap, NOT the deterministic ceiling (which becomes 9 on a clean commit).
```

**Fabrication risks to guard.**
- FLOOR-BLIND (Hard Rule 4): once VoC commits via synthesis, the deterministic floor cannot verify (a) the fourForcesBalanceVerdict / strategicVerdict grounding — these are DERIVED inference prose carrying no number/URL/quote, so they never become verifier claims; (b) wrong-company quote attribution — the verifier's quote-match is NOT URL-scoped; (c) token-subset entity matches. The synthesized prose at voice-of-customer-synthesis.ts:340-410 is a FIXED template grounded in the candidate snippets (low fabrication risk because it's deterministic, not model-authored), but the LLM value-read + SKILL Iron Laws + a human spot-check are the only nets. Keep a human spot-check on this section's first post-fix commit.
- DIRECTIONAL TIER MUST RENDER: if edit #2's evidenceTier='directional' stamp is dropped or the renderer ignores the badge, the section would present review-page extracts as independently-verified verbatim VoC — a trust violation. The badge is load-bearing for honesty; verify it survives into the committed body and the Audit Reader card.
- DO NOT chase the plan §4.2 'fix the domain counter at synthesis.ts:585' as the fix — VERIFIED that counter (uniqueDomains:200) ALREADY uses getRegistrableDomain and is correct; 'fixing' it is a no-op that would burn effort and could mask the real lever. Same trap the vetting caught on the candidates-file getUniqueDomains.
- REGRESSION: the directional fallback must NOT fire when the strict pack already succeeded (guard on !result.ok) and must NOT tolerate any reason other than the lone source-url-not-permalink on trusted hosts (isDirectionalAdmissibleQuote already enforces this — do not weaken it). A blanket relaxation would re-admit subject-domain/chrome/not-human-voice quotes = laundering.

**Shared files:** `src/lib/lab-engine/agents/run-section.ts — HIGH-CONFLICT: edited by nearly every section build item (BuyerICP, Market, Demand, Offer, PaidMedia all touch run-section.ts). VoC edits are localized to the buildVoiceOfCustomerCandidatePrepass region (~10250-10440) and the VoC synthesis-success branch (~3884-3993); coordinate merge order to avoid line-drift. Re-grep all locators at execution time rather than trusting line numbers.`, `src/lib/lab-engine/agents/verification/quote-admission.ts — READ-ONLY here (no edits), but shared with any item touching quote admission; the dedupeQuoteBearingFields/isAdmissibleQuote contract is relied on by BuyerICP and Competitor too.`, `src/lib/lab-engine/artifacts/schemas/voice-of-customer.ts — VoC-exclusive; low conflict, but the 4.7 enrichment evidenceTier field is shared schema vocabulary.`, `src/app/api/research-v2/run-lab-section/__tests__/route.test.ts + src/lib/lab-engine/agents/__tests__/run-section-corpus-only.test.ts — the scoped 44-test gate shared by ALL section build items; every item must keep them green, so run them after each item lands.`

**Sequencing.** Depends on Phase 1 keystone (the confidence/presentation split that removes apologetic framing chrome) landing first — plan §4.2 explicitly states VoC's honest self-presentation depends on the keystone. Within the section order VoC is item #2 (after Competitor #1). VoC UNBLOCKS PaidMedia #7 (VoC re-attribution: PaidMedia binds competitorReviewInsights rows to VoC painLanguage quotes). Edits #1 and #2 are tightly coupled (the directionalFallback flag set in #1 is read in #2) and MUST land in the same commit. Edit #3 (getSourceKey) is independent defensive hardening and can land separately. Re-verify all run-section.ts line locators against HEAD immediately before editing, because other section items mutate the same file.

**Open questions.**
- LIVE-RETRIEVAL STARVATION (deferred, must verify by harness re-run): the committed run promoted only 2 of 59 candidates partly via the strict permalink gate, but it is UNVERIFIED whether the deeper ranking/selection also starved the directional pool. If live retrieval still yields <3 directional domains, the directional fallback won't fire and the section degrades to an HONEST directional badge — NOT an apology, but also not an 8. MUST re-run the VoC harness on the Ramp fixture after the fix and read the committed body before promising 8.
- DIRECTIONAL-FALLBACK FLAG PLUMBING: confirm the cleanest way to carry the directionalFallback signal from buildVoiceOfCustomerCandidatePrepass (edit #1) to the synthesis-success branch (edit #2) — likely an additive optional boolean on the VoiceOfCustomerCandidatePrepass return type. Verify it doesn't collide with existing result.ok consumers.
- RENDERER BADGE: confirm the Audit Reader VoC card (src/components/research-v3) reads evidenceTier and renders the 'directional · review-sourced' badge; if not, the honesty signal is invisible to the user even though the data is correct (the plan §4.2 visualization calls for a per-row tier badge). This may be a separate render-side item.
- LLM VALUE-READ CALIBRATION: the plan §379 requires the value-read agent reproduce VoC=2 on the CURRENT deck before its post-fix scores are trusted. Confirm calibration passes before trusting the post-fix 8.


### §4.3 Market & Category — target 8 · 🟢 READY

**Root cause.** Shipped 3 = min(llmScore 3, ceiling 7). Ceiling=7 from unsupportedCount=9 (zz-value-read.mjs:256). llmScore=3 because the marketSize signals-under-2 floor (market-category.ts:653) fired with 1 signal, the gate at run-section.ts:8855 returned minimumsFailed, and 8881 built the gap artifact pinning confidence 0.3 (3009). Fix: promote the verifier-supported 13B valuation (claims[11]) into marketSize.signals as a 2nd funding-flow signal. Ceiling 8 not 9: volumes 440/27200 are live-tool-fragile.

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/agents/run-section.ts`** — `Add helper promoteVerifiedValuationSignal(body, verification) and call it between line 8764 (verifySectionBody) and 8765 (buildEnvelope) guarded by if sectionId is positioningMarketCategory. Helper: no-op if marketSize.signals length is 2 or a funding-flow signal already exists; else find first verification.claims entry with status verified, entailmentVerdict supported, matchedSourceRef.kind corpusExcerpt and a non-empty sourceUrl, claim.kind numeric, claim.value or claim.raw a billion valuation, claim.raw mentions valuation or funding; structuredClone and push one funding-flow signal copying evidence and sourceUrl verbatim, omit the optional verification field. Read nested c.claim.kind NOT c.kind. Apply the same guard on the second gate path between 11562 and 11716. This is the only window with body and claims before the gate at 8855; the candidate site reconcileMarketCategoryCoverage at 5363 is inside annotateEvidenceSupportReview which runs after the gap artifact at 8882 so it is dead code.`
  - *Change:* Verifier-gated funding-flow promotion before the committable gate on both paths.
  - *Why:* Clears the signals-under-2 floor with the supported 13B valuation so the 0.3 pin never fires; gated so honest where absent.
- **`src/lib/lab-engine/artifacts/schemas/market-category.ts`** — `In marketCategoryBodySchema near line 200-250 add optional confidenceBasis (string), categoryVerdict (enum own-existing-shelf/create-new-category/defend-current-frame), tamGapPosture (string). No min/max.`
  - *Change:* Add three optional body fields.
  - *Why:* Typed call plus honest TAM posture; pure-optional so prior artifacts still parse.
- **`src/lib/lab-engine/skills/positioning-market-category/SKILL.md`** — `Lead the asymmetry not in nonObviousRead; Iron Law vs defeatism with confidenceBasis; valuation as a funding-flow signal; TAM as a 2-input CTA via tamGapPosture; exact buyer labels Procurement; demote model-derived percentages; live-tool-fragile volume caveat.`
  - *Change:* Rewrite SKILL.md framing and honesty guidance.
  - *Why:* Raises llmScore by fixing framing; prevents re-introducing the 9 unsupported rows.

**Tests.**
- NEW run-section-market-valuation-promotion.test.ts plus market-category.test.ts optional-field parse test; RUN downgrade-coverage and run-section-corpus-only.

**Verify.**
```bash
npx tsc --noEmit
npm run build
```

**Fabrication risks to guard.**
- Live-tool-fragile lead and wrong-company over-match are both floor-blind; demotion of 40% and 4.2% must not be re-sourced.

**Shared files:** `src/lib/lab-engine/agents/run-section.ts VERY HIGH (shared pipeline 8758-8900 and 11562-11716, all 7 sections); downgrade-coverage.ts NOT touched.`

**Sequencing.** run-section.ts helper edit shares the pipeline window with all siblings; land sequentially. Plan orders Market after Competitor and VoC.

**Open questions.**
- Subject-domain check on the promoted valuation?
- Confirm 11562-11716 is the market-category path.


### §4.4 Buyer & ICP — target 7.5 (from 5.5) · 🟢 READY

**Root cause.** The committed positioningBuyerICP.json is a SUCCESSFUL draft (not .error.json) carrying real, corpus-grounded evidence, but it self-presents as a 0.4-confidence evidence-gap apology. Two independent mechanisms drive this below its ceiling. (1) DOUBLE-HEDGE on grounded evidence: withNormalizedBuyerICPOutput (run-section.ts:7440) fires injectPersonaGap on `validatorGradePersonaCount < 3`. countValidatorGradePersonas (run-section.ts:7059, via isValidGroundedBuyerUnit) is tier-BLIND — it counts the 2 verifier-DOWNGRADED Ramp personas (coverage.byTier={hard_evidence:0,directional_signal:2}) as grade-2, so `2 < 3` injects evidenceGap=true + reason=insufficient_named_buyer_personas + requiredNamedPersonaCount:3 onto a body that HAS 2 grounded buyer units. The acquisition-ledger early-return (buyer-icp-acquisition-ledger.ts:259) compounds it: when report is absent it gates `groundedPersonaCount >= BUYER_ICP_PROMOTED_PERSONA_FLOOR(3)` to skip synthesis, but isNamedBuyerPersona (named-human-only) counts only Alicia Coleman → 1 < 3 → synthesizes the 'personaReality is empty' insufficient report even though personaReality is NOT empty. (2) The DETERMINISTIC value-read ceiling is 7, band=overclaim, reason='11 unsupported load-bearing claim(s) shipped (verified 9)'. This ceiling is BLIND to the hedging — it caps purely on the verifier's unsupported-claim count. The 11 unsupported claims cluster into: the unsourced '10-2,000' employee-band numeric interpolation (×2, also in strippedNumericClaims), the WizeHire/Alicia Coleman persona chain, and the 'Finance team buyer'/About-Us chain — all matchedSourceRef=null because containmentPassRate=0 (the LIVE re-fetch of JS-rendered Ramp pages didn't echo the entity string; the evidence IS real in the frozen corpus, excerptIndex 3+18). So the honest 7.5 has two levers: (a) STOP the redundant double-hedge so 2 grounded personas commit as a usable directional partial, not an apology (pure de-hedge, adds zero new claims); (b) REDUCE the unsupported-claim count by reframing SKILL.md to forbid the '10-2,000' numeric interpolation and lean on segmentLabel grounding (the one move that actually raises the deterministic ceiling). Confidence math (Math.min(quoteCapped,0.4) at run-section.ts:4857) is a SHARED cross-section function — OUT OF SCOPE; the Phase-1 confidence-semantics split is what stops 0.4 being the headline.

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/agents/run-section.ts`** — `injectPersonaGap predicate inside withNormalizedBuyerICPOutput, ~line 7440. Current: `const injectPersonaGap = normalizedPersonaReality !== null && !stripUnnecessaryGap && validatorGradePersonaCount < 3 && !personaRealityHasBlockGap && !alreadyDeclaresPersonaGap;``
  - *Change:* Change ONLY the `validatorGradePersonaCount < 3` conjunct to `validatorGradePersonaCount === 0`. Leave stripUnnecessaryGap (still `>= 3`), personaRealityHasBlockGap, alreadyDeclaresPersonaGap UNCHANGED. Single load-bearing line. Do NOT touch the stripUnnecessaryGap path (that is the separate model-authored-gap strip; over-relaxing it would un-hedge a genuinely empty section).
  - *Why:* countValidatorGradePersonas (7059) is tier-blind via isValidGroundedBuyerUnit (grounded-buyer-unit.ts), which checks only a live http(s) sourceUrl + named/segmentLabel grounding and never inspects verification.outcome/evidenceTier. The 2 verifier-DOWNGRADED Ramp personas already count as grade-2, so `< 3` force-injects evidenceGap=true on a body that HAS grounded buyers — the double-hedge. Gating on `=== 0` keeps the honest exit ONLY when there is genuinely zero grounded buyer unit, and lets >=1 grounded-but-downgraded persona commit as a usable directional ICP. validateBuyerICPMinimums (buyer-icp.ts:556) is already floor-1 quality-aware ('have N grounded, need >=1') so it does not re-reject. Pure removal of a gap-injection branch; adds NO new load-bearing claim, so the deterministic unsupported-count ceiling is unaffected.
- **`src/lib/lab-engine/agents/__tests__/with-normalized-buyer-icp-output.test.ts`** — `Two tests pin the OLD `<3` semantics and WILL FAIL after edit 1: (a) ~line 258 'keeps the gap when fewer than 3 validator-grade personas remain' (feeds 2 personas via gappedOutput(threeValidPersonas.slice(0,2)), asserts evidenceGap===true); (b) ~line 291 'injects an honest persona gap when <3 validator-grade personas...' (feeds 2 personas via buyerOutput(twoValidPersonas), asserts evidenceGap===true, foundNamedPersonaCount===2, requiredNamedPersonaCount===3).`  ⚠️ NOT additive-safe — review
  - *Change:* Rewrite both to the new `=== 0` contract. Test (a): rename to 'keeps the gap ONLY when zero validator-grade personas remain' — feed 2 personas, assert evidenceGap toBeUndefined() (committed directional, no apology); add a sibling case feeding `[]` (0 personas) asserting evidenceGap toBe(true). Test (b): rename to 'does NOT inject for 2 grounded personas (directional partial, not apology)' — feed twoValidPersonas, assert evidenceGap toBeUndefined() AND personasOf(output) toHaveLength(2) (the 2 real personas still carried); add a sibling 'injects an honest persona gap when ZERO grounded personas' feeding `buyerOutput([])` asserting evidenceGap toBe(true), report.reason 'insufficient_named_buyer_personas', report.foundNamedPersonaCount 0. KEEP the unchanged-behavior tests at ~line 248 (>=3 strips model gap) and ~line 308 (>=3 no inject) as regression anchors; KEEP the 'does NOT override a model-declared gap' test (~line 330) unchanged.
  - *Why:* npm run test:run is a MANDATORY verification gate (CLAUDE.md). Without this rewrite the two `<3` assertions fail and the change cannot land. The new tests pin the corrected contract: grounded-but-downgraded personas commit as directional; only zero-grounded triggers the honest apology.
- **`src/lib/lab-engine/agents/buyer-icp-acquisition-ledger.ts`** — `Report-absent early-return guard, line 259: `if (groundedPersonaCount >= BUYER_ICP_PROMOTED_PERSONA_FLOOR) { return artifact; }`. groundedPersonaCount is computed line 250 via personas.filter(isNamedBuyerPersona) (named-human-only). BUYER_ICP_PROMOTED_PERSONA_FLOOR = 3 is the constant at line 26.`
  - *Change:* Change ONLY the guard comparator from `groundedPersonaCount >= BUYER_ICP_PROMOTED_PERSONA_FLOOR` to `groundedPersonaCount >= 1`. DO NOT lower the constant at line 26 (rejected below). Update the stale comment block at ~lines 248-258 to: a report-absent body carrying >=1 grounded persona is left untouched (no synthesized insufficient report); the synthesized 'personaReality is empty' report is reserved for the genuine zero-grounded case. Also fix the now-stale line 24-25 comment claiming the floor 'mirrors validateBuyerICPMinimums' — that validator is floor-1 quality-aware (buyer-icp.ts:556).
  - *Why:* For the Ramp body, isNamedBuyerPersona (named-human-only via isLikelyNamedBuyerIdentity) counts only Alicia Coleman → groundedPersonaCount===1 (the run-section count of 2 diverges by predicate — ledger uses named-human-only, run-section uses tier-blind segment-OR-named). Under `>= 3` the guard FALSE-synthesizes the 'personaReality is empty' insufficient report even though personaReality has grounded units, contradicting the body. Gating on `>= 1` skips the dishonest synthesis whenever there is any grounded persona, while preserving the honest synthesis path for the true zero-grounded body. This does NOT touch promotedFloor, so computeAcquisitionSufficiency banding stays correct (promoted:1 → tier 'partial', NOT 'sufficient').
- **`src/lib/lab-engine/agents/__tests__/buyer-icp-acquisition-ledger.test.ts`** — `New test needed: the report-absent / 1-grounded-persona boundary is currently UNTESTED. The existing C-LEDGER guard test (~line 552 'sufficient personas, report absent') only covers the >=3 (3-grounded) boundary; the existing happy-path tests (line 105+) pass requiredNamedPersonaCount:3 as fixture INPUT (not assertions on the constant), so they survive edit 3 unchanged.`  ⚠️ NOT additive-safe — review
  - *Change:* Add one test to the C-LEDGER describe block: 'does NOT synthesize an insufficient report for a 1-grounded-persona report-absent body'. Build a report-absent artifact (evidenceGapReport:null) whose personaReality.personas = [one isNamedBuyerPersona-passing named human, e.g. {name:'Alicia Coleman', title:'Controller', company:'WizeHire', role:'champion'}]. Call withBuyerICPAcquisitionLedger with 1 promoted candidate. Assert: body.evidenceGap NOT toBe(true); JSON.stringify(body) NOT toContain('personaReality is empty'); no synthesized report.reason === buyerICPEvidenceGapReason. This pins the `>= 1` guard. Verify the existing 5 ledger assertions (lines 105,140,292,424,465) still pass unchanged.
  - *Why:* The 1-2 grounded boundary (the exact Ramp case) was previously untested; edit 3 changes its behavior, so a regression anchor is required. Confirms the guard relaxation does not flip a 1-grounded body into a dishonest gap and does not regress the 0-grounded synthesis path.
- **`src/lib/lab-engine/skills/positioning-buyer-icp/SKILL.md`** — `Move 1 'Five-layer ICP' (~line 64) and the disqualifier/numeric-grounding guidance. The unsourced '10-2,000 employees' band (claims 0,1 + 4 strippedNumericClaims entries on icpExistenceCheck.prose / firmographicCuts[1].value / clusters.prose) is an interpolation from homepage 'fast-growing startups to established enterprises' language with NO source.`
  - *Change:* Additive prose only (no structural rewrite). In Move 1, add an explicit prohibition: 'NEVER interpolate an employee-count, revenue, or account-count BAND (e.g. "10-2,000 employees") from qualitative homepage language like "startups to enterprises". A firmographic cut carries a numeric ONLY when that exact number appears verbatim on the cited sourceUrl page; otherwise state the boundary qualitatively (e.g. "fast-growing startups through established enterprises") with NO fabricated range. Unsourced numerics are stripped by the verifier and counted as unsupported load-bearing claims, dragging the section ceiling down.' Reinforce the existing Move 2 segmentLabel-first guidance: when a named champion's live page fails containment (JS-rendered), keep it as a directional segmentLabel persona rather than re-asserting the named-human claim. Do NOT add any new schema-field instruction.
  - *Why:* The deterministic ceiling (7, band=overclaim) is driven SOLELY by the 11-unsupported count, and the '10-2,000' numeric is 2 of those 11 (plus 4 strippedNumericClaims). Forbidding the interpolation is the single highest-leverage move that ACTUALLY raises the ceiling on the next run — every other edit de-hedges presentation but leaves the unsupported count untouched. The claim-extractor (claim-extractor.ts extractStringClaims) walks ALL string fields and emits a numeric claim from any currency/percent/magnitude match, so a verbatim-only numeric discipline directly lowers the unsupported count.

**Tests.**
- src/lib/lab-engine/agents/__tests__/with-normalized-buyer-icp-output.test.ts — rewrite the two `<3` tests to the `=== 0` contract (2 grounded → evidenceGap undefined; 0 grounded → evidenceGap true); keep the >=3 strip/no-inject anchors
- src/lib/lab-engine/agents/__tests__/buyer-icp-acquisition-ledger.test.ts — add 1-grounded-persona report-absent guard test (no dishonest synthesis); confirm existing 5 assertions (lines 105,140,292,424,465) unchanged
- src/components/research-v2/section-renderers/__tests__/buyer-icp.test.tsx — run as regression: the renderer already reads personaReality.coverage; confirm a 2-persona directional body (no evidenceGap) renders the personas + existing coverage, not the empty-state path (renderer empty-check at buyer-icp.tsx:85-88)

**Verify.**
```bash
npm run test:run -- src/lib/lab-engine/agents/__tests__/with-normalized-buyer-icp-output.test.ts
npm run test:run -- src/lib/lab-engine/agents/__tests__/buyer-icp-acquisition-ledger.test.ts
npm run test:run -- src/components/research-v2/section-renderers/__tests__/buyer-icp.test.tsx
npx tsc --noEmit
npm run test:run
```

**Fabrication risks to guard.**
- DROPPED (Variant A laundering bug): lowering BUYER_ICP_PROMOTED_PERSONA_FLOOR from 3 to 1 at line 26 would make computeAcquisitionSufficiency band promoted:1 >= floor:1 → tier 'sufficient' (verified strategic-insight.ts:148-159 meetsAbsoluteFloor) — laundering a single-promoted ledger into a self-reported 'sufficient'. HARD RULE 1 violation. The constant MUST stay 3; only the line 259 early-return guard moves to `>= 1`.
- DROPPED (redundant + claim-risk): the candidate 'verifierDowngradeNote on personaReality.coverage' field is REDUNDANT — personaReality.coverage (blockCoverageFieldSchema) ALREADY carries strippedByVerifier + byTier downgrade info (it is how the harness recorded the 2 downgraded Ramp personas). Adding a free-text note would be walked by claim-extractor and could emit new entityName/numeric claims, RAISING the unsupported count and LOWERING the ceiling. The renderer should consume the EXISTING coverage field.
- DROPPED (claim-raising): candidate additive schema fields icpScorecard.thirtySecondTest / triggerSchema.painWeight that carry free numerics or entity strings. claim-extractor.walkValue is index-less and extracts numeric claims from ANY string field (currency/percent/magnitude regex) and entityName claims from name-like fields — a thirtySecondTest containing '10-2,000 employees' would re-introduce the exact stripped numeric as a NEW unsupported claim, defeating the ceiling lift. If any presentation field is added later it MUST be claim-inert (enum/boolean, or text guaranteed verbatim-on-source).
- The deterministic value-read floor is BLIND to: (a) whether the 2 retained personas are wrong-company / token-subset attributions — 'Finance team buyer' is a generic-token label, not a verified human, and the floor cannot tell a directional segment persona from a fabricated one; (b) renderer prose quality. So the honest 7.5 rests partly on prose the floor cannot verify; the gate's ceiling of 7 will only rise on the next run if the SKILL.md numeric prohibition actually reduces the unsupported count from 11.
- The containmentPassRate=0 downgrade is a LIVE-FETCH artifact (JS-rendered Ramp pages not echoing the entity string), declared OUT OF SCOPE by the plan. These edits de-hedge the PRESENTATION of already-downgraded evidence; they do NOT attempt to re-promote downgraded rows to hard_evidence (which would be fabrication). The personas stay directional_signal — honest.

**Shared files:** `src/lib/lab-engine/agents/run-section.ts — LARGE shared file touched by nearly every section build item (the answer-tool path, all withNormalized* normalizers, confidence math at 4857). Edit 1 is a single-line change to the BuyerICP-only withNormalizedBuyerICPOutput block (~7440); MUST NOT touch the shared Math.min confidence function (4857) or any cross-section normalizer. High merge-conflict surface — sequence BuyerICP's run-section edit to land in isolation or rebase carefully against Competitor/VoC/Market/Offer/Demand items that also edit run-section.ts.`, `src/lib/lab-engine/agents/verification/claim-extractor.ts — READ-ONLY for this item but shared as the gate that other section items must also respect; any additive field added by ANY section that carries free numerics/entities will raise that section's unsupported count. Cross-item invariant: additive presentation fields must be claim-inert.`, `src/lib/lab-engine/artifacts/schemas/buyer-icp.ts — BuyerICP-only schema; NOT touched by this final spec (the redundant schema-field additions were dropped). If a later renderer-lead item re-opens it, it is BuyerICP-scoped and low-conflict, but the .strict() bodies require explicit optional declaration.`

**Sequencing.** "Land AFTER the Phase-1 keystone (confidence-semantics split) per the build plan — that keystone is what stops 0.4 confidence reading as the headline; without it, de-hedging the gap still leaves a 0.4-confidence directional draft. The two code edits (run-section.ts injectPersonaGap, ledger early-return) are independent of each other and can land together. The SKILL.md numeric prohibition is independent and can land anytime, but its ceiling-lift only manifests on the NEXT live/harness run (it changes model authoring, not the committed artifact). Because run-section.ts is a shared conflict surface with Competitor/VoC/Market/Offer/Demand, prefer to sequence this single-line BuyerICP edit so it rebases cleanly against the other run-section.ts touches rather than racing them."

**Open questions.**
- The deterministic ceiling (7) will only rise on a re-run if the SKILL.md numeric prohibition reduces the 11 unsupported claims below the band threshold. The code edits (de-hedge + ledger guard) do NOT change the committed unsupported count — they improve presentation/value-read of the EXISTING artifact. Confirm with the build-plan owner whether the 7.5 honest target is scored on (a) the re-run artifact with fewer unsupported claims, or (b) a value-read of the de-hedged presentation of the current artifact — the two edits serve different scoring models.
- Renderer-lead value: the existing renderer (buyer-icp.tsx) reads personaReality.coverage and personas[0]. After de-hedging, does it surface the directional personas + coverage downgrade note prominently, or still lead with an empty/apology state? A renderer copy/ordering pass (consuming the EXISTING coverage field, no schema change) may be needed for the full value-read lift — but that is value-read-prose the deterministic floor is blind to, so it is a presentation polish item, not a ceiling-raiser. Confirm whether it is in this item's scope or deferred.
- Predicate divergence is now documented (run-section tier-blind counts 2; ledger named-human-only counts 1). Both relaxed gates fire correctly for the Ramp case, but a future body with 2 segmentLabel personas and 0 named humans would still hit the ledger named-human path at groundedPersonaCount===2 → `>= 1` true → no synthesis (correct). No action needed, but flagged so a reviewer does not 'fix' the divergence by unifying the predicates (which would change the honest-zero boundary).


### §4.5 Offer Diagnostic — target 8 · 🟢 READY

**Root cause.** Offer Diagnostic ALREADY commits clean at the deck's highest deterministic ceiling: facts.json shows ceiling=9, band="clean", reason="verifier clean (verified 18, 0 unsupported)", confidence=0.9473684210526315 (the single highest in the deck; .947 = 18/19 because the ONE provenanceFlag is the user-provided "$1k–$10k ACV" numeric in retentionHealth, correctly flagged no-source, never laundered). The §4.7 enrichment already landed here (every block carries coverage.{byTier,acquisitionGaps,readiness}; every row carries evidenceTier) — this is the deck's reference implementation. So the honest target is 8, NOT 9, and the build is a FABRICATION-PREVENTION + framing job, not a data-acquisition job. It sits at 8 for exactly two reasons: (1) channelTruth is a pure structural gap — all 3 channels (Google/Meta/LinkedIn) carry hasWorked="unknown", quantifiedEvidence="No source-backed performance data available", sourceUrl="https://ramp.com/", coverage.byTier={hard_evidence:0,directional_signal:3}, yet coverage.readiness self-labels "adequate" (verified in positioningOfferDiagnostic.json) — a media buyer distrusts a self-labelled-adequate table with zero performance data. Per-channel CPL/CAC is STRUCTURALLY UNACQUIRABLE from public sources (operator ad-platform exports only); no schema change fills it. decodeRepairs already dropped channelTruth.channels[3] twice (wrong sourceUrl primitive) leaving exactly 3 at the >=3 floor. (2) A latent prose-fabrication risk (HARD RULE 4): the would-be-9 thesis "non-branded demand is ZERO / demand-creation is the only lever" is FALSE — it contradicts the pipeline's own Demand artifact (13 commercial keywords with real SpyFu volume at HIGHER CPC than branded) and the verifier is BLIND to it because that inference lives in prose, not in an extractable numeric/quote. The honest-8 build GUARANTEES the section can never drift to that laundered thesis and adds ONE contextualizing, self-policing benchmark field.

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/artifacts/schemas/offer-diagnostic.ts`** — `funnelBreakSchema z.object().strict() at lines 64-74. Insert the new field immediately AFTER the `verification: rowVerificationFieldSchema,` line (currently line 71) and BEFORE the closing `})` + `.strict()` (lines 73-74). USE THIS SEMANTIC ANCHOR, NOT a literal line range — the candidate spec's '62-73' is stale (drifted ~2 lines).`
  - *Change:* Add ONE optional, additive, render-inert field to funnelBreakSchema:

    benchmark: z
      .object({
        stageLabel: z.string().min(1),
        typicalRange: z.string().min(1),
        excellentRange: z.string().min(1),
        sourceUrl: z.string().url(),
      })
      .strict()
      .nullable()
      .transform((value) => value ?? undefined)
      .optional(),

NOTE the deliberate divergences from the candidate spec: (a) sourceUrl is z.string().url() NOT z.string().min(1) — a min(1) string lets an unfetched band ride a non-URL placeholder past the claim-extractor's url claim (urlPattern only fires on real URL syntax); .url() forces a real fetchable citation. (b) The .nullable().transform(undefined).optional() chain MATCHES the file's established evidenceTierFieldSchema/rowVerificationFieldSchema pattern (lines 35-44) so an absent or null benchmark validates and a committed artifact with zero benchmarks still ships. NO prepass, NO runtime injection — benchmark is model-filled ONLY via the model's own web_search during the Offer run (published B2B SaaS conversion bands ARE publicly acquirable, unlike per-channel CAC).
  - *Why:* Contextualizes funnel thresholds (e.g. provesWrongIf 'trial-to-paid above 20%') with a published benchmark band so the cutoff reads as benchmarked, not bare-asserted — the marginal honest lift toward a defensible 8. SELF-POLICING against fabrication (this INVERTS the candidate's medium-severity worry): a numeric range in benchmark.typicalRange like '15-25%' IS extracted by claim-extractor.ts walkValue (recurses all nested records) as a kind:'numeric' claim, and evidence-support.ts line 53 lists 'numeric' in defaultLoadBearingKinds — so an unfetched band becomes an UNSUPPORTED load-bearing claim and (with LAB_VERIFIER_MAX_UNSUPPORTED=0 armed) HARD-FAILS the section; a model-fetched band entails against the tool result and passes. The verifier is therefore NOT blind to a fabricated benchmark number. Renderer is inert: offer-diagnostic.tsx funnelStepsFromBreak (line 113) reads only stageName/metric/magnitude/hypothesis; the .map at line 177 never reads benchmark, no break, no tier chrome (the literal 'benchmark' at tsx line 117 is a FunnelMathStep basis-label string, unrelated to this field).
- **`src/lib/lab-engine/skills/positioning-offer-diagnostic/SKILL.md`** — `'Move 4: Channel truth.' block (lines 63). Additive prose rewrite of the Move-4 paragraph; do NOT touch the schema-anchors line (69) or the Tool Contract table.`
  - *Change:* Rewrite Move 4 so the model NEVER asserts a zero-non-branded-demand / demand-creation-is-the-only-lever thesis. Add: 'Characterize each channel's DEMAND reality from whatever demand evidence is actually present in Prepared evidence rows / ResearchInput (intent mix, branded vs commercial CPC, volume) — NOT from the channel's mere existence. Mark per-channel CAC/CPL/conversion as operator_input_required; NEVER invent it. The honest defensible read where branded search dwarfs any single non-branded term and branded CPC is cheaper than commercial CPCs is: a chunk of paid-search spend likely chases branded demand organic already owns. Do NOT claim non-branded demand is zero — the pipeline's Demand section carries real commercial-intent keywords at higher CPC.'
  - *Why:* Kills the latent prose-fabrication path the verifier is structurally blind to (HARD RULE 4). The verifier extracts numeric tokens/quotes, never the strategic inference 'non-branded demand is zero' — so the ONLY guard against that false, deck-incoherent overclaim is deterministic-feeling SKILL.md prose. Forces the honest branded-vs-commercial read instead of a laundered would-be-9 thesis.
- **`src/lib/lab-engine/skills/positioning-offer-diagnostic/SKILL.md`** — `New Iron Law bullet inserted into the 'Iron Laws' list (lines 41-51), AND a new Move-3 sentence on funnel thresholds (line 61).`
  - *Change:* (1) Iron Law: 'Distinguish DEMAND evidence (publicly acquirable: search volume, CPC, intent mix, branded share) from PERFORMANCE evidence (operator-only: CAC, CPL, conversion-by-channel). Demand evidence may be cited from web_search; per-channel performance is operator_input_required and must NEVER be invented or asserted as discovered.' (2) Move 3 funnel sentence: 'When you state a provesWrongIf / funnel threshold, attach a published B2B SaaS conversion benchmark via the optional body.funnelDiagnosis.breaks[].benchmark field, fetched live via web_search (stageLabel, typicalRange, excellentRange, sourceUrl=a real fetched URL). Emit benchmark ONLY when you actually fetched the band — an unfetched band hard-fails the evidence gate as an unsupported numeric.'
  - *Why:* (1) The demand-vs-performance Iron Law is the conceptual backstop for the Move-4 reframe — it tells the model WHY per-channel CAC stays unknown (structurally unacquirable) while demand context is fair game, preventing the model from 'fixing' the all-unknown channel table by inventing performance numbers. (2) The Move-3 sentence is the SKILL-side acceptance condition for the new benchmark schema field: it makes explicit that benchmark is fetched-only, reinforcing the verifier's numeric-claim gate at the prompt level so the field is filled with real bands or left absent — never a plausible-but-unfetched range.
- **`src/lib/lab-engine/skills/positioning-offer-diagnostic/SKILL.md`** — `'Final Check' list (lines 79-86). Append one bullet.`
  - *Change:* Append: '- Did channelTruth give a media buyer something actionable (the branded-vs-commercial demand read), or just mark everything unknown and self-label coverage "adequate"? If every channel is hasWorked=unknown with no performance data, set the channelTruth coverage.readiness to "directional" (not "adequate") and state the operator-data gap plainly.'
  - *Why:* Fixes the over-confident self-label: the committed artifact shows channelTruth.coverage.readiness='adequate' while byTier.hard_evidence=0 — mildly generous and exactly what a media buyer distrusts. readiness is a MODEL-AUTHORED prose field (not deterministic), so the only lever is SKILL.md instruction. Downgrading the self-label to 'directional' makes the honest gap read as honest, the last marginal lift to a solid 8. NO schema change — blockCoverageSchema already permits any readiness string.

**Tests.**
- ADD to src/lib/lab-engine/artifacts/schemas/__tests__/offer-diagnostic.test.ts a new describe('offer-diagnostic benchmark field (additive)'): (1) 'a funnel break WITHOUT benchmark still parses bodySchema and passes validateOfferDiagnosticMinimums' — clone the existing fixture (offerDiagnosticFixtureArtifact), assert .ok===true (proves additive-safe / backward compat). (2) 'a funnel break WITH a valid benchmark {stageLabel,typicalRange,excellentRange,sourceUrl:https://...} parses and passes minimums' — assert offerDiagnosticBodySchema.safeParse succeeds and validateOfferDiagnosticMinimums(rebuilt).ok===true. (3) 'a benchmark with a non-URL sourceUrl FAILS bodySchema parse' — set sourceUrl:'not-a-url', assert offerDiagnosticBodySchema.safeParse(...).success===false (proves the z.string().url() tightening). (4) 'a null benchmark normalizes to undefined' — set benchmark:null, assert parse succeeds and parsed.body.funnelDiagnosis.breaks[0].benchmark===undefined (proves the transform matches the evidenceTier pattern).
- RUN the existing offer-diagnostic.test.ts suite UNCHANGED to prove no regression — all 4 existing describes (T2b evidence-gap escape hatch, T2c structural blockGap, parseOfferDiagnosticStrategicEvidenceGapPath, R1 deadline-exhaustion skeleton) must stay green; the additive optional field touches none of their assertions.
- ADD a claim-extractor regression test in src/lib/lab-engine/agents/verification/__tests__/claim-extractor.test.ts: 'extractClaims surfaces a benchmark.typicalRange percent as a load-bearing numeric claim' — build a body with funnelDiagnosis.breaks[0].benchmark.typicalRange='15-25%', call extractClaims(body), assert the returned claims include a kind:'numeric' claim whose value contains '25%'. This LOCKS IN the self-policing guarantee (proves an unfetched benchmark band cannot ride past the verifier as an unchecked field).
- NO SKILL.md unit test exists or is needed (prose-only); verify the SKILL.md edits via the skill-parity/skill-resolution suite if it loads the file — run section-registry.test.ts only if allowedTools changed (it did NOT here, so it is not required).

**Verify.**
```bash
npx tsc --noEmit
npm run test:run -- src/lib/lab-engine/artifacts/schemas/__tests__/offer-diagnostic.test.ts
npm run test:run -- src/lib/lab-engine/agents/verification/__tests__/claim-extractor.test.ts
npm run lint
node -e "const {offerDiagnosticBodySchema}=require('./src/lib/lab-engine/artifacts/schemas/offer-diagnostic.ts'); console.log('manual: confirm a break with benchmark.sourceUrl=non-url fails parse')" # (illustrative — run the real assertion through the vitest case above; .ts cannot be require'd directly)
```

**Fabrication risks to guard.**
- PRIMARY RISK — INVERTED BY THE BUILD: the candidate spec feared benchmark is a verifier-blind dangling-sourceUrl fabrication surface. VERIFIED FALSE on this codebase: a numeric range in benchmark.typicalRange IS extracted by claim-extractor.ts walkValue (recurses every nested record, line 503-508) as kind:'numeric', and evidence-support.ts defaultLoadBearingKinds (line 53) includes 'numeric' — so an unfetched band becomes an unsupported load-bearing claim and hard-fails when LAB_VERIFIER_MAX_UNSUPPORTED=0 is armed. The z.string().url() tightening (vs the candidate's min(1)) closes the only residual hole: a non-URL placeholder in sourceUrl. Net: benchmark is self-policing, NOT a fabrication surface — but ONLY because the field carries a numeric and the env is armed; if the section is ever run with LAB_VERIFIER_MAX_UNSUPPORTED=Infinity (commit-with-honest-badge), an unfetched band would commit flagged-but-present. State this as the acceptance condition.
- VERIFIER-BLIND PROSE (HARD RULE 4, the real residual risk): the zero-non-branded-demand thesis lives in channelTruth.prose / strategicInsight prose and is NOT extractable as a numeric/quote/url claim — the verifier cannot catch it. The ONLY guard is the SKILL.md Move-4 reframe + demand-vs-performance Iron Law. There is no deterministic backstop; if the model ignores the SKILL prose it could still emit the false thesis and commit clean. A coding agent CANNOT add a deterministic gate for this without new infra (out of scope for honest-8) — flag it as the one unguarded prose path and rely on the SKILL reframe.
- DEAD-SCHEMA FABRICATION (the trap the build AVOIDS): the plan's headline demandSignal {keyword,monthlyVolume,cpc,intentType,brandedShare} field on channelEvidenceSchema is DROPPED. VERIFIED no runtime data source for Offer: the SpyFu/keyword prepass (buildBrandedKeywordPrepass) is gated ONLY to positioningDemandIntent||positioningMarketCategory at run-section.ts:12543-12544 (and the symmetric guard ~13331-13332); Offer gets NO keyword prepass, and the cited demandIntentEvidenceGapArtifact precedent is an OUTBOUND self-degradation failure-envelope, not an inbound evidence pipe. Shipping demandSignal would be inert schema the model can fill ONLY by inventing SpyFu numbers it never fetched — a fabrication invitation. Dropping it is load-bearing.
- SELF-LABEL GENEROSITY (low): if the model leaves channelTruth.coverage.readiness='adequate' despite zero hard_evidence (as the committed artifact does), that is a soft over-claim the deterministic floor does not catch (readiness is free prose in blockCoverageSchema). Mitigated by the Final-Check SKILL.md bullet only — no code gate.

**Shared files:** `src/lib/lab-engine/agents/verification/claim-extractor.ts — READ-ONLY here (we add a test against it, do NOT edit it). VoC/Demand/Competitor build items may also touch claim-extractor field-name sets (entityNameFieldNames, quoteAttributionFieldNames, countFieldNames); if any sibling item EDITS claim-extractor, the Offer benchmark-numeric test (item-3) must be re-run after their change to confirm 'numeric' still extracts from nested benchmark records. Conflict surface = the field-name Sets and walkValue, not the funnel path.`, `src/lib/lab-engine/agents/verification/evidence-support.ts — READ-ONLY here. defaultLoadBearingKinds (line 53) is the contract the benchmark self-policing relies on; if a sibling item changes load-bearing kinds or LAB_VERIFIER_MAX_UNSUPPORTED defaults, re-validate the benchmark fabrication guarantee.`, `src/lib/lab-engine/agents/run-section.ts — READ-ONLY here, but it is the highest-traffic shared file across ALL seven section build items (prepass gates, per-section branches). Offer makes NO edit to it (the demandSignal drop is precisely the decision to NOT add an Offer prepass branch). Any sibling item editing the keyword-prepass gate (lines ~12543/13331) must NOT add Offer to the branch — that would reintroduce the dead-data path Offer deliberately avoids.`, `docs/plans/2026-06-20-seven-section-honest-ceiling-build-plan.md — the §4.5 plan text proposes demandSignal; this spec OVERRIDES it (drop). Flag to the orchestrator so §4.6 Demand / other items do not assume Offer.channelTruth now carries SpyFu rows.`

**Sequencing.** INDEPENDENT — Offer can build in any order relative to the other six; it makes ZERO edits to run-section.ts and reads (does not write) claim-extractor.ts / evidence-support.ts, so it has no hard dependency on the keystone confidence-split or any sibling section. ONE soft ordering note: the benchmark-numeric regression test (test item 3) asserts current claim-extractor behavior; if the keystone or a sibling item EDITS claim-extractor's walkValue or field-name Sets, land Offer's test AFTER that change (or re-run it) so it pins the post-change behavior. Recommend landing Offer LATE in the build order (it is already at honest-8; it is the lowest-risk, lowest-urgency item and serves as the reference the other six are measured against — landing it last avoids churn if a sibling touches the shared verifier files). No section must land before Offer.

**Open questions.**
- Is LAB_VERIFIER_MAX_UNSUPPORTED armed (=0) on the build/harness path? The benchmark self-policing guarantee (unfetched band -> hard-fail) holds ONLY when the verifier hard-fails on unsupported numerics. If the harness runs at the Infinity default (commit-with-honest-badge), an unfetched benchmark band would commit flagged-but-present, not blocked. Confirm the armed env before relying on benchmark as a fabrication guard. (The committed harness-ramp-2e3adf77 run shows needs_review:true with the ACV provenanceFlag surfaced, consistent with armed-but-commit-anyway — verify the intended gate mode for the build.)
- Should benchmark.stageLabel/typicalRange be ADDED to a self-authored-label allowlist (selfAuthoredLabelPaths in claim-extractor.ts) to avoid a stageLabel string being mis-extracted as an entityName claim? Likely NOT needed — entityNameFieldNames does not include 'stageLabel'/'typicalRange' — but confirm no fieldName collision (e.g. a 'name' key) exists in the benchmark object as authored. The proposed field uses stageLabel (not name), so this is a no-op, but flag for the executor to keep the field names exactly as specified.
- Does any sibling build item (VoC / Demand / Competitor) plan to widen claim-extractor field-name Sets in a way that changes how nested benchmark numerics are extracted? If so, the orchestrator should sequence Offer's claim-extractor test after that change. No conflict expected, but the shared-file surface warrants a heads-up.
- The SKILL.md readiness-downgrade instruction ('adequate' -> 'directional' when all channels unknown) relies on the model honoring prose. There is no deterministic enforcement. Acceptable for honest-8, but confirm the team accepts a prose-only guard here rather than a coverage.readiness validator (a validator would be a larger, non-additive change out of scope for this item).


### §4.6 Demand & Intent — target 8 · 🟡 NEEDS-REVISION

**Root cause.** Demand is the ONLY section already at its deterministic ceiling: value-read/facts.json scores it ceiling=9, band="clean", reason="verifier clean (verified 55, 0 unsupported)", zero deck-ledger gate violations (gate.json violations are positioningPaidMediaPlan-only). finalScore=min(LLM 7.5, deterministic 9)=7.5, so the ENTIRE 7.5→target gap is the LLM value-read — which reads committed.body JSON ONLY (zz-value-read.mjs:314-318 `truncate(committed.body, SECTION_BUDGET=7000)`; comment :314 "the prompt deliberately carries ONLY the body + verdict text"). The LLM holds it at 7.5 for exactly two reasons stated in verdict.json: (1) weakestSentence = "'ramp' draws 27,200 monthly searches (SpyFu-estimated) — a single-source, live-tool estimate carrying the section's whole lead" — EVERY quantitative claim is single-source SpyFu with no corroboration (sources=23: 19 spyfu.com + 4 ramp.com); the verifier passes the number because it IS in the fetched page but is blind to whether SpyFu's estimate is accurate (HARD RULE 4 — single-source-estimate fragility the floor cannot test); (2) "a confidence number (0.4) that understates it" — section-level confidence=0.4 + needs_review=true make a claimSupportShare=1.0 rich block read as 40% confident. CRITICAL: the body ALREADY frames the brand/non-branded split honestly (keyFindings[0] carries "27,200... while non-branded category terms total ~1,100/mo combined — a known destination, not a category synonym"; strategicVerdict same) and ALREADY labels CAC "operator-reported" in prose. So the candidate specs' renderer edits (demandKeyFindings "largest demand cluster" reframe, readiness badge, provenance column) are INVISIBLE to the value-read (it never runs React) and cannot move 7.5. The honest target is 8, not 9: reaching 9 requires corroborating SpyFu volumes with a second source, which is a live-tool/data-acquisition change out of scope for this build and forbidden by HARD RULE 1 if faked. The achievable move to 8 is making the section's OWN provenance discipline legible IN THE BODY (the surface the value-read scores): a single SpyFu CPC cited alongside the operator number in the conversion sentence + structural economicsProvenance tags, driven by a SKILL Iron Law — turning the prose-only "operator-reported" label into a structurally-verifiable distinction so the read can confirm the section never launders an operator number as a discovery.

**Edits** (additive-safe flagged):

- **`src/lib/lab-engine/skills/positioning-demand-intent/SKILL.md`** — `## Iron Laws section (lines 45-62). Insert ONE new bullet immediately AFTER the existing bullet 'Do not ship a demand read whose only numbers are echoed from the operator brief...' (the second-to-last Iron Law) and BEFORE 'Lead with `keyFindings`...'. This is a PROSE/instruction edit only — no schema or code.`
  - *Change:* Add Iron Law: 'Single-source provenance must be legible in the body, not just true. (a) In any conversion-math sentence that anchors on an operator figure (CAC, budget, target), state the operator number AND cite at least one tool-measured SpyFu CPC from `body.keywordDemand.keywords` in the SAME sentence, so a reader can see the math fuses operator input with researched demand — e.g. "at the operator-reported $3,000 CAC, the $8.58 corporate-cards CPC (SpyFu) implies low-single-digit conversion." (b) When every keyword volume/CPC is single-source (one tool), say so explicitly in `body.keywordDemand.prose` ("all volumes are SpyFu-estimated and not independently corroborated") rather than leaving the limitation implicit — naming the single-source ceiling is more trustworthy than hiding it. (c) Populate `body.operatorEconomics` with the operator CAC/budget/allocation figures (provenance="operator-brief") so the operator-vs-researched boundary is structural, not prose-only.'
  - *Why:* This is the ONLY edit that lands on the surface the value-read actually scores (committed.body): the SKILL is what makes the writer emit body changes. It directly answers verdict.json's weakestSentence (single-source SpyFu) by forcing the body to NAME the single-source ceiling and to fuse a researched CPC with the operator number — the precise 'honest framing' move that lifted the comparison sections, applied to the one structural distrust driver Demand has. Per plan §4.6 line 306 ('Iron Law: when anchoring conversion math on operator CAC/budget, populate body.operatorEconomics AND cite ≥1 tool-measured SpyFu CPC in the same sentence'). No fabrication: every figure already exists in the corpus/tool results.
- **`src/lib/lab-engine/artifacts/schemas/demand-intent.ts`** — `Add a module const tuple alongside intentTypes/questionSurfaces/etc. (lines 23-44), e.g. after `const venueTypes = [...] as const;` (line 44): `const economicsProvenanceTypes = ['tool-measured','operator-brief','model-estimated'] as const;`. Then add the body field: in demandIntentBodySchema (lines 127-174), insert ONE optional `operatorEconomics` property AFTER the `venueMap: {...}.strict(),` block (closes at line 172) and BEFORE the body's closing `})
  .strict();` (line 173-174).`
  - *Change:* Insert: `operatorEconomics: z.object({ targetCac: z.string().min(1).nullable().transform((v)=>v??undefined).optional(), monthlyBudget: z.string().min(1).nullable().transform((v)=>v??undefined).optional(), googleAllocation: z.string().min(1).nullable().transform((v)=>v??undefined).optional(), provenance: z.literal('operator-brief') }).strict().nullable().transform((v)=>v??undefined).optional(),`. STRICT-SAFE: `.nullable().optional()` inside the body's `.strict()` is additive — strict() rejects UNKNOWN keys, never a declared-but-absent optional; the frozen positioningDemandIntent.json (no operatorEconomics key) and every prior committed Demand artifact still validate.
  - *Why:* Structurally tags the $3,000 CAC / $25K budget / 60% Google split as operator-brief, so the value-read can confirm the section never launders an operator number as a researched fact (verdict.json secondary distrust driver: these anchors 'are harness-brief, not corpus, correctly labeled... but the only economics anchor'). VERIFIED SAFE against evidence-support.ts: the numbers $3,000/$25,000 verify CLEAN today (committed verdict: verified 55, 0 unsupported) via `briefMoneyDigits` (evidence-support.ts:925-957) — a digit-normal index of operator figures; `tokenAppearsInBriefEconomics` exempts by VALUE not by path, so duplicating these exact numbers into a new field stays exempt and does NOT add unsupported claims. The `provenance: z.literal('operator-brief')` enum string is label-like and carries no sourceUrl, so claim-extractor.ts emits no new numeric/url/attribution claim from it. Inert WITHOUT the SKILL edit above — ship them together.
- **`src/lib/lab-engine/artifacts/schemas/demand-intent.ts`** — `keywordSignalSchema (lines 65-81), the `.object({...})` before `.strict()` (line 81). Insert ONE optional field AFTER `verification: rowVerificationFieldSchema,` (line 79) and before the closing `})
  .strict();`. Reuse the economicsProvenanceTypes tuple defined in the prior edit.`
  - *Change:* Add `economicsProvenance: z.enum(economicsProvenanceTypes).nullable().transform((v)=>v??undefined).optional(),` to keywordSignalSchema. STRICT-SAFE (same reasoning as evidenceTier at line 78, which uses the identical .nullable().optional() pattern inside this strict object). Per plan §4.6: badge each keyword row's number source — all 19 committed Ramp rows are tool-measured (SpyFu).
  - *Why:* Lets a row carry its number-provenance structurally (19/19 = tool-measured) so the body JSON the value-read reads makes the tool-measured vs operator-brief distinction explicit at row level, reinforcing the SKILL's single-source-legibility law. SAFE: enum value is a short label token, not a number/url/multi-token entity — claim-extractor.ts:255 only pushes entityName claims for non-label paths, and an enum string like 'tool-measured' adds no numeric/url claim, so the clean 55/0 verdict is preserved. Optional → the frozen artifact (no economicsProvenance key) validates unchanged.
- **`src/components/research-v2/section-renderers/demand-intent.tsx`** — `demandKeyFindings() at lines 119-137: line 120-122 `const topKeyword = [...artifact.keywordDemand.keywords].sort(...)` sorts ALL keywords by raw monthlyVolumeValue and grabs `ramp` (27,200, intentType=navigational); line 131 emits sentence `${topKeyword.keyword} is the largest visible demand cluster at ...`.`
  - *Change:* Filter the topKeyword selection to `intentType !== 'navigational'` BEFORE the volume sort, so the rendered lead finding names the largest CATEGORY/commercial demand cluster (not the branded `ramp` navigational term). If the filtered set is empty, fall back to the current behavior. This is RENDERER-PARITY ONLY: it fixes a UI-correctness bug where the slide mislabels the branded numerator as a 'demand cluster', matching the rendered finding to the BODY (keyFindings[0] already correctly frames ramp as 'a known destination, not a category synonym').
  - *Why:* HONEST SCOPE CORRECTION vs the candidate specs: this does NOT move the 7.5 (value-read reads committed.body JSON, never this React function — zz-value-read.mjs:318). It is a legitimate human-facing slide-correctness fix so the rendered key finding stops contradicting the body's own correct framing. Ship it as polish, NOT as a value-read mover. Do NOT invent the ~1,100 figure in the renderer — it is already in keyFindings[0] from the body; the renderer must read it, never compute it.

**Tests.**
- src/lib/lab-engine/artifacts/schemas/__tests__/demand-intent.test.ts — ADD to the existing `describe('keywordSignalSchema — optional cpc')` block (lines 587-625): a test 'accepts a row WITH economicsProvenance' asserting `keywordSignalSchema.safeParse({...baseRow, economicsProvenance: 'tool-measured'}).success === true`, and 'rejects an unknown economicsProvenance enum value' asserting `economicsProvenance: 'guessed'` → success === false (proves strict enum). Mirror the baseRow fixture already defined at lines 588-596.
- src/lib/lab-engine/artifacts/schemas/__tests__/demand-intent.test.ts — ADD a new `describe('demandIntentBodySchema — optional operatorEconomics')`: (1) 'accepts a body WITHOUT operatorEconomics' using demandIntentFixtureArtifact.body unchanged → success === true (proves additive/back-compat with the frozen shape); (2) 'accepts a body WITH operatorEconomics' spreading `operatorEconomics: { targetCac: '$3,000', monthlyBudget: '$25,000/mo', googleAllocation: '60%', provenance: 'operator-brief' }` → success === true; (3) 'rejects operatorEconomics with provenance other than operator-brief' → success === false (proves z.literal). Use the existing demandIntentFixtureArtifact import already at the top of the file.
- src/components/research-v2/section-renderers/__tests__/demand-intent.test.tsx — UPDATE/ADD a render assertion that demandKeyFindings no longer labels a `navigational` keyword as the 'largest visible demand cluster': construct a keywords array containing a high-volume navigational row + a lower-volume commercial row and assert the rendered lead finding names the COMMERCIAL keyword. Verify the existing fixture (demand-intent fixture) still renders without throwing.
- No new test for operatorEconomics numeric-exemption is needed — it is covered by the existing live-verified verifier behavior (briefMoneyDigits is value-based). Do NOT add a mocked verifier test asserting exemption; per learned-patterns, mocked verifier tests give false confidence on briefMoneyDigits — the live harness rerun is the real gate.

**Verify.**
```bash
npx tsc --noEmit (must stay at the known pre-existing baseline — additive optional fields add 0 new errors)
npm run test:run -- src/lib/lab-engine/artifacts/schemas/__tests__/demand-intent.test.ts
npm run test:run -- src/components/research-v2/section-renderers/__tests__/demand-intent.test.tsx
npm run test:run -- src/lib/lab-engine/agents/verification/__tests__/claim-extractor.test.ts (confirms the new optional fields do not change claim extraction over the body)
node scripts/zz-value-read.mjs --gate over a fresh Demand artifact AFTER a solo production-path rerun (/api/research-v2/rerun-section positioningDemandIntent) — REQUIRED: this is the only gate that proves the body changes (operatorEconomics populated + SpyFu-CPC-in-conversion-sentence + single-source-ceiling prose) actually lifted the LLM read toward 8 AND that verified/unsupported stays 55/0 (no regression from clean band 9). Renderer/schema unit tests CANNOT prove the score move.
```

**Fabrication risks to guard.**
- HARD RULE 1 / 4 — the ~1,100 non-branded total and 27,200 branded figure must come from the BODY (already present in keyFindings[0]/strategicVerdict and the 19 SpyFu rows). The renderer edit must READ them, never recompute or invent. The SKILL must never instruct the writer to manufacture a SECOND corroborating source for SpyFu volumes — single-source is the honest ceiling; the law is to NAME the single-source limitation, not paper over it. Reaching 9 by claiming corroboration that doesn't exist is the exact laundering this build forbids.
- operatorEconomics must ONLY ever hold figures supplied in the operator brief ($3,000 CAC, $25K budget, 60% Google). If the writer ever puts a researched/estimated number there with provenance='operator-brief', it both mislabels provenance AND could escape the briefMoneyDigits exemption (if the digits aren't in the brief set) → becomes an unsupported claim that drops the clean 55/0 verdict. The z.literal('operator-brief') makes mislabeling structurally loud but cannot stop a number that isn't actually in the brief — the SKILL law must be explicit that ONLY brief figures go here.
- The SKILL's 'cite a SpyFu CPC in the conversion sentence' law could tempt the writer to cite a CPC for a keyword that wasn't actually measured. Mitigated because the law says cite FROM body.keywordDemand.keywords (already tool-measured rows) — but the value-read floor is blind to whether the cited CPC matches the right keyword (prose attribution), so a wrong-keyword CPC pairing would pass the verifier. Flag in review.
- The value-read is prose-sensitive and the deterministic floor cannot verify prose claims about 'single-source' or 'not independently corroborated' — a writer could OVER-hedge (the VoC failure mode: laundering real data into apology). The SKILL law must say NAME the single-source ceiling in ONE clause, then keep leading with the strong evidence — not turn the section into a hedge. Over-correction here would LOWER the 7.5, not raise it.

**Shared files:** `src/lib/lab-engine/agents/verification/claim-extractor.ts — READ-ONLY here (verified walkValue is index-less, fieldPath=`${fieldPath}.${key}` for objects with NO array index; arrays recurse keeping the same fieldPath). Other build items (Market §4.3 fieldPath .N. bug, BuyerICP) may EDIT this file — Demand only depends on its current behavior, so a concurrent edit that changes numeric/entity extraction could regress Demand's clean 55/0. Coordinate: re-run zz-value-read on Demand after any claim-extractor change.`, `src/lib/lab-engine/agents/verification/evidence-support.ts — READ-ONLY here (depends on briefMoneyDigits value-based exemption staying intact). Paid Media §4.7 (deck-ledger token-not-in-ledger) and Market touch the verifier/gate path; a change to the money-provenance exemption sets would affect Demand's operatorEconomics safety.`, `src/lib/lab-engine/artifacts/schemas/strategic-insight.ts — READ-ONLY (Demand imports keyFindingsSchema, strategicInsightSchema, blockCoverageSchema, evidenceBlockGapFieldSchema, etc.). Shared across ALL positioning sections — if another build item edits these shared schemas, every section schema test (incl Demand) must re-run.`, `scripts/zz-value-read.mjs — the shared scoring harness/gate used by ALL 7 sections' verify step. No edits proposed; conflict only if another item changes SECTION_BUDGET or the prompt shape.`, `src/lib/lab-engine/fixtures/demand-intent-artifact.ts — the fixture the renderer + schema tests consume; if the fixture is regenerated for another reason it must keep operatorEconomics ABSENT to preserve the back-compat test.`

**Sequencing.** Build order #6 of 7 (after Competitor §4.4, VoC §4.5, Market §4.3, BuyerICP §4.1/§4.2, Offer; BEFORE Paid Media §4.7). RATIONALE: Paid Media (#7) consumes Demand's keyword/CPC rows via the cross-section binder — Demand's keyword + economicsProvenance data must be FINAL before Paid Media's audience/angle rows bind to them, or Paid Media re-binds to stale rows. WITHIN this item: (1) land the two schema edits (operatorEconomics + economicsProvenance) FIRST so the fields exist; (2) then the SKILL edit (depends on the fields being declared so the writer can populate them); (3) the renderer parity edit is independent and can land anytime. The mandatory proof step (zz-value-read after a solo rerun) must run LAST and is the only thing that proves the 7.5→8 move — do not mark ready on unit tests alone.

**Open questions.**
- HONEST CEILING DISPUTE: plan §4.6 asserts achievable9=true via 'display + provenance + skill reframe, NO gate loosening'. Ground truth contradicts the MECHANISM: the value-read reads committed.body JSON (not the renderer), and the body ALREADY frames the brand/non-branded split and labels CAC 'operator-reported'. The LLM's stated cap reasons (single-source SpyFu + understated 0.4 confidence) are NOT addressable by the proposed edits without either (a) corroborating SpyFu volumes with a second data source (out of scope, data-acquisition) or (b) changing how the section-level confidence is computed/displayed in the BODY (confidence=0.4 lives in the artifact envelope, not the body the value-read scores — does the value-read even see it?). I set honestTarget=8, not 9. NEEDS A DECISION: is an honest 8 (legible single-source provenance, no laundering) acceptable, or does the build owner want to invest in second-source keyword corroboration (the only path to a defensible 9)?
- Does the value-read prompt include the section-level confidence/needs_review, or ONLY committed.body? zz-value-read.mjs:318 truncates committed.body; verdict.json cites '0.4 confidence' in its read, implying the LLM DOES see confidence somewhere (possibly via the facts.json sections summary in the same prompt). If the 0.4 is NOT in the value-read input, the 'confidence understates it' complaint is unfixable from this section and the honest ceiling is firmly 8. CONFIRM by reading the full prompt.txt the harness emitted (tmp/zz-full-run/harness-ramp-2e3adf77/value-read/prompt.txt).
- Will populating operatorEconomics + adding a SpyFu CPC to the conversion sentence actually move the LLM read, or is it below the value-read's resolution? The single-source-SpyFu complaint is fundamental (it's true), so the realistic lift is 7.5→8 (clearing the 'understated/under-legible provenance' half of the gap), not 7.5→9. The solo-rerun zz-value-read is the only way to know — treat the numeric target as a hypothesis to validate, not a guarantee.
- Confirm decode-fallbacks.ts:48 ('demandVenues.venues[].venueType') is a documentation enum-inventory table comment, not executable code that would need updating for the new fields. Initial read shows it is a markdown-style table; verify it is not parsed at runtime before asserting the new fields need no decode-fallback entry.


### §4.7 Paid Media Plan — target 8 · 🟡 NEEDS-REVISION

**Root cause.** PaidMedia COMMITTED a best-in-class strategy (58KB, 14 body arrays, correct two-track capture+create thesis) but is the ONLY deck section hit with the hard fabrication-cap-at-4. value-read/facts.json: ceiling=4, band="fabrication-cap", reason="proven laundering: 2 deck-ledger gate violation(s)", llmScore=5 (matches human read). The cap (NOT thinness) is the dominant blocker — finalScore=min(llm,ceiling)=4, and no binder/bind-rate work lifts it while the 2 violations stand. TWO independent failure loci, both ground-truthed:

LOCUS A — THE CAP (gate.json: both violations are audienceTypes rowIndex 0, reason token-not-in-ledger-quote, sourceUrl https://ramp.com/). audienceTypes[0] is the only grounded audience; its 3 refs (verified in committed body) are BuyerICP firmographicCuts[0], firmographicCuts[1], personaReality.personas[0]. Dereferenced through deck-ledger-gate.ts: cut[0] value 'Technology, ecommerce, professional services' -> claimTokenFromQuote takes first whitespace token 'Technology,' (capital-T + comma); the ledger has 19 facts at ramp.com/ containing 'technology, ecommerce' (LOWERCASE) but 0 containing 'Technology,' because quoteContainsTokenCleanly does a CASE-SENSITIVE quote.includes(token) (deck-ledger-gate.ts:184, no toLowerCase) -> violation 1 is a CASE-SENSITIVITY FALSE POSITIVE (the data IS grounded). cut[1] value '10-2,000 employees (approximate, ... no precise floor/ceiling verified)' -> claimToken '10-2,000' -> 0 ledger backing (case-insensitive too: 0 hits for '10','2,000','2000') -> violation 2 is a GENUINE un-grounded inferred band. personas[0] (about-us, token 'Ramp') is CLEAN. So: 1 gate false-positive + 1 real un-grounded inference, BOTH inherited from BuyerICP's firmographicCuts authoring.

LOCUS B — BIND RATE (caps the LLM read at 5, not the ceiling). looksLikeNamedRecords (paid-media-evidence-pack.ts:410) requires entry.name OR entry.competitor; DemandIntent keywordDemand.keywords rows key on 'keyword' (verified: keys keyword/monthlyVolume/cpc/intentType, no name) so genericArrayCandidates('positioningDemandIntent','keyword') never enumerates them -> 10 of the synthesized rows that cite positioningDemandIntent (anglesToTest x3, audienceTypes x2, channelSuggestions x2, creativeFramework x3) fall to status=gap with empty refs. A media buyer cannot trace an audience to a keyword. Confirmed: of all DemandIntent-cited rows, 100% are status=gap.

LOCUS C (RECON over-read, the plan's '17 stripped numerics = derived-stamp' premise) — FALSE on current HEAD. reconcilePaidMediaBudgetCascade (paid-media-plan.ts:2550) ALREADY stamps dailySpendProvenance:'derived' (:2568) and monthlyBudgetProvenance:'derived' (:2582); trustedPaidMediaMoneyProvenances (evidence-support.ts:420) ALREADY exempts 'derived' from redactPaidMediaMoneyFields. 'derived' is ALREADY in paidMediaMoneyProvenanceValues (:33) and the provenance field is z.string().min(1) (open, not an enum). The committed deck's null/unknown daily-budget values are STALE artifacts of the 2e3adf77 run, NOT a live code gap. No money-provenance code fix is warranted.

**Edits** (additive-safe flagged):

- **`/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/agents/paid-media-evidence-pack.ts`** — `looksLikeNamedRecords, the .every predicate at lines 414-422 (verified: function starts line 410; predicate is value.every(entry => isRecord(entry) && (typeof entry.name === 'string' || typeof entry.competitor === 'string')))`
  - *Change:* LOCUS B CORE FIX (~80% of bind-rate gap). Widen the per-entry predicate to ALSO accept rows keyed on keyword or term: add `|| typeof entry.keyword === 'string' || typeof entry.term === 'string'` to the inner OR. This admits DemandIntent keywordDemand.keywords[] via the EXISTING genericArrayCandidates nested path (keywordDemand is a record -> its child keywords array now passes looksLikeNamedRecords; genericRowCandidate anchors on 'keyword' which is ALREADY in identityFields :530; locator emits body.keywordDemand.keywords[N] WITH the [N] index that deck-ledger-gate dereferenceLocator REQUIRES). DO NOT add a bespoke demandIntentCandidates enumerator or change the positioningDemandIntent case at :579 — that path already passes evidenceKind='keyword' and the widen alone enumerates the rows (adversarial vetting flagged the bespoke enumerator as redundant + double-enumeration risk). KEEP the overlap>=2 floor at :668 unchanged (false-bind guard). Side-effect audit: widening only ADMITS additional arrays, never removes a competitor/persona row; competitorLandscapeCandidates rows still match via name/competitor; no other section's body has a keyword/term-keyed array that should be excluded.
  - *Why:* Flips ~10 DemandIntent-cited rows (anglesToTest x3, audienceTypes x2, channelSuggestions x2, creativeFramework x3) from status=gap/empty-refs to grounded with a real array-indexed locator + verbatim keyword+volume+CPC excerpt the gate can back against the ledger. This is the single largest source of the hollow bind rate and the direct lever on the LLM value-read score (5 -> 8).
- **`/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/agents/paid-media-evidence-pack.ts`** — `buyerICPCandidates, firmographicCuts loop at lines 274-289 (verified: arrayAt(icpExistenceCheck,'firmographicCuts').forEach((cut,index) => ... const value = asString(cut.value); out.push(makeCandidate('firmographicCut', `body.icpExistenceCheck.firmographicCuts[${index}]`, joinText([cut.cutType,cut.value]), value)))`
  - *Change:* LOCUS A PaidMedia-side guard (clears violation 2). Inside the forEach, after `const value = asString(cut.value)`, SKIP (return; do not push) when the value carries an inference disclaimer, gated on a deterministic case-insensitive marker set: ['approximate','no precise','not verified','inferred','directional','sourced from','estimated','no floor','no ceiling']. Add a small local const INFERENCE_DISCLAIMER_MARKERS = [...] and `if (INFERENCE_DISCLAIMER_MARKERS.some(m => value.toLowerCase().includes(m))) { return; }`. Keep grounded cuts (cut[0] industry, cut[2] geography) enumerable. This is purely SUBTRACTIVE of candidates that would produce un-ledger-backable refs; it never invents a candidate, never changes a grounded row.
  - *Why:* firmographicCuts[1] ('10-2,000 employees (approximate ... no precise floor/ceiling verified)') is a genuine un-grounded inferred band with 0 ledger backing; binding audienceTypes[0] to it produces deck-ledger violation 2. Refusing to enumerate inference-disclaimed cuts removes that ref so the audience binds only to its real grounded cut[0] + persona[0]. This is the ONLY violation §4.7 can clear unilaterally; violation 1 (cut[0] case false-positive) MUST be fixed upstream in §4.4 (see sequencing) and CANNOT be fixed here without relaxing the gate (HARD RULE 1).
- **`/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/artifacts/schemas/paid-media-plan.ts`** — `section body object schema (add a new top-level optional field alongside the existing body fields; verified the schema uses z.string().min(1) open provenance fields and a .strict() body, so the new key must be a known-optional key)`
  - *Change:* Add additive optional rollup field evidenceBinding: z.object({ groundedRows: z.number(), gapRows: z.number(), bindRate: z.number(), byTier: z.record(z.string(), z.number()).optional() }).nullable().transform(v => v ?? undefined).optional(). Populate it DETERMINISTICALLY inside withPaidMediaEvidencePack (paid-media-evidence-pack.ts :718) AFTER the row-binding map completes: count grounded vs gap evidencePack statuses across SYNTHESIZED_ROW_ARRAYS, bindRate = groundedRows/(groundedRows+gapRows). Ship the schema field + the decode-fallbacks.ts inventory entry (if one exists for paid-media optional fields) in the SAME change so structural/contract tests do not break.
  - *Why:* Gives the renderer a Plan Provenance strip ('N of M rows traced') and makes the bind-rate improvement legible without tier chrome on individual rows. Additive optional; frozen artifacts re-parse cleanly (.strict() rejects only UNKNOWN keys, a new known-optional key is fine).
- **`/Users/ammar/Dev-Projects/AI-GOS/src/components/research-v2/section-renderers/paid-media-plan.tsx`** — `existing evidenceGapCell renderer at :391 (per plan) + the section header region; verify the file path and the evidenceGapCell symbol with grep before editing`
  - *Change:* Additive renderer-only: (1) render a Plan Provenance strip from body.evidenceBinding when present ('X of Y rows traced; Z gaps') with a red banner when bindRate < 0.5; (2) competitorMarketingInsights/competitorReviewInsights rows whose evidencePack.status=gap AND whose sourceSection body is absent this run render as ONE amber GapNote ('Competitor section did not commit this run') instead of hollow rows. NO tier chrome on individual data cells. Guard every new read with optional chaining so a frozen artifact lacking evidenceBinding renders unchanged.
  - *Why:* Surfaces the bind-rate lift and degrades the competitor-sourced rows honestly (the plan's gap-degradation requirement). Renderer-only, no schema/floor coupling; old artifacts render identically because evidenceBinding is optional.

**Tests.**
- UPDATE /Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/agents/__tests__/paid-media-evidence-pack.test.ts: add a case proving a synthesized row citing positioningDemandIntent with a salient text overlapping a real keywordDemand.keywords[N] row (>=2 shared meaningful tokens, e.g. the keyword string + volume) now gets evidencePack.status='grounded' with refs[0].locator === 'body.keywordDemand.keywords[N]' and a verbatim excerpt containing the keyword. Use an inline DemandIntent committedArtifacts body shaped like the frozen one (keywordDemand.keywords[] keyed on keyword/monthlyVolume/cpc/intentType).
- UPDATE /Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/agents/__tests__/paid-media-evidence-pack-overlap.test.ts (10.7KB, exists): add a NEGATIVE case proving an audienceTypes row citing positioningBuyerICP whose only candidate match would be firmographicCuts[1] (value carries 'approximate ... no precise floor/ceiling verified') gets status='gap' (the inference-disclaimer guard skipped that candidate), while a row matching firmographicCuts[0] (clean industry value) still binds grounded. Keep the existing TEST 2 (single-incidental-token must stay gap) green — assert the overlap>=2 floor is unchanged.
- ADD a unit test (new file or extend the binder test) proving looksLikeNamedRecords now returns true for an array of rows keyed on keyword (no name/competitor) and on term, AND still true for name/competitor rows, AND false for rows with none of {name,competitor,keyword,term} — pins the widen and prevents a future over-widen.
- UPDATE /Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/verification/__tests__/deck-ledger-gate.test.ts: add a fixture proving a PaidMedia audienceTypes[0] cell whose refs dereference ONLY to a grounded firmographicCut (clean ledger-backed token) + a clean persona yields blocked=false (regression guard that the inference-disclaimer guard removed the un-backable ref). This documents the cap-clear at the gate level. Do NOT add a case-insensitive gate test (HARD RULE 1 — gate stays case-sensitive; the cut[0] false-positive is fixed in §4.4, not here).
- UPDATE the schema test under /Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/artifacts/schemas/__tests__/ (paid-media-plan schema test) to assert: (a) a frozen-shaped body WITHOUT evidenceBinding still parses (additive-safe), (b) a body WITH a valid evidenceBinding parses, (c) an unknown key still rejects (.strict() intact).
- UPDATE the renderer test /Users/ammar/Dev-Projects/AI-GOS/src/components/research-v2/section-renderers/__tests__/paid-media-plan.test.tsx (verify exact filename) to assert the Plan Provenance strip renders from evidenceBinding, the red banner appears when bindRate<0.5, competitor-sourced gap rows collapse to one amber GapNote, and a body lacking evidenceBinding renders unchanged.

**Verify.**
```bash
npx tsc --noEmit 2>&1 | grep -c 'error TS' (expect 0; baseline is clean — do not regress)
npm run test:run -- src/lib/lab-engine/agents/__tests__/paid-media-evidence-pack.test.ts src/lib/lab-engine/agents/__tests__/paid-media-evidence-pack-overlap.test.ts
npm run test:run -- src/lib/lab-engine/verification/__tests__/deck-ledger-gate.test.ts src/lib/lab-engine/verification/__tests__/deck-ledger-gate.fixture.test.ts
npm run test:run -- src/lib/lab-engine/artifacts/schemas/__tests__/ (paid-media-plan schema test)
npm run test:run -- src/components/research-v2/section-renderers/__tests__/paid-media-plan.test.tsx
npm run test:run -- src/app/api/research-v2/run-lab-section/__tests__/route.test.ts (MANDATORY: paid-media-evidence-pack + run-section changes are src/lib AND wired through src/app — the Phase 0 hidden-regression lesson)
npm run test:run (FULL ~2754-test suite must be green before commit)
AFTER all green: re-run the harness ONCE (~$1, abort condition mandatory, NO loops) and confirm in the fresh tmp/zz-full-run/<run>/: (1) gate.json has 0 violations for positioningPaidMediaPlan audienceTypes (or only the cut[0] case-FP if §4.4 not yet landed — see sequencing), (2) value-read/facts.json for positioningPaidMediaPlan shows ceiling > 4 / band != fabrication-cap, (3) the DemandIntent-cited rows now show evidencePack.status='grounded', (4) verifierSummary.strippedNumericClaims no longer contains provenance-unknown daily-budget strips (confirms LOCUS C was stale, not a live gap).
```

**Fabrication risks to guard.**
- The deterministic value-read floor is BLIND to the crossSectionInsight spend-thesis PROSE (the 27,200 vs ~1,100 tension, the $4,200 vs $3,000 CAC inversion) — those carry numbers the verifier checks, but the strategic narrative around them is plausible-reading prose the floor cannot verify. The honest-8 read depends on the LLM value-read catching any drift here; keep a human spot-check on the spend thesis. (HARD RULE 4.)
- The binder fix makes a row 'grounded' on a >=2-token overlap with a keyword row — a media buyer could over-trust an audience that shares 2 incidental tokens with an unrelated keyword. The overlap>=2 floor is the only guard; do NOT lower it. The excerpt (keyword+volume+CPC) must render so a human can eyeball the bind.
- DemandIntent's keywordDemand volumes/CPC are themselves live-tool-fetched (SpyFu), NOT in the frozen corpus — the ledger holds no keyword_volume facts (per the plan's PHASE 0 cross-cutting note). So a DemandIntent-cited row that binds to keywordDemand.keywords[N] is grounded in the SECTION ARTIFACT but the underlying number is live-tool-fragile. This is acceptable for PaidMedia bind-rate (it binds to a real upstream row), but do NOT promote the keyword numbers as corpus-grounded in any prose.
- The inference-disclaimer marker set (LOCUS A guard) is a heuristic — a firmographicCut authored WITHOUT the disclaimer words but still inferred would slip through. The real fix for un-grounded cuts is upstream in §4.4 (blockGap inferred bands at authoring); the PaidMedia guard is defense-in-depth, not the primary cure.
- Competitor section did NOT commit this run; the renderer collapses competitor-sourced rows to amber GapNotes. NEVER let the model assert competitor ad spend/messaging off a missing section — the gap-degradation path must fire, not synthesize.

**Shared files:** `/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/agents/paid-media-evidence-pack.ts — also the home of the buyerICPCandidates enumerator that §4.4 BuyerICP work may touch; coordinate the firmographicCuts loop edit (LOCUS A guard) with §4.4's authoring change so they don't conflict.`, `/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/verification/deck-ledger-gate.ts — read-only here (NOT edited); §4.1 Competitor and any gate change must NOT relax case-sensitivity (HARD RULE 1). Shared conflict surface = the gate test file.`, `/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/verification/__tests__/deck-ledger-gate.test.ts — touched by this item (new PaidMedia cap-clear fixture) and potentially by §4.1/§4.2 (VoC re-attribution gate cases). Merge-coordinate the fixtures.`, `/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/agents/run-section.ts — the plan's §4.7 lists a VoC re-attribution post-processor here; if that sub-edit is taken it shares run-section.ts with §4.2 VoC and §4.4 BuyerICP. This spec defers that sub-edit (see open questions) to avoid the shared-file conflict; the core cap+bind fix does NOT need run-section.ts.`, `/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/artifacts/schemas/paid-media-plan.ts — only this item edits it (evidenceBinding field); low conflict risk.`, `/Users/ammar/Dev-Projects/AI-GOS/src/lib/lab-engine/artifacts/decode-fallbacks.ts — if a paid-media optional-field inventory lives here, the evidenceBinding field must be added; shared with any other section adding optional fields (Market/VoC/BuyerICP all add optional fields in this program).`

**Sequencing.** "BUILD LAST (per plan §4.7). HARD DEPENDENCY on §4.4 BuyerICP for a full cap-clear: this spec's LOCUS A guard removes deck-ledger violation 2 (cut[1] inferred band) but CANNOT remove violation 1 (cut[0] 'Technology,' case false-positive) without relaxing the gate, which HARD RULE 1 forbids. Violation 1 must be cleared upstream in §4.4 by grounding firmographicCuts[0] to a verbatim ledger token (the ledger HAS 'technology, ecommerce' at ramp.com/ — author the cut value to match the captured span exactly, OR have §4.4 blockGap it). THEREFORE: land §4.4 BuyerICP firmographic grounding BEFORE the final PaidMedia harness re-run, or the fabrication-cap stays at 4 and the section cannot reach 8. The LOCUS B bind-rate fix and the evidenceBinding rollup are INDEPENDENT of §4.4 and can land first (they raise the LLM read but the ceiling stays 4 until both violations clear). Also depends on §4.2 VoC committing (the competitorReviewInsights re-attribution path references VoC painLanguage) IF that optional sub-edit is taken. Demand §4.6 is upstream of the keyword binder but already commits cleanly, so no dependency there."

**Open questions.**
- VoC re-attribution post-processor (plan §4.7 lists a run-section.ts edit setting sourceSection=positioningVoiceOfCustomer on competitorReviewInsights rows whose complaint token-overlaps a VoC painLanguage quote): INCLUDE or DEFER? It shares run-section.ts with §4.2 and §4.4 (conflict surface) and only converts 2 'unattributed' competitorReviewInsights rows. Recommend DEFER to a follow-up unless §4.2 VoC lands first and exposes a clean painLanguage.quotes[N] locator the binder can target — the core cap+bind fix does not need it. Decide based on §4.2 landing order.
- Does decode-fallbacks.ts carry a paid-media optional-field inventory that the new evidenceBinding field must be registered in? Grep before editing the schema; if yes, ship the inventory entry in the SAME change or the structural/contract test breaks (cross-cutting risk in plan §7).
- Confirm the exact renderer test filename and the evidenceGapCell symbol at paid-media-plan.tsx:391 before writing renderer edits — the plan asserts :391 but verify with grep (sketched line numbers drift).
- After §4.4 lands, re-confirm via fresh harness that BOTH violations clear (cut[0] grounded + cut[1] skipped) and the value-read ceiling rises above 4 — the cap-clear is only proven by a clean gate.json on a fresh run, not by offline reasoning. If §4.4 grounds cut[0] but the LOCUS A guard accidentally also skips it (if §4.4 leaves a disclaimer word in the value), the audience loses its grounding entirely — coordinate the exact cut[0] value string between §4.4 and this guard's marker set.
- The keyword binder grounds rows against keywordDemand.keywords[N], but those SpyFu numbers are live-tool-fetched, not in the frozen ledger (no keyword_volume facts). Confirm the deck-ledger gate does NOT then flag these newly-grounded DemandIntent-cited cells as token-not-in-ledger-quote (the gate backs against the LEDGER, and the keyword excerpt token may not be a ledger fact). If it does, the bind fix trades a 'gap' for a NEW gate violation — must verify on fresh harness that the keyword cells either resolve to a ledger-backed token or are correctly treated as section-internal (not deck-ledger-gated). THIS IS THE SINGLE HIGHEST-RISK UNKNOWN and the reason readiness is needs-revision.
