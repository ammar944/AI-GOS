# GLM Agentic Research → Media-Plan: Live Wire-Up Execution Plan

> Distilled 2026-06-24 from a 7-subsystem read-only recon (orchestrator-tier, composer-media-plan, agentic-section-flag, fact-ledger, concurrency-serverless, frontend-research-v3, ad-engine-spyfu). Every file:line below is anchored to a map that read the source in full this session. Items the maps did not establish are marked **unverified** — read them before editing.
>
> Two verified anchor corrections the synthesis confirmed at source this session:
> - The supabase client threaded into dispatch is `createAdminClient()` (service-role) — confirmed `run-lab-section/route.ts:97/504`. The cross-process `research_facts` SELECT will NOT be blocked by RLS (migration grants SELECT to service_role). Map 4's RLS worry is **resolved**.
> - The keystone read-path bug is `lab-section-dispatch.ts:138` `prepareSectionContext(..., { store })` omitting `factStore`/`parentAuditRunId`, even though both ARE threaded into the deferred job at `:162-163`. The bug is real but narrower than the maps implied.

---

## Current state

What works end-to-end today: `/research-v3` welcome → onboarding (user-filled from blank) → onboarding-submit auto-fires `POST /api/research-v2/orchestrate`, which seeds 6 positioning `section_run_id`s + a `parent_audit_run_id` (`seed_orchestration` RPC), freezes the brief snapshot, and fans out 6 sections over an HTTP-kickoff pool to `/run-lab-section`, each running its real work inside Next.js `after()`; sections commit as typed cards in `AuditReaderShell` (status via 2500ms poll on `/audit-state`, draft deltas via Supabase Realtime `section-partials:${runId}`); on the 6th commit the 7th "section" `positioningPaidMediaPlan` auto-dispatches via the DeepSeek answer-tool path and renders the 13-block deck automatically. **Built-but-completely-unwired (zero non-test callers, confirmed by grep):** the GLM **orchestrator** (`orchestrator-glm.ts`), the GLM **composer** (`composer-glm.ts`), and the **deck-ledger-gate**. The agentic-GLM *section* path IS dispatch-wired (`run-section.ts:15219` → `runAgenticGLMSection`) but gated OFF by the unset `LAB_AGENTIC_GLM_SECTIONS` env flag, and would silently fall back to DeepSeek in prod anyway because (a) GLM defaults to localhost Ollama unless OpenRouter env is set and (b) the shared `research_facts` ledger is **write-only in practice** — `getFacts()` returns an in-process echo array, never a DB SELECT, and the dispatch seam never passes `factStore` to `prepareSectionContext` (`lab-section-dispatch.ts:138`). `dispatchBounded(3)` bounds only HTTP kickoffs, not the 6 GLM tool-loops that run unbounded across 6 lambdas.

---

## Ordered edit plan

Dependency order honored: ledger-readable + real-concurrency land **before** wiring orchestrator/composer; one section flipped agentic + quote-at-URL bugfix is the **first live proof**; composer/media-plan user-click is **last**.

### P1 — Ledger readable (getFacts DB SELECT + factStore threading) + real bounded pool

> Two clusters land together because Phase 3+ wiring is worthless without both. Ends each cluster tsc-clean + targeted tests.

**Cluster A — make the shared ledger READABLE (Task 06).** Ends: tsc-clean + `research-fact.test.ts` green with a new cross-instance SELECT test.

1. `src/lib/lab-engine/evidence/research-fact.ts:286-294` — extend `ResearchFactsSupabaseClient` so `from('research_facts')` also returns a select builder (`.select('*').eq('parent_audit_run_id', value)` → `{ data, error }`). Mirror the evidence-pool client shape (`evidence-pool.ts:519-522`). **Why:** interface today declares INSERT only — there is no SELECT capability at all.
2. `research-fact.ts:310-334` — `createResearchArtifactsResearchFactStore(client, parentAuditRunId?)` gains a 2nd arg; `getFacts()` (today `:333` returns `[...appended]`) becomes a real async SELECT keyed by `parent_audit_run_id`, with a snake→camel row mapper (inverse of `toResearchFactRow` at `:296-308`, null `parent_audit_run_id` → omit) validated by `researchFactSchema.array()`. Dedup by `id` in the mapper (no unique constraint on the table, migration `:4-5`). **Why:** THE write-only-ledger fix; without it the orchestrator writes rows nobody reads.
3. `research-fact.ts:61-64 + 86-100` — flip `ResearchFactStore.getFacts` to `() => Promise<ResearchFact[]>`; make `readResearchFactsFromStore` async (`await store.getFacts()` before `safeParse` at `:89`); update `createInMemoryResearchFactStore` (`:66-77`), `createNoopResearchFactStore` (`:79-84`), the echo getFacts (`:333`), and `Pick<ResearchFactStore,'getFacts'>` typings (`run-section.ts:444, 470, 690`). **Why:** SELECT is async; convert the whole chain in one commit or tsc breaks.
4. `run-section.ts:1242-1276` — `buildPreparedSectionContext` calls `readResearchFactsFromStore(factStore)` synchronously (`:1262-1267`); make it async + await, propagate up through `getPreparedSectionContext` (`:719-746`) and `prepareSectionContext` (`:763-779`, already async). **Why:** async ripple; convert atomically.
5. **KEYSTONE READ-PATH FIX** — `lab-section-dispatch.ts:138-144` — change `prepareSectionContext({runId, sectionId}, { store })` to `{ store, factStore, parentAuditRunId: seeded.parent_audit_run_id }`. `factStore` is already constructed at `:133-135` and threaded into the *job* at `:162-163`, but the pre-built `preparedContext` WINS downstream via a `??` short-circuit (`run-section.ts:728-735`), so the job's factStore is dead. **Why:** this single line is why the ledger is write-only in practice even after the SELECT fix.
6. `lab-section-job.ts:64-71` — the in-job fallback `prepareSectionContext(...,{ store })` (`:66-72`) should also pass `factStore`+`parentAuditRunId` (defense-in-depth so read works even when dispatch doesn't pre-build context). Construct factStore with `parentAuditRunId` per edit-2's 2-arg signature.
7. `research-fact.test.ts:62,82,130,143` — convert to async `getFacts`; **add a test** proving a Supabase-backed store issues a SELECT `eq parent_audit_run_id` and returns a *sibling-written* fact (the cross-instance case the echo array structurally cannot cover).

**Cluster B — real bounded pool, replace the `after()` fiction (P0; Task 05).** Ends: tsc-clean + a concurrency test green + existing `route.test.ts:1128` timeout-contract assertion still holds.

8. Decide topology first (**Needs owner**). **Recommended least-risk (Map 6 Option c):** `orchestrate/route.ts:256` — replace the HTTP fan-out (`dispatchLabSectionJobs`) with an in-process `p-limit(3)`-bounded loop (or reuse the existing 4-line bounded-worker pattern at `:131-148` to avoid the `p-limit` dep — grep found zero `p-limit` references) that runs all 6 sections inside ONE Fluid lambda; keep `lab-section-dispatch.ts:146` `input.schedule` but feed it a synchronous-await scheduler (`schedule: (fn)=>fn()`) so the 3 GLM loops share one process and the bound is REAL. **Why:** `dispatchBounded(3)` today bounds only 202-ACK kickoffs; 6 GLM loops run unbounded across 6 lambdas → token-explosion / rate-limit risk.
9. `orchestrate/route.ts:34-35` — `maxDuration=800` + Fluid (pulled forward from P8 because GLM loops exceed 285s); `lab-section-dispatch.ts:38-42` — raise `LAB_SECTION_JOB_TIMEOUT_MS` to ~720s so `job(720) < route(800)` holds. **Why:** the 285s job vs 300s route ceiling aborts GLM loops mid-tool-call.
10. **Re-point the paid-media onJobComplete chain** — if edit-8 collapses 6 lambdas into one in-process loop, the per-section `onJobComplete` → `dispatchPaidMediaIfSixComplete` (`run-lab-section/route.ts:550-619`, `lab-section-dispatch.ts:168`) must fire after the loop drains, not per-lambda, or paid-media never dispatches and the ADR-0012 wave-drain rescue (`run-lab-section/route.ts:213-381`) breaks. **Why:** both assume 6 independent invocations. (This trigger is removed in P6 anyway, but must not break in between.)

### P2 — Flip ONE section agentic on the live path + quote-at-URL bugfix + loud fallback telemetry (Task 03)

Ends: tsc-clean + `run-section-agentic-unbypass.test.ts` green + new fallback-telemetry tests green.

1. **Quote-at-URL per-record bugfix FIRST** — commit `7afc84fc` is the quote-grounding keystone. **unverified:** no map covered this module's file/line. **Read the verification module before editing** to confirm the per-record check is complete. **Why:** gates "no fabrication"; flipping a section live without it ships ungrounded quotes.
2. `run-section.ts:13016, :13244, :13454, :13546` — add `appendEvent({type:'agentic-fallback', metadata:{reason}})` at the four silent fallback exits (non-projectable, generation-failure, no-committable-artifact, honest-gap-build-failure), mirroring the existing observable block at `:13089-13107`. Event type + zod schema already accept it (`activity-event.ts:21,227`). **Why:** today 4 of 5 fallback paths are console.warn-only — a section can flip ON, fail, and silently serve DeepSeek with no telemetry trail.
3. **No code change to flip one section** — set env `LAB_AGENTIC_GLM_SECTIONS=positioningVoiceOfCustomer` + ensure `FIRECRAWL_API_KEY` present + set `GLM_BASE_URL`/`GLM_MODEL_ID`/`GLM_API_KEY` (OpenRouter `z-ai/glm-5.2`). Verified: flag read via `process.env` at `run-section.ts:15219`; `deps.env` never set on live path (`lab-section-job.ts:80-92`). **Why:** VoC is proven-billable (Ramp VoC self-gapped a wrong-entity result with no oracle). The target section MUST be in `answerToolSectionIds` (agentic branch gated at `:15218`) AND in `PROJECTABLE_SECTION_IDS` (`:12769-12775`).
4. Run one live `/research-v3` validation against a frozen subject; confirm via the new telemetry that GLM actually ran (no `agentic-fallback` event), facts landed AND were read (P1), quotes URL-grounded.

### P3 — Drop the output cage: projector + strict parse → tolerant decoder, free-markdown + visible labels

> Scoped: for the **media-plan composer** specifically the cage is the strict parser; the projector is load-bearing for the 6 positioning sections and is NOT deleted while only one section is flipped (see Contradictions). Ends: tsc-clean + tolerant-decoder test green.

1. `composer-glm.ts:130/160-161` — replace the strict `parsePaidMediaPlanFromText` (`paidMediaPlanBodySchema.safeParse`) so the extracted fenced JSON runs through `normalizePaidMediaPlanBody` (`artifacts/schemas/paid-media-plan.ts:1994-2095`) instead. Thread structured onboarding economics (`targetCac`, `cvrChain`, `creativeCapacity`, `channelHint`) the way `withNormalizedPaidMediaPlanOutput` does (`run-section.ts:8150-8189`). **Why:** strict parser nulls usable GLM decks on shape drift (`{audiences:[...]}` wrappers, <2-leg cross-section insights, overshoot rows) the section path already survives — a regression vs today's DeepSeek deck. Composer also receives `onboardingFrame` as a STRING (`composer-glm.ts:60`); thread the structured economics object or the budget cascade / CAC math is lost.
2. Free-markdown + visible grounded/inferred/gap labels: the agentic section path already commits GLM markdown even when projection fails (`run-section.ts:13337-13344`, no DeepSeek fallback on projection failure). Confirm the markdown-with-labels survives to the typed-artifact-renderer card (per memory: glm-raw-uncage slice already proven, markdown survives to card). **unverified at line-level by the maps** — re-confirm the renderer path before relying on it.

### P4 — Wire orchestrator into /orchestrate + onStepFinish progress (Task 07 orchestrator half)

Ends: tsc-clean + `orchestrator-glm.test.ts` green + an /orchestrate route test asserting orchestrator runs before fan-out + idempotency-guard test. **Mind the route budget — P1 edit-9 must have already moved this route to maxDuration=800/Fluid** (orchestrator latency ~161-340s would blow a 300s route inline).

1. `orchestrate/route.ts` (new imports) — `generateAgenticGLMOrchestrator`, `promoteOrchestratorFactsToLedger` from `@/lib/lab-engine/agents/orchestrator-glm`; `createResearchArtifactsResearchFactStore` from `@/lib/lab-engine/evidence/research-fact`; `createAdminClient` from `@/lib/supabase/server`.
2. `orchestrate/route.ts:~255` (between `freezeReviewedBriefSnapshot` `:250-254` and `dispatchLabSectionJobs` `:256`) — call `generateAgenticGLMOrchestrator({websiteUrl, onboardingBrief: JSON.stringify(session.onboarding_data ?? {}), env})` under an AbortController bounded under the route budget, then `promoteOrchestratorFactsToLedger(store, result.transcript, {runId: body.run_id, createdAt: new Date().toISOString(), parentAuditRunId: seeded.parent_audit_run_id})` so facts land with the SAME parent the sections filter on (P1 makes them readable). **Why:** sections currently start cold; this seeds the ledger.
3. **gtm-fields delivery** — `orchestrate/route.ts:253` — merge `result.gtmFields` (esp. `topCompetitors`, `marketProblem`) + `result.researchDigest` into the snapshot passed to `freezeReviewedBriefSnapshot` (reorder so orchestrator runs BEFORE freeze) so `corpusToResearchInput` (`run-lab-section/route.ts:510-515`) → `buildCompetitorSeeds` (`corpus-to-research-input.ts:1169-1234`) gets agent-discovered competitors. **Why:** orchestrator enrichment is otherwise discarded — defeats the onboarding-reframe AND fixes the blank-topCompetitors → empty-ad-seeds starvation (Map 7).
4. **Idempotency guard** — skip the orchestrator (paid GLM call + duplicate fact rows; no unique constraint) if facts already exist for `parent_audit_run_id` or sections aren't all `queued`. **Why:** a re-POST is idempotent for seeding (`orchestrate-db.ts seed_orchestration`) but the orchestrator is not.
5. **onStepFinish streaming** — add a step callback in `orchestrator-glm.ts:180-188` `generateText` that broadcasts progress via the existing `section-partials:${runId}` Realtime layer (`use-section-partials.ts:83-104`, `realtime-broadcast.ts`, `section-partial-broadcaster.ts`); add a UI consumer. The GLM section runner at `agentic-glm-runner.ts:225-233` has NO `onStepFinish` today — add it there too if section step-progress is wanted. **Why:** silent multi-min orchestrator gap violates the streaming-progress requirement.

### P5 — Composer "generate media plan" click live in research-v3 (Task 04 + Task 07 composer half)

Ends: tsc-clean + `composer-glm.test.ts` green + a compose-route test.

1. `committed-positioning-artifacts.ts:34-102` — add a helper returning `Partial<Record<PositioningSectionId,string>>` of committed narrative markdown keyed by sectionId (the `committedSectionMarkdown` shape the composer expects at `composer-glm.ts:56`), distinct from the existing full-ResearchInput helper. Add a `buildLedgerDigest` helper feeding `composer-glm.ts:108-109` `ledgerDigest` from the now-readable SELECT (P1). **Why:** both composer inputs have NO live producer today.
2. NEW route `src/app/api/research-v2/compose-media-plan/route.ts` (or a `mode:'compose'` branch on `rerun-section/route.ts` which already special-cases `PAID_MEDIA_PLAN_SECTION_ID` at `:61`) — user-click endpoint: load 6 committed markdowns → build ledgerDigest → `composePaidMediaPlan` → `composerStripFloor` (`composer-glm.ts:187-202`) + `deck-ledger-gate` → commit via existing `saveCompletedArtifact` spine. Add to `isPublicRoute` in `src/middleware.ts` if it uses `x-internal-key` (learned-patterns Next.js rule). **Normalize `competitorReviewInsights` to EXACTLY 3** before `composerStripFloor` (it hard-requires 3 at `:195`; GLM emits 2 or 4; tolerant decoder does not force exactly-3).
3. **Remove BOTH auto-triggers** — `use-audit-state.ts:290-318` (client poll `shouldDispatchPaidMediaPlan` + `dispatchPaidMediaPlan` `:142-158`) AND `run-lab-section/route.ts:351-440` (server `onJobComplete` → `dispatchPaidMediaIfSixComplete`). **Why:** target UX = user clicks; disabling only one leaves it auto-firing (CAS dedup `claimSectionRun` is load-bearing today — both must go together).
4. `audit-reader-shell.tsx:1434-1442` — replace the static "Ready to run." `PaidMediaPlanTerminalPanel` render with a "Generate media plan" button (enabled only when `statusOf(mediaPlan)==='ready'`, the gate already computed at `:1021-1023`) that POSTs to the compose route. (The existing `rerunSection` helper at `:1225-1257` is a ready-made dispatch shape if the route reuses rerun-section.)

### P6 — Anti-fab thin-layer + human-review-before-Share gate (Task 08)

Ends: tsc-clean + anti-fab gate tests green. **unverified by the maps** (outside their read scope) — read the LOCKED arch doc (`docs/plans/2026-06-24-LOCKED-ai-engineering-architecture.md`) before implementing.

1. Machine-assigned labels (model-claimed labels must be MACHINE-assigned, not model-self-reported), numeric-provenance gate over the undefended numeric surface, `claim_ledger`, and a **code-enforced human-review-before-Share gate**. The `deck-ledger-gate.ts` blocking liar-catcher exists but has zero callers — wire it in the compose route (P5 edit-2), but ONLY after P1 makes the ledger readable or every cell dereferences against an empty ledger and the gate false-fails the whole deck.

### P7 — Ad/SpyFu firing verified in agentic competitor + media-plan path (Task 07/11)

Ends: tsc-clean + a deterministic-ad-prepass test on the agentic path.

1. `run-section.ts:13073-13074` — the agentic path already sources `agenticAllowedTools = deps.allowedTools ?? definition.allowedTools` and builds via `buildAgenticTools`. Add a deterministic competitor ad prepass here (before generation) mirroring `runCompetitorAdRescueProbe` (`:9419-9475`, constants `:5998-6034`): seed from `researchInput.competitorSeeds` (+ post-draft from GLM's discovered competitorSet) and inject results into the prompt/transcript. **Why:** the agentic path has NO deterministic ad prepass — the `adReservedLookups:24` reserve, the 8-advertiser probe, and the rescue probe are NEVER invoked, so the 8/8 competitor ad coverage the answer-tool path guarantees does not happen.
2. `run-section.ts:13150-13151` — the "AT MOST 6 tool calls total" prompt cap starves competitor ad coverage (positioning research crowds out ad-library calls). For the competitor section, raise the cap or carve a separate ad-call budget (the `adReservedLookups:24` model). Note `runCompetitorAdRescueProbe`'s guard at `:9456` returns undefined when `shouldUsePreparedContext(deps)` is true — the agentic path always has preparedContext, so that guard must be relaxed/parameterized.
3. `corpus-to-research-input.ts:1169-1171` — `buildCompetitorSeeds` early-returns `[]` on blank `rawTopCompetitors`. P4 edit-3 (orchestrator topCompetitors → competitorSeeds) is the fix; verify the seam delivers seeds so the deterministic ad probe is seeded even when human onboarding left competitors blank. **Why:** on the agentic path the deterministic rescue is bypassed, making blank-topCompetitors → empty-ad-wall WORSE than the answer-tool path.

### P8 — Serverless hardening (Tasks 09/10)

Ends: tsc-clean + non-localhost preflight test green. (maxDuration=800/Fluid already landed in P1 edit-9.)

1. `vercel.json` (does not exist; only `next.config.ts`) or per-route config — enable Fluid compute; confirm `maxDuration=800` holds on the route hosting the GLM loop (`orchestrate/route.ts:35`). **Confirm Vercel plan tier supports it (Needs owner).** Confirm Next.js `after()` survives the full 800s on Fluid before relying on it.
2. **Non-localhost env assertion** — `models.ts:634-650` `checkAgenticGLMPreflight` today only checks non-empty strings. Add a hard-fail-loudly assertion when `GLM_BASE_URL` is still `localhost` while `LAB_AGENTIC_GLM_SECTIONS` is non-empty; wire through `buildLabSectionProviderPreflightResponse` (`run-lab-section/route.ts:493`, `orchestrate/route.ts:235`). **Why:** with the flag set but no `GLM_BASE_URL`, every GLM call hits dead `http://localhost:11434` → generation-failure fallback → silently serves DeepSeek, masking the misconfiguration.
3. **Model fallback** — define the explicit DeepSeek fallback posture (observable, not silent) per the new P2 telemetry events.

### P9 — Live E2E validation

Ends: a full 6-section + composer run against a frozen subject on the OpenRouter GLM endpoint, with telemetry confirming GLM ran, facts were read cross-invocation, quotes URL-grounded, ad tools fired on competitor, and the human-review gate held before Share.

---

## Riskiest edits

The 5 where a wrong change silently breaks prod (no error, no telemetry) — and the test to write FIRST.

- **R1 — `lab-section-dispatch.ts:138` (`{ store }` → `{ store, factStore, parentAuditRunId }`).** Wire `promoteOrchestratorFactsToLedger` (P4) but forget this line and the integration LOOKS done — rows written, no errors — but sections never read them. Zero cross-invocation value, completely silent. **Test first:** an integration test running `prepareSectionContext` against a Supabase-backed store seeded with a sibling fact, asserting `preparedContext.factRows.length > 0`. Must pass BEFORE P4.
- **R2 — `research-fact.ts:333` getFacts async + SELECT.** Half-conversion is a loud tsc break (fine); the worse failure is the SELECT silently returning `[]` if keyed on the wrong column or run against a non-service-role client. **Test first:** the cross-instance SELECT test (P1 Cluster A edit-7) — a fact written by store-instance-A is returned by store-instance-B. (Client IS service-role per `createAdminClient`, verified — but the test must still confirm.)
- **R3 — `composer-glm.ts:130` strict→tolerant decoder swap.** Without it, real GLM decks null out on shape drift the DeepSeek path survives → `composerStripFloor` returns `deck_body_missing` → user clicks "Generate" and gets nothing, no error. **Test first:** a decoder test feeding 3-4 known GLM drift shapes through the composer extraction, asserting a non-null deck + passing strip floor.
- **R4 — P1 Cluster B concurrency rewire (`orchestrate/route.ts:256` + paid-media re-point).** Collapsing 6 lambdas into one loop silently orphans the per-section `onJobComplete` → paid-media chain and the ADR-0012 wave-drain rescue (both assume 6 independent invocations). Symptom: sections complete but media plan never dispatches, OR a crashed section is never rescued — both silent. **Test first:** assert (a) the bound is real (≤3 concurrent GLM-model invocations via a counting mock) and (b) the drain-complete hook fires exactly once after all 6.
- **R5 — GLM localhost-in-prod (`models.ts:15-16` defaults).** Flag set, no `GLM_BASE_URL` → every call hits dead `http://localhost:11434` → generation-failure fallback → silently serves DeepSeek, masking the misconfiguration entirely. **Test first:** the non-localhost preflight assertion (P8 edit-2) that hard-fails loudly — write it + its test BEFORE any prod env flip (P2 edit-3 / P9). Highest-leverage make-the-silent-failure-loud guard.

---

## Needs owner

Verbatim, genuinely ambiguous, code cannot resolve:

- **Concurrency topology:** Map 6 offers three real options — (a) expiring DB-lease/pgmq (most robust, survives lambda eviction, most work), (b) Supabase pgmq/Vercel Queues (platform-native, highest infra cost, changes the synchronous-ACK UX), (c) `p-limit(3)` in one Fluid lambda (least risk, but holds the connection / relies on `after()` surviving 800s). The LOCKED spec named expiring DB-lease/pgmq; Map 6 recommends (c) first. **Owner must pick** — this determines Phase 2's entire shape.
- **How many sections go agentic GLM** (topology: "3-of-6 pool" per LOCKED spec vs all 6). The maps verify the *mechanism* (env CSV) but not the *count* decision.
- **Keep or remove the section-path media plan as a fallback** once the composer user-click lands (`run-lab-section/route.ts:550-619` auto-dispatch). Task 11 also flags a Perplexity keep/kill A/B.
- **ceiling-7 vs citation-perfect** anti-fab posture (carried open from the FINAL arch doc) — affects Phase 5 gate strictness.
- **maxDuration=800 requires Vercel Pro/Fluid confirmation** — owner must confirm the plan tier supports it.

---

## Contradictions / low-confidence

Where maps disagreed or a mapper was unsure — flag for hand-verification before editing:

- **RESOLVED this session (maps were imprecise):** Map 1 and Map 4 both implied dispatch passes only `{ store }` and never threads `factStore`. Actual code: `lab-section-dispatch.ts:138` passes `{ store }` to `prepareSectionContext` (the consumed context) BUT `:162-163` DO thread `factStore`+`parentAuditRunId` into the deferred job. The bug is real but narrower — the *job's* factStore is dead only because `preparedContext` wins via short-circuit. Edit P1-Cluster-A-5 (line 138) is the precise fix. No further hand-verification needed.
- **RESOLVED: RLS / client privilege.** Map 4 flagged "VERIFY which client dispatch receives" as inferred. Confirmed: `run-lab-section/route.ts:97/504` → `createAdminClient()` (service-role). The cross-process SELECT will work. No hand-verification needed.
- **Read-after-write ORDERING (Map 4, genuine architectural catch, NOT resolved):** `prepareSectionContext` is called ONCE at dispatch time, before sibling sections write. A SELECT then captures orchestrator facts (written earlier in P4) but MISSES live sibling-section facts. If true cross-*section* fact sharing is required (not just orchestrator→section), the SELECT must move INSIDE the section job just before drafting. **Verify the requirement with owner** — orchestrator→section sharing works with the dispatch-time SELECT; section→section does not.
- **Orchestrator latency (~161-340s)** is from the task brief, NOT re-measured (Map 1 medium-confidence). Phase 1's 800s budget assumes this; confirm with the Task-01 value-bar live runs.
- **`transcriptToCorpusExcerpts` tool-result shapes (`orchestrator-glm.ts:264-305`)** — Map 1 did not open the actual `web_search`/`firecrawl`/`perplexity_research` result shapes to confirm they expose `url`/`text` keys. If they don't, facts silently won't promote. **Hand-verify against TOOL_CATALOG result shapes before trusting P4 ledger seeding.**
- **Quote-at-URL grounding check internals (commit `7afc84fc`)** — NO map covered this module's file/line. P2 edit-1 depends on it. **Read it directly before editing.** (unverified)
- **`run-section.ts` line anchors for the async ripple (`:444, :690` per Map 4) and the agentic fallback exits (`:13016/13244/13454/13546`)** — `run-section.ts` is ~15.7k lines; these were grepped by the mappers, not re-confirmed this session. **Re-grep before editing** (line numbers drift across the uncommitted working-tree changes).
- **`agentic-glm-projector.ts` deletion** (Map 2 says delete 566 lines + 5 call sites in run-section.ts) — for the media plan it's MOOT (paid-media is non-projectable). But the 6 positioning sections still USE the projector on the agentic path. **Do NOT delete it while only one section is flipped (P2/P3)** — it's load-bearing for the agentic section path being flipped on. Map 2's "delete the projector" conflicts with P2's "flip one section agentic"; treat projector deletion as out-of-scope until all sections move off it.

---

## Map appendix

The `exactIntegrationPoints` arrays from each of the 7 maps, verbatim. These are the gold.

### Map 1 — Orchestrator tier (orchestrator-glm.ts, /orchestrate route, orchestrate-db.ts)

- src/app/api/research-v2/orchestrate/route.ts:255 — insert the orchestrator call AFTER freezeReviewedBriefSnapshot (route.ts:250-254) and BEFORE dispatchLabSectionJobs (route.ts:256). Build args from the already-loaded `session`: websiteUrl from session onboarding/url field, onboardingBrief = JSON.stringify(session.onboarding_data ?? {}). Pass env (defaults process.env). Use an AbortController bounded well under the 300s route budget.
- src/app/api/research-v2/orchestrate/route.ts (new import) — `import { generateAgenticGLMOrchestrator, promoteOrchestratorFactsToLedger } from '@/lib/lab-engine/agents/orchestrator-glm'` and `import { createResearchArtifactsResearchFactStore } from '@/lib/lab-engine/evidence/research-fact'` + `createAdminClient` from '@/lib/supabase/server'. After generateAgenticGLMOrchestrator returns, call promoteOrchestratorFactsToLedger(store, result.transcript, {runId: body.run_id, createdAt: new Date().toISOString(), parentAuditRunId: seeded.parent_audit_run_id}) so facts land with the SAME parent_audit_run_id the sections filter on.
- src/lib/lab-engine/evidence/research-fact.ts:333 — getFacts() in createResearchArtifactsResearchFactStore must be changed (or a sibling reader added) to SELECT from research_facts WHERE parent_audit_run_id = <parent> so section invocations actually SEE orchestrator-seeded facts. As written it returns only in-process appended[]; this is the keystone of the write-only-ledger bug. (Section read path that consumes it: run-section.ts:684-702 + factMatchesPreparedRun run-section.ts:599-611.)
- src/app/api/research-v2/orchestrate/route.ts:253 (gtm-fields delivery) — to make orchestrator gtmFields/researchDigest reach sections, merge result.gtmFields + result.researchDigest into the snapshot passed to freezeReviewedBriefSnapshot (or persist them where corpusToResearchInput at run-lab-section/route.ts:510-515 will pick them up). Reorder so the orchestrator runs BEFORE freezeReviewedBriefSnapshot if the frozen brief is the chosen carrier.
- TIMEOUT MITIGATION — either (a) bump orchestrate route maxDuration to 800 with Fluid compute (route.ts:35) per the locked spec, OR (b) run the orchestrator in its own after()-deferred sub-invocation / dedicated kickoff route and gate section fan-out on its completion. Inline at maxDuration=300 (route.ts:35) is the risky path given ~161-340s orchestrator latency.

### Map 2 — Composer + media plan (positioningPaidMediaPlan, composer-glm.ts, tolerant decoder, projector cage)

- src/lib/lab-engine/agents/composer-glm.ts:130 — replace `parsePaidMediaPlanFromText(result.text)` so the extracted fenced JSON is run through normalizePaidMediaPlanBody (import from artifacts/schemas/paid-media-plan) instead of the strict paidMediaPlanBodySchema.safeParse at line 160. This closes the strict-parse hole and reuses the proven tolerant decoder + budget cascade. Pass onboarding economics options (creativeCapacity/targetCac/cvrChain/channelHint) the same way withNormalizedPaidMediaPlanOutput does (run-section.ts:8150-8189).
- NEW route src/app/api/research-v2/compose-media-plan/route.ts (or add a `mode:'compose'` branch to rerun-section/route.ts which already special-cases PAID_MEDIA_PLAN_SECTION_ID at line 61) — user-click endpoint that: (1) loads the 6 committed section markdowns keyed by PositioningSectionId, (2) builds a ledgerDigest from research_facts, (3) calls composePaidMediaPlan, (4) runs composerStripFloor + deck-ledger-gate, (5) commits the artifact via the existing saveCompletedArtifact spine. Must be added to isPublicRoute in src/middleware.ts if it uses x-internal-key (per learned-patterns Next.js rule).
- src/app/api/research-v2/run-lab-section/route.ts:550-619 (dispatchPaidMediaIfSixComplete + the onJobComplete chain) — this is the auto-trigger to REPLACE with the user-click composer per the locked arch. Today it scheduleLabSectionJob(PAID_MEDIA_PLAN_SECTION_ID); the target is to stop auto-dispatching the 7th section and instead let a user click invoke composePaidMediaPlan. Decide: keep section auto-dispatch as fallback or remove.
- src/lib/research-v2/committed-positioning-artifacts.ts:34-102 — add a helper that returns Partial<Record<PositioningSectionId,string>> of committed narrativeMarkdown keyed by sectionId (the committedSectionMarkdown shape composePaidMediaPlan expects at composer-glm.ts:56), distinct from the existing full-ResearchInput committedPositioningArtifacts.
- src/lib/lab-engine/evidence/research-fact.ts:330 — getFacts must do a DB SELECT from research_facts (by parent_audit_run_id) so the ledger is readable across after() processes; add a buildLedgerDigest helper to feed composePaidMediaPlan's ledgerDigest arg (composer-glm.ts:108-109). (Task 06 prerequisite for the composer.)
- DELETE src/lib/lab-engine/agents/agentic-glm-projector.ts and its imports/uses in run-section.ts (lines 140-142 import, 12752-12779 ProjectableSectionId/isProjectableSectionId, 13014-13020 guard, 13318-13340 projectMarkdownToTypedBody call). For the media plan specifically, this is moot since paid-media is already non-projectable — the deck must instead flow through normalizePaidMediaPlanBody (tolerant decoder), which is the projector's replacement for this section.
- src/components/research-v2/audit-reader-shell.tsx:562/1390/1435 (PaidMediaPlanTerminalPanel) — add the 'Generate Paid Media Plan' user-click button that POSTs to the new compose route, gated on 6/6 rollup complete; currently the panel only renders an already-committed artifact.

### Map 3 — Agentic GLM section path + LAB_AGENTIC_GLM_SECTIONS flag

- TO FLIP AGENTIC GLM ON FOR ONE SECTION ON THE LIVE PATH (no code change needed): set env var LAB_AGENTIC_GLM_SECTIONS=positioningVoiceOfCustomer (CSV; add more sectionIds comma-separated) in the run environment (Vercel prod or .env.local), AND ensure FIRECRAWL_API_KEY is present in that same env. The flag is read via process.env at run-section.ts:15219 (deps.env is never set on the live path, lab-section-job.ts:80-92). To target OpenRouter GLM instead of localhost Ollama also set GLM_BASE_URL / GLM_MODEL_ID / GLM_API_KEY (consumed in models.ts:569-577). No source edit is required to enable one section live.
- MINIMAL CODE CHANGE IF A SECTION MUST BE ON BY DEFAULT WITHOUT AN ENV VAR: edit shouldUseAgenticGLM at src/lib/lab-engine/agents/run-section.ts:6086-6099 to add a default-on allowlist when the env key is unset (e.g. fall back to a const DEFAULT_AGENTIC_SECTIONS=['positioningVoiceOfCustomer'] instead of returning false at :6091). Surgical and reversible. Prefer the env-var route above unless a code default is explicitly wanted.
- agentic_fallback TELEMETRY ALREADY LANDED — extend, don't add. The only fallback that emits today is the empty-tools guard (run-section.ts:13089-13107). The OTHER silent fallbacks do NOT emit an agentic-fallback event and should each gain one for full observability: (a) non-projectable section fallback at run-section.ts:13016 (console.warn only); (b) generation failure fallback at :13244; (c) 'no committable typed artifact' fallback at :13454; (d) honest-gap build failure at :13546. Add an appendEvent({type:'agentic-fallback', metadata:{reason:...}}) at each, mirroring the :13089-13107 block. The event type + zod schema already accept it (activity-event.ts:21,227).
- TO MAKE dispatchBounded BOUND REAL GLM CONCURRENCY (P0): the cap must move from HTTP kickoff (orchestrate/route.ts:113) into the run-lab-section lambda's after()-scheduled job (lab-section-dispatch.ts:145 / lab-section-job.ts). Per the LOCK doc this means an expiring DB-lease or pgmq gate acquired inside runLabSectionJob before runSection, not a Promise-pool over fetch kickoffs. (Larger than a one-section flip; flagged for Task 05/07.)

### Map 4 — research-fact ledger read/write (research-fact.ts + consumption)

- research-fact.ts:286-294 — extend ResearchFactsSupabaseClient so from('research_facts') also returns a select builder. Add ResearchFactsSelectBuilder with chainable .select('*').eq('parent_audit_run_id', value) resolving to { data: Record<string,unknown>[] | null; error: {message:string}|null }. Mirror evidence-pool client shape (evidence-pool.ts:519-522: client.from(table).select(field).eq(...).eq(...)).
- research-fact.ts:310-334 — make createResearchArtifactsResearchFactStore.getFacts() a REAL DB SELECT keyed by parent_audit_run_id. Pass parentAuditRunId as a 2nd factory arg: createResearchArtifactsResearchFactStore(client, parentAuditRunId); getFacts does `await client.from('research_facts').select('*').eq('parent_audit_run_id', parentAuditRunId)`. Because a SELECT is async, getFacts MUST become async (Promise<ResearchFact[]>). Add a row->ResearchFact mapper (inverse of toResearchFactRow at research-fact.ts:296-308: snake_case->camelCase, null parent_audit_run_id -> omit), then validate with researchFactSchema.array() (reuse readResearchFactsFromStore pattern at research-fact.ts:89).
- research-fact.ts:61-64 + 86-100 — flip ResearchFactStore.getFacts to () => Promise<ResearchFact[]> and make readResearchFactsFromStore async (await store.getFacts() before safeParse at :89). Update createInMemoryResearchFactStore (:66-77), createNoopResearchFactStore (:79-84), the appended-echo getFacts (:333), and the Pick<ResearchFactStore,'getFacts'> typings (run-section.ts:444, 470, 690) to the async signature.
- run-section.ts:1242-1276 — buildPreparedSectionContext calls readResearchFactsFromStore(factStore) synchronously (:1262-1267). Make it async and await; propagate async up through getPreparedSectionContext (:719-746) and prepareSectionContext (:763-779, already async — just await the builder).
- lab-section-dispatch.ts:138-144 — THE KEY READ-PATH FIX: change the deps arg from `{ store }` to `{ store, factStore, parentAuditRunId: seeded.parent_audit_run_id }`. With getFacts now SELECTing by parent_audit_run_id, the section's preparedContext.factRows will include orchestrator + sibling facts. If using the 2nd-arg factory design, create factStore at :133-135 with seeded.parent_audit_run_id (available at :122).
- lab-section-job.ts:64-71 — the in-job fallback prepareSectionContext(...,{ store }) (:66-72) should ALSO pass factStore+parentAuditRunId (defense in depth so the read works even when dispatch does not pre-build context): `{ store: input.store, ...(input.factStore?{factStore:input.factStore}:{}), ...(input.parentAuditRunId?{parentAuditRunId:input.parentAuditRunId}:{}) }`.
- research-fact.test.ts:62,82,130,143 — update to async getFacts; add a test that a Supabase-backed store getFacts issues a SELECT eq parent_audit_run_id and returns sibling-written facts (the cross-instance case the echo array can't cover).

### Map 5 — Concurrency + Serverless (after()-based dispatch, fake bounded pool)

- RECOMMENDED FIRST (least-risk): Option (c) p-limit(3) inside ONE Fluid lambda. Touch points: (1) src/app/api/research-v2/orchestrate/route.ts:34-35 — add `export const preferredRegion`/keep nodejs and bump `maxDuration = 800`; (2) orchestrate/route.ts:256 `await dispatchLabSectionJobs(...)` — replace the HTTP fan-out with an IN-PROCESS `p-limit(3)`-bounded loop that calls `scheduleLabSectionJob` with `schedule: (fn)=>fn()` (run inline, awaited) instead of `schedule: after`, so all 6 sections run inside this one 800s invocation under a true semaphore of 3; (3) lab-section-dispatch.ts:146 `input.schedule(async()=>…)` already supports a synchronous-await scheduler, so the existing per-job 285s AbortController stays as the inner guard — raise `LAB_SECTION_JOB_TIMEOUT_MS` (lab-section-dispatch.ts:38-42) to ~720s for the GLM path. Add `p-limit` via npm. This keeps the existing claim/CAS, store, and rescue logic untouched and needs no new DB objects — the bound becomes real because all 3 GLM loops share one process.
- ALTERNATIVE (most robust, more work): Option (a) expiring DB lease keyed on parent_audit_run_id. Touch points: (1) NEW migration extending supabase/migrations/20260601_claim_section_run.sql to add `lease_expires_at timestamptz` + a `claim_section_slot(p_parent_audit_run_id, p_max_concurrency)` RPC that atomically counts non-expired `running` siblings and only flips queued→running if count < 3 (advisory lock or `FOR UPDATE` on the parent row); (2) src/lib/research-v2/section-run-claim.ts:88 — add a `claimSectionSlot` wrapper; (3) lab-section-dispatch.ts:85-90 — call the slot-claim instead of the plain claim, and if denied re-enqueue (return without scheduling); (4) agentic-glm-runner.ts:225 — add `onStepFinish: () => renewLease(sectionRunId)` to `generateText` to heartbeat `lease_expires_at = now()+interval`; (5) lab-section-dispatch.ts:169 `finally` block — release the lease (set lease_expires_at = now()) so a crashed/aborted section frees its slot for the rescue pass. This is the only option that survives lambda eviction and the after() invisibility, but it requires a new RPC + heartbeat + a requeue trigger.
- ALTERNATIVE (platform-native): Option (b) Supabase pgmq / Vercel Queues. Touch points: orchestrate/route.ts:256 enqueues 6 messages; a queue consumer (pg_cron-driven worker or a Vercel cron route) dequeues with visibility-timeout and concurrency 3, calling scheduleLabSectionJob. Highest infra cost (new queue, consumer, visibility-timeout tuning) and a larger departure from the current synchronous-ACK UX, so NOT first.
- Serverless prod-readiness (needed for ANY option that lets GLM run >285s): create /Users/ammar/Dev-Projects/AI-GOS/vercel.json (or set per-route) enabling Fluid compute, and raise `maxDuration` to 800 on the route that actually hosts the GLM loop (orchestrate/route.ts:35 for Option c; run-lab-section/route.ts:40 if kept per-section). Pair with raising `LAB_SECTION_JOB_TIMEOUT_MS` (lab-section-dispatch.ts:38) so job<route still holds (e.g. 720<800).
- Prod env assertion (orthogonal but required): models.ts:617 `getAgenticGLMModel` / :632 `checkAgenticGLMPreflight` default to localhost (models.ts:15). Add a prod guard that fails loudly if `GLM_BASE_URL` is still localhost when `LAB_AGENTIC_GLM_SECTIONS` is non-empty, wired through buildLabSectionProviderPreflightResponse (called at run-lab-section/route.ts:493 and orchestrate/route.ts:235).

### Map 6 — Frontend flow (/research-v3 page, onboarding submit, card render, progress/realtime, media-plan trigger)

- src/lib/research-v2/use-audit-state.ts:290-318 — REMOVE/GATE the client auto-dispatch of the paid media plan (shouldDispatchPaidMediaPlan + dispatchPaidMediaPlan at 142-158). For 'user clicks to generate', this auto-fire must become a no-op (or behind a flag) so the media plan does not start on its own.
- src/app/api/research-v2/run-lab-section/route.ts:351-440 — REMOVE/GATE the SERVER autonomous trigger dispatchPaidMediaIfSixComplete fired from onJobComplete on the 6th core-section commit. This is the tab-survives autonomy path; it must stop auto-running for the user-click UX.
- src/components/research-v2/audit-reader-shell.tsx:1434-1442 — REPLACE the static 'Ready to run.' PaidMediaPlanTerminalPanel render (when activeStatus==='ready') with a 'Generate media plan' button that calls a generate handler. The existing rerunSection helper at 1225-1257 (POST /api/research-v2/rerun-section { runId, zone: positioningPaidMediaPlan, executionMode:'lab' }) is the ready-made dispatch path — wire a button to call rerunSection(PAID_MEDIA_PLAN_SECTION_ID), then bump pollRefreshKey.
- src/app/api/research-v2/orchestrate/route.ts:243-262 + dispatchLabSectionJobs 88-121 — INTEGRATION SEAM for wiring the GLM orchestrator: this is where seedOrchestration + per-section kickoff happen. To run the locked architecture, the orchestrator-glm fact-write pass + GLM section pool would be invoked here instead of (or before) the DeepSeek run-lab-section fan-out. Currently calls neither GLM file.
- src/lib/lab-engine/agents/composer-glm.ts (201 lines) + a NEW route (e.g. /api/research-v2/compose-media-plan) — BUILD the user-click composer trigger. The 'Generate media plan' button (point 3) should target a composer route that runs composer-glm and commits the positioningPaidMediaPlan artifact, rather than the DeepSeek rerun-section path, to match the locked target.
- src/lib/research-v2/use-section-partials.ts:83-104 (channel section-partials:${runId}) + src/lib/research-v2/realtime-broadcast.ts + section-partial-broadcaster.ts — EXTEND for orchestrator/composer onStepFinish progress: add a broadcast emit from orchestrator-glm/composer-glm step callbacks and a UI consumer (today only DeepSeek section partials are broadcast). This is where onStepFinish progress would surface (Supabase Realtime broadcast).

### Map 7 — Ad Engine + SpyFu Tools (ad-library/SpyFu/keyword tools on the agentic GLM section path)

- src/lib/lab-engine/agents/run-section.ts:13073-13074 — the agentic path already sources agenticAllowedTools = deps.allowedTools ?? definition.allowedTools and builds tools via buildAgenticTools. To make ad tools RELIABLY fire for competitor under agentic GLM, add a deterministic competitor ad prepass here (before generation) that mirrors runCompetitorAdRescueProbe: seed from researchInput.competitorSeeds (and, post-draft, from GLM's discovered competitorSet) and inject the probe results into the prompt/transcript so the model does not have to spend its 6 calls discovering+probing.
- src/lib/lab-engine/agents/run-section.ts:13150-13151 — the 'AT MOST 6 tool calls total' cap in userPrompt is the throttle that starves ad coverage. For the competitor section specifically, either raise the cap or carve a separate ad-call budget (the answer-tool path's adReservedLookups:24 model) so positioning research does not crowd out ad-library calls.
- src/lib/lab-engine/agents/run-section.ts:9419-9475 (runCompetitorAdRescueProbe) and the deterministic prepass constants at :5998-6034 — this is the reusable, proven ad-probe machinery. The integration is to CALL it from runAgenticGLMSection (it is currently only reachable from runSectionViaAnswerTool / runSectionViaStructuredBodyStream). Note its guard at :9456 returns undefined when shouldUsePreparedContext(deps) is true — the agentic path always has a preparedContext, so that guard would have to be relaxed/parameterized for the agentic path.
- src/lib/research-v2/corpus-to-research-input.ts:1169-1171 (buildCompetitorSeeds early return []) — the blank-topCompetitors -> empty-seeds root. If onboarding is supposed to force the user to supply competitors (target arch), validate competitorSeeds is non-empty at submit, OR seed from the orchestrator's extracted topCompetitors (orchestrator-glm.ts:108 orchestratorGtmFieldsSchema.topCompetitors) before the competitor section runs.
- src/lib/lab-engine/agents/orchestrator-glm.ts:108 + :10-14 — the orchestrator extracts topCompetitors[] but its tool set excludes ad tools by design. To feed the section pool, persist orchestrator topCompetitors into researchInput.competitorSeeds (target arch: orchestrator writes facts to the ledger) so the competitor section's deterministic ad probe is seeded even when human onboarding left topCompetitors blank.
- src/lib/lab-engine/agents/run-section.ts:15219-15220 (shouldUseAgenticGLM gate) + env LAB_AGENTIC_GLM_SECTIONS — flipping competitor/demand onto the agentic path is what makes any of the above live; until then the ad/SpyFu tools fire only on the DeepSeek answer-tool path.
- src/app/api/research-v2/orchestrate/route.ts:107-131 + src/lib/research-v2/lab-section-dispatch.ts:144-172 — the after()/dispatchBounded split is where real GLM (and its paid ad/keyword tool) concurrency is unbounded; bounding ACTUAL section execution (not HTTP kickoffs) requires moving execution off after() to an expiring DB-lease/pgmq worker (already tracked as task 05).
